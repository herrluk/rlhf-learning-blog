"""算法先修导读。默认读者掌握 Python 基础语法，解释概念、条件与状态。"""
FOUNDATIONS = {
1: {
 'prerequisites':'无需先刷其他章节。先理解“按值查询”和“按位置访问”的区别，以及重复元素是否需要保留。',
 'concepts':[
 ('哈希表解决的是哪一种重复工作','假设读到 7，需要在前面找 2。每次扫描前缀会重复比较；哈希表把值映射到存储位置，通常可以平均常数时间查询。不同键可能落到同一位置，称为冲突，容器仍需比较实际键；所以平均 O(1) 不等于所有情况下都只做一次比较。','顺序扫描：[2, 11, 15] → 逐个比较\n位置映射：{2: 0, 11: 1, 15: 2} → 查询 2 得到原下标 0'),
 ('集合、频次表、位置表分别保留什么','集合回答“是否存在”；频次表回答“有几个”；位置表回答“在哪里”。例如 [2,2,3] 的集合是 {2,3}，频次为 {2:2,3:1}，若只存一个下标就会丢掉次数。先看下一步要查询什么，再决定键和值，不能仅凭题目有重复元素就选集合。','存在：2 → 是\n频次：2 → 2 次\n位置：2 → 某个满足要求的原下标'),
 ('分组键要表达等价关系','两个输入应该分为一组，当且仅当它们的特征相同。异位词忽略字符顺序但保留各字符次数；eat 和 tea 的计数特征相同，eat 和 eatx 不同。特征的构造成本也属于算法时间。','eat / tea → a:1, e:1, t:1 → 同组\neat / eatx → x 的次数不同 → 不同组')]
},
2: {
 'prerequisites':'先掌握数组下标和连续区间。读写指针、相向指针、字典序与位权在本章从例子建立，不要求提前背模板。',
 'concepts':[
 ('区间边界先有含义，再写循环','闭区间 [left,right] 包含两端，元素数是 right-left+1；半开区间 [0,write) 表示已经生成的有效前缀，长度恰好是 write。两种表示服务于不同任务：边界必须与“已经处理”和“尚未处理”的范围对应。','[0,1,2,3] 的闭区间 [1,2] 有 2 项\nwrite=2：下标 [0,2) 已完成，下一个写入位置是 2'),
 ('双指针依赖可证明的区域划分','读写指针将数组分为有效输出、已读但无用、尚未读取的区域；相向指针缩小候选范围。指针移动的理由来自有序性或已确认的性质。例如在排序数组 [1,3,6,9] 中找和 10，1+9 命中；若和太小，固定更小的右值只会更小，因此应增大左值。','过滤：已保留 [0,write) | 已读 [write,read) | 未读 [read,n)\n相向：已排除 | left … right | 已排除'),
 ('字典序与十进制位权','字典序看最左边不同的位置：[1,4,2] 大于 [1,3,9]，后面的 9 无法补偿前面的 3。十进制解析则按位权累计，读到下一位 d 时，旧数乘 10 再加 d；加法进位和乘法落位都由这个表示推导。','字典序：[1, 3, 9] < [1, 4, 2]\n十进制：读完 12，再读 3 → 12×10+3=123')]
},
3: {
 'prerequisites':'先理解连续区间、频次表与双指针的区域含义；可先读第 01、02 章的导读。这里需要进一步证明窗口左端为什么可以不回退。',
 'concepts':[
 ('窗口是原数组的一段连续区间','窗口由 left、right 两个端点表示，不必复制子数组。维护的状态必须精确对应当前区间，例如元素和、零的数量、各值频次；出窗时要撤销离开元素的贡献。子序列允许跳选，通常不能直接用一对窗口端点表示。','nums=[2,1,3,2]，窗口 [1,2] → 元素 [1,3]，和为 4\n右端加入 2：和 6；左端移出 1：和 5'),
 ('可收缩性是窗口算法的前提','最长无重复窗口变得重复后，再向右加字符不能消除已有重复，必须移出左侧元素。正数区间和达标后，收缩才能尝试更短答案。允许负数时这些方向未必成立：加入 -5 会让总和变小，因此必须重新证明排除依据。','最长：扩张 → 不合法时收缩 → 记录合法长度\n最短：扩张 → 合法时先记录，再收缩\n定长：扩张 → 移除超长部分 → 检查内容'),
 ('统计数量时要数出具体起点','若合法窗口的每个后缀也合法，固定 right 后，起点可以取 left 到 right，共 right-left+1 个。例如 [2,3] 的乘积小于 10，则以 3 结尾的 [2,3]、[3] 都合法。恰好 K 种不能直接照搬，要用“至多 K 种”减“至多 K-1 种”。','固定右端：[left … right]、[left+1 … right]、…、[right]\n同一右端共有 right-left+1 个不同区间')]
},
4: {
 'prerequisites':'先理解连续区间与哈希表的键值含义。下面的前缀和基础导读会从累计表开始，说明空前缀、区间相减，再进入统计次数和最早位置。',
 'concepts':[]
},
5: {
 'prerequisites':'先理解第 02 章的区间边界与原地修改。矩阵模拟需要坐标和剩余区域，不需要先掌握图搜索或动态规划。',
 'concepts':[
 ('矩阵坐标与移动方向','m 行 n 列的矩阵用 (r,c) 表示位置：r 向下增加，c 向右增加。向上、下、左、右分别改变一个坐标。非方阵的行列上限不同，不能把所有边界都写成 n。','两行三列：\n(0,0) (0,1) (0,2)\n(1,0) (1,1) (1,2)\n从 (0,1) 向下 → (1,1)；向右 → (0,2)'),
 ('访问顺序与坐标变换是两件事','螺旋遍历决定先访问哪格；旋转矩阵决定每个旧值的新位置。n×n 矩阵顺时针旋转时，(r,c) 去 (c,n-1-r)：旧行变新列，且方向反转。转置再逐行反转正好完成这两个变化。','3×3：旧 (0,1) → 新 (1,2)\n先转置：(0,1) → (1,0)\n再反转该行：(1,0) → (1,2)'),
 ('原地标记需要区分原始事实和处理结果','某行需要清零是原始零触发的事实；写出的新零是结果，不能继续触发传播。标记法先保存事实，再统一生成结果。若借第一行和第一列存标记，它们自身是否原本含零必须另外保留。','原始零 → 行/列标记 → 根据标记写结果\n禁止：刚写出的零 → 再触发其他行列')]
},
6: {
 'prerequisites':'先理解有序数组和区间边界。二分的关键是单调判断、候选范围与边界保留，后面再推广到数值答案和旋转数组。',
 'concepts':[
 ('从有序数组到单调判断','把每个位置是否满足条件写成真假序列。对 [1,3,3,7] 判断“值至少为 3”，得到 假、真、真、真；要找的是第一个真。中点为真时，答案可能更早也可能就是中点，因此保留中点；中点为假时，它及左侧都可排除。','候选位置：0  1  2  3\n值至少 3：假 真 真 真\n边界答案：   ↑ 下标 1'),
 ('二分要维护两个保证','正确性要求答案没有被排除；终止性要求候选范围严格缩小。先定义搜索的是元素下标还是插入边界，再确定 right 是否能取 n。不存在的目标也可能有合法插入位置，边界查找与精确查找的返回条件不同。','精确查找：候选元素 [left,right]，空时 left>right\n边界查找：逐步夹住分界位置，结束时 left=right'),
 ('答案空间也能二分，但判断成本要另算','吃香蕉速度越大，需要小时数不会增加，因此可二分最小可行速度。每次判断必须扫描全部堆，若有 n 堆、候选上界 M，时间是 O(n log M)。局部峰值题则不依赖全局真假单调，需要单独证明保留侧一定有峰。','速度：   小 …………… 大\n可否按时：否 否 否 | 是 是 是\n目标：第一个可行速度')]
},
7: {
 'prerequisites':'先建立节点、连接、前驱和后继的概念；链表题的下标展示只是辅助，算法实际沿连接移动。',
 'concepts':[
 ('节点身份与节点值','节点含值 val 和后继 next。A(2)、B(2) 的值相等，但 A、B 是两个节点；相交与判环比较的是节点身份。单链表无法直接从一个节点走回前驱，要提前保留引用或用遍历定位。','A(2) → B(2) → C(5) → 空\nA 与 B 数值相同，身份不同\n前驱是入边来源；后继是 next 指向的节点'),
 ('改连接之前保住可达性','反转当前节点的出边以后，原后缀可能不再从头可达，所以先保留后继 nxt。prev 是已反转部分的头，cur 是待处理部分的头；每轮把一个节点从后者移到前者。','已反转：prev → … → 空\n待处理：cur → nxt → …\n改边后：cur → prev；接下来从保存的 nxt 继续'),
 ('哨兵与速度差分别解决什么','哨兵 dummy 给真实头节点增加一个固定前驱，使删头、换头与处理中间节点使用相同重连规则。快慢指针利用步数比例找中点、相遇或距离；停止条件决定停在中点、前半尾还是删除前驱，不能混用。','dummy → head → …\n删头：dummy.next 指向原 head.next\n两步快指针与一步慢指针：快走完整段时，慢约走一半')]
},
8: {
 'prerequisites':'先完成第 07 章 206 反转、21 合并、876 找中点，并理解哨兵。复杂链表操作由这些基础动作组合而成。',
 'concepts':[
 ('局部变换先划清四个连接点','before 是处理段前驱，first、last 是段首段尾，after 是后缀入口。反转后 first 变新尾、last 变新头。完成内部改边还不够，必须把 before 接新头、新尾接 after。','变换前：before → first → … → last → after\n反转后：before → last  → … → first → after'),
 ('分治与自底向上归并','归并要求两个输入段已经有序；把长度 1 的段两两合并，得到长度 2，再得到 4、8，直到覆盖全链。每轮处理全部节点，共约 log n 轮。每次切段必须保留下一段入口，否则后续节点会丢失。','轮 1：[4] [2] [1] [3] → [2,4] [1,3]\n轮 2：[2,4] [1,3] → [1,2,3,4]'),
 ('深拷贝复制的是连接关系','新节点不能指回原节点；两个原节点指向同一个目标，副本也要共同指向同一个新目标。可先建立“原节点 → 副本”的映射，再翻译 next 和 random。交织法把副本临时放在原节点旁边，最后必须恢复原链。','原 A.random → 原 C\n映射：原 A→新 A，原 C→新 C\n复制：新 A.random → 新 C')]
},
9: {
 'prerequisites':'先理解连续区间与下标。窗口最大值还需要第 03 章的定长窗口；本章补齐栈、单调性和候选淘汰。',
 'concepts':[
 ('栈和队列的顺序来自问题依赖','栈最后进入的先处理，适合嵌套：遇到右括号，必须先关闭最近尚未关闭的左括号。队列先进入的先处理，适合按层推进。重要的是其中每项代表待匹配字符、未结算下标，还是暂停的上下文。','表达式：外层 ( 内层 ( … ) )\n关闭顺序：内层 → 外层；与打开顺序相反'),
 ('单调栈为何能丢弃候选','单调栈按某种值保持顺序。每日温度中，新温度更高就为栈顶旧日确定了第一个更暖日，旧日任务完成；窗口最大值中，新值更大且更晚过期，会完全替代旧小值。要分别说清“已经能结算”与“以后不可能更好”的依据。','温度 [73,71,74]：读到 74 → 71、73 都找到首个更暖日\n窗口候选 [5,3] 加入 4：3 更小又更早过期，可以删除'),
 ('下标同时保存数值位置和有效范围','只存值无法算等待天数、矩形宽度或判断窗口过期。单调队列通常队首删过期项、队尾删被替代项。每个下标最多入一次、出一次，所以累计操作是 O(n)，即使某一轮连续弹出多项。','窗口 [left,right]：下标 <left 的候选过期\n队首：当前最大候选；队尾：检查是否被新元素替代')]
},
10: {
 'prerequisites':'先理解数组下标、比较排序与第 02 章的分区。多链归并还要会第 07 章的两链合并；矩阵按值二分依赖第 06 章。',
 'concepts':[
 ('堆是部分有序的完全二叉树','完全二叉树除末层外都填满，末层节点从左连续排列。小顶堆要求每个父节点不大于孩子，所以根是全局最小；左右子树及同层节点并不整体排序。数组下标 i 的孩子为 2i+1、2i+2，n 个节点的完全二叉树高度为 O(log n)，插入和删除堆顶沿一条树路径调整。','合法小顶堆数组 [1,3,2,7,4]\n        1\n      /   \\\n     3     2\n    / \\\n   7   4\n数组里的 3 可以排在 2 前面，仍然是堆。'),
 ('保留前 K 个时，堆顶是谁','保留最大的 K 个，需要快速淘汰候选中最小的，因此用小顶堆；保留最近的 K 个点，需要淘汰最远候选，可用负距离实现大顶边界。K 路归并则每路只放最小未输出项，弹出谁，就补入同一路后继。','最大的 3 个候选：[5,8,9]，门槛是 5\n新值 4 → 不值得保留；新值 7 → 淘汰 5'),
 ('完整排序与排名选择','排序要确定所有元素的相对次序；第 K 大只需确定目标名次。快速选择分出小于、等于、大于基准三段，只保留目标所在段。随机化给出期望线性时间，最坏仍可能平方；重复值各占一个名次。','[小于 pivot | 等于 pivot | 大于 pivot]\n升序目标下标 = n-k；只继续包含该下标的一段')]
},
11: {
 'prerequisites':'先理解节点身份、栈与队列（第 07、09 章）。这里从树结构、遍历顺序、子树返回值开始，再学习路径与祖先问题。',
 'concepts':[
 ('二叉树、子树与路径','每个节点最多有两个孩子；以某节点为根，它及全部后代构成一棵子树。根到节点的深度可以按节点数或边数计，必须随题目约定；本章最大深度按节点数，直径按边数。二叉搜索树还要求整棵左子树更小、整棵右子树更大。','    4\n   / \\\n  2   7\n / \\\n1   3\n根到 3：经过 3 个节点、2 条边'),
 ('遍历顺序决定什么时候信息齐全','前序先处理父，中序在左右之间处理父，后序等两个孩子完成才处理父。求高度、直径、最大路径和需要子树结果，因此采用后序；按层输出需要队列，并在一层开始时固定该层节点数。','前序：父 → 左 → 右\n中序：左 → 父 → 右\n后序：左 → 右 → 父\n层序：距离根 0 层 → 1 层 → 2 层'),
 ('返回给父节点的状态与全局答案要分开','求最大路径和时，父节点只能接一条向下分支；经过当前节点的全局候选却能同时连接左右两边。因此返回状态是一条单臂收益，全局答案可以是双臂路径。显式栈中的 expanded 标记表示孩子是否已处理完，模拟递归返回时机。','左分支 → 当前节点 → 右分支：可作为全局路径\n父节点 → 当前节点 → 只选一边：才能继续接向父亲')]
},
12: {
 'prerequisites':'先理解第 09 章的栈、队列和第 11 章的遍历。图允许多个入口指向同一节点，也允许环，所以还需要访问状态。',
 'concepts':[
 ('顶点、边与连通块','图用顶点表示对象、边表示关系。无向边可以双向走，有向边必须按方向走。网格的每个合法格子可以看作顶点，上下左右邻接是边；岛屿就是陆地构成的连通块，不含对角相邻。','1 0 1\n1 0 1\n左列与右列各自连通，共 2 块；隔着水不能连通。'),
 ('DFS 与 BFS 维护不同的待处理顺序','DFS 沿一个分支深入，用栈或递归；BFS 用队列按距离逐层扩张，在每条边代价相同的情况下，首次到达就是最短步数。发现节点时就标记，防止多个前驱重复把它加入待处理集合。','BFS：起点 → 距离 1 → 距离 2\n多源 BFS：多个起点同时在第 0 层，模拟同时扩散'),
 ('拓扑排序：入度就是尚未完成的前置任务数','课程 prerequisite→course 表示先学前者。入度为 0 的课可以立即学习；学完它，就消除它对后续课程的一条限制。若队列空了但仍有未学课程，剩余依赖中有环，无法产生新的起点。','A → B → C：先 A，再 B，再 C\nA → B → A：两个入度都为 1，没有可开始的课程')]
},
13: {
 'prerequisites':'先理解递归调用的展开与返回、列表作为路径、集合记录占用。网格搜索先学第 12 章；这里重点区别全局访问与当前分支访问。',
 'concepts':[
 ('把解空间画成一棵决策树','每层决定一个选择，根到当前节点的路径就是部分答案。子集决定选哪些元素，排列决定每个位置放谁，切分决定下一段在哪结束。先定义一层代表什么，再定义结束条件和可选范围。','排列 [1,2]：\n空路径 → 选 1 → 选 2 → [1,2]\n       → 选 2 → 选 1 → [2,1]'),
 ('回溯撤销的是当前分支的影响','进入下一层前记录选择，返回后撤销，使下一分支从相同父状态开始。path 是当前路径，result 保存已经完成的答案，需要保存快照。若标记用于“本路径不能重复使用”，回退时必须恢复；全图已访问则通常不撤销。','选择 → 修改 path/占用 → 递归 → 撤销\n父状态先后尝试多个兄弟分支，每次起点应相同'),
 ('去重与剪枝各自排除什么','同层去重排除会生成重复答案的兄弟选择；剪枝排除已经无法满足条件的分支。排列 [1a,1b,2] 中，同一层用两个 1 开头会重复，但同一路径仍必须允许使用两个不同位置的 1。','同层：本次开头选 1a 与选 1b 等价，可跳过一个\n同路：选完 1a 后仍可选 1b，才能组成 [1,1,2]')]
},
14: {
 'prerequisites':'先理解有序性、区间端点和第 02 章的候选排除。贪心必须说明局部选择如何保留全局最优；无法证明时，考虑动态规划等方法。',
 'concepts':[
 ('贪心的依据是可替换性','从任意一个最优方案出发，证明可以把它的一步换成我们的选择，答案不会变差，就能放心固定这一步。例如选择互不重叠区间，先取结束最早者，会给后面留下至少同样多的时间。','方案首段结束于 8，我们选的首段结束于 5\n原本能在 8 之后安排的区间，在 5 之后仍能安排'),
 ('区间端点决定等号','合并闭区间 [1,2]、[2,3] 时，共享端点所以要合并；安排不重叠任务通常允许一个结束时另一个开始，此时可以同时保留。气球问题允许在端点射中，等号又属于同一箭可覆盖的情况。','合并：next_start ≤ current_end → 相连\n保留不重叠任务：next_start ≥ previous_end → 可接上'),
 ('历史最优与当前可行边界','股票一次交易要保存卖出日之前的最低价；跳跃可达性保存已确认可达的最远下标。最少跳数还要区分当前跳数覆盖边界 end 和再多跳一次的最远边界 farthest，扫描完整层才增加跳数。','第 t 跳可达区间的边界：end\n遍历这一层所有起跳点 → 得到第 t+1 跳边界 farthest')]
},
15: {
 'prerequisites':'先会描述子问题和遍历顺序。背包依赖容量含义，字符串题依赖前缀长度，网格题依赖坐标；本章先定义状态，再写递推。',
 'concepts':[
 ('动态规划保存的是有明确问题含义的答案','同一个子问题会被反复遇到，就把答案保存起来。状态必须包含后续决策需要的全部信息：最大子数组和保存“必须在当前下标结尾”的最优值；如果只保存全局最大值，就不知道它能否接上当前元素。','nums=[-2,3,-1]\n结尾最优：-2 → 3 → 2\n全局最优：-2 → 3 → 3；两种状态不能互换'),
 ('状态、转移、初值、顺序、答案位置','先定义 dp 下标，再列出最后一步的全部可能。爬楼梯 f(i) 表示到第 i 级的方案数，最后一步来自 i-1 或 i-2，故 f(i)=f(i-1)+f(i-2)。f(0)=1 是空方案，保证统一计数；求最小值时不可达常用正无穷，求最大收益时不合法状态常用负无穷。','先定义子问题 → 枚举最后一步 → 合并来源\n初值让最小子问题可算；填表顺序保证来源已经完成'),
 ('0/1 背包的状态为什么要包含容量','背包把“从一些物品中选择，满足总量限制”抽象成子问题。0/1 表示每件只能不选或选一次。例如数字 [2,3] 要凑出和 5，处理完 2 可达的和是 0、2；再处理 3，可达的和是 0、2、3、5。此时 reachable[s] 表示已处理物品能否凑出和 s，初始只有和 0 可达。完全背包允许同一种物品重复选，像零钱兑换；两者的更新顺序不能混用。','处理 2：不选 → 0；选 → 2\n再处理 3：旧和保留 0、2；旧和加 3 得到 3、5\n每次转移都必须知道旧状态已经允许使用哪些物品。'),
 ('滚动数组压缩存储，不改变状态定义','如果当前行只依赖上一行，可以复用一行，但覆盖后旧值就丢失。以编辑距离为例，外层逐步取第一个字符串的前缀，j 表示第二个字符串的前缀长度。dp[j] 保存这两个前缀之间的最少编辑数。更新前，dp[j] 是上方旧值，dp[j-1] 是左方新值，diagonal 是提前保存的旧左上角。0/1 背包倒序容量，避免同一件物品被本轮重复使用。','更新前：上方 = 旧 dp[j]；左方 = 新 dp[j-1]\n保存旧上方 → 计算新 dp[j] → 旧上方交给下一列当左上'),
 ('子序列与连续子串的递推不同','最长公共子序列在末字符不同的时候可以跳过一边；最长公共连续子数组若末项不同，当前位置结尾的连续长度必须清零。状态里是否包含“连续”“必须结尾”“恰好容量”会直接决定转移。','不连续：不同 → 比较跳过左/右末项的结果\n连续：不同 → 本次共同后缀长度为 0')]
},
16: {
 'prerequisites':'先掌握哈希表、栈/队列、双向链表、堆（第 01、07、09、10 章）。设计题要求这些结构在多次操作后始终一致。',
 'concepts':[
 ('接口背后是一组长期保持的关系','设计题不是单次计算答案，状态会在多次调用之间保留。先写每个操作的可见效果，再写结构之间的对应关系。例如最小栈的两个栈必须等长，LRU 的哈希记录与链表节点必须一一对应。','一次更新 → 状态改变 → 检查关系仍成立 → 下一次操作\n只测最终结果，可能遗漏中间状态已经损坏的问题'),
 ('将定位、顺序和极值交给合适结构','哈希表按键平均常数时间定位，双向链表用已知节点常数时间摘除和插入，堆维护极值。LRU 将定位与最近使用顺序组合；中位数把较小半与较大半分开，只需知道两边的边界值。','LRU：key → 节点；链表头最近、尾最久\n中位数：较小半最大值 | 较大半最小值'),
 ('均摊复杂度与淘汰规则','双栈队列的一次搬运可能 O(n)，但每项一生只搬一次，整个操作序列均摊 O(1)。LFU 则先比较访问频次，再在同频次内比较最近使用时间，需要频次桶与桶内顺序同时维护。','LRU：最近一次使用最早者淘汰\nLFU：频次最小 → 若并列，再淘汰最久未使用者')]
},
17: {
 'prerequisites':'先阅读题目对重复次数、输入范围和能否修改数组的约束。287 还依赖第 07 章的环入口；其余数学性质在下面单独解释。',
 'concepts':[
 ('抵消法必须依赖具体约束','异或满足 x⊕x=0、x⊕0=x，且次序可交换，所以其他数恰好成对时，只剩唯一数。多数投票每次抵消两个不同值；一个值超过一半时，无论怎样配对都不能被其他值全部抵消。votes 是未抵消票数，不是出现总次数。','[2,1,2] → 2⊕2⊕1 = 1\n多数值 5 次、其他值总共 4 次 → 至少剩 1 票'),
 ('有限答案范围可以变成位置约定','长度 n 的数组，最小缺失正数一定在 1..n+1：若前 n 个正数齐全，答案 n+1；否则答案在其中。允许修改输入时，可约定值 x 应放在下标 x-1。重复数找环则把“下标 → nums[下标]”看作出边，不能改写输入。','n=3：只需检查 1、2、3 是否齐全\n位置约定：值 1→下标 0，值 2→下标 1，值 3→下标 2'),
 ('二进制幂与等概率样本空间','指数分解为二的幂之和，例如 13=8+4+1，所以只需反复平方并乘入选中的幂。rand10 则要求每个结果对应相同数量的等概率原样本：两次独立 rand7 有 49 种，保留前 40 种，每 4 种映射到一个结果。','x^13 = x^8 × x^4 × x\n49 个等概率样本 → 接受 40 个 → 10 组，每组 4 个\n尾部 9 个拒绝重抽，直接取模会产生偏差')]
}
}
