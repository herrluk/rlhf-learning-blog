from textwrap import dedent

CHAPTER={
 'lead':'题目约束可以提供额外信息，但每个技巧都有适用条件。先解释为什么允许消去、放回或映射，再讨论常数空间实现。',
 'intro':['这一章不是一套统一模板。136 利用异或抵消重复值，169 利用超过一半的多数性质，41 利用答案的有限范围，287 利用值与下标的对应关系，50 利用指数的二进制分解，470 利用等概率样本空间。','遇到额外空间必须为 O(1) 的要求，不要直接猜某个技巧。先写朴素解法，再列出输入约束究竟保证了什么。例如 41 允许修改数组，可以把数组本身当作标记表；287 明确禁止修改，就需要另一种信息利用方式。','本章按照 136 → 169 → 41 → 287 → 50 → 470 学习。287 建议先理解链表环入口的快慢指针；470 的等概率推导可作为独立数学专题，不能只背一个取模表达式。'],
 'sections':[
  {'title':'三个常见的条件变化','body':['“其他数字恰好出现两次”变成“出现三次”，136 的异或抵消便不能直接使用。','“保证存在超过一半的数”变成“不一定存在”，169 的候选者需要第二遍统计验证。','“允许原地修改”变成“禁止修改”，41 的换位方法不能直接搬到 287。算法正确性与题目限制需要一起记忆。']},
  {'title':'常数空间不代表只有一种操作','body':['几项整数状态、固定数量指针、输入中的可复用位置，都可能减少额外存储。但修改输入与否是另一个维度，必须单独说明。','随机算法的期望时间与最坏时间也要区分。470 通常很快，但拒绝采样理论上可能重试任意多次，不能声称每次最多调用固定次数。'],'diagram':'已知限制 → 能证明的性质 → 可维护的状态\n两两重复 → 异或成零 → 一个累积值\n数量过半 → 异类抵消后仍占优 → 候选者与票数\n值在 1..n → 可映射到合法下标 → 数组换位或函数图\n均匀随机 → 等概率样本点 → 整组映射与拒绝采样'}
 ],
 'apis':[{'signature':'a ^ b -> int\nn & 1 -> int\nn >>= 1','description':['^ 是按位异或，不是乘方。每个二进制位相同得到 0，不同得到 1，因此 x^x=0、x^0=x。Python 的乘方符号是 **。','对于本章快速幂使用的非负整数 n，n&1 读取最低位，结果为 0 或 1；n>>=1 将 n 右移一位，相当于整除 2。这里先把负指数转为正数后才做位运算。']}],
 'problems':[]
}

CHAPTER['problems'].append({
 'id':136,'slug':'single-number',
 'summary':'非空整数数组中，恰好有一个元素出现一次，其余每个元素都出现两次，找出只出现一次的值。要求线性时间与常数额外空间。',
 'baseline':'用字典统计每个值的次数，再找次数为 1 的项，时间 O(n)、空间 O(n)。排序后按相邻值配对也能处理，但需要 O(n log n) 时间，并可能修改输入。题目“其他值恰好两次”的保证让我们可以直接把成对信息消去。',
 'insight':'异或满足 x^x=0、x^0=x，而且具有交换律和结合律。把数组所有值异或在一起，成对的值无论相隔多远都可在代数上放到一起抵消，最终只剩出现一次的值。',
 'steps':['初始化 answer=0，它是异或的单位元。','扫描每个 x，执行 answer ^= x，将当前值并入累计异或。','遍历结束后返回 answer。不需要统计位置、排序或保存已经出现的值。'],
 'invariant':'处理前 i 个元素后，answer 等于这个前缀所有值的异或。整体处理完后，每个出现两次的值都贡献 x^x=0，仅出现一次的值贡献自己；交换律保证出现顺序不影响最终结果。',
 'examples':[{'label':'成对数字可以不相邻','input':'nums=[4,1,2,1,2]','output':'4','frames':[
  {'title':'从 0 开始','note':'0^x=x，因此初始 0 不会改变最终结果。','array':[4,1,2,1,2],'metrics':[['answer',0]]},
  {'title':'依次读 4、1、2','note':'4^1=5，5^2=7。中间的 7 不是一个候选答案，只是当前前缀的累计异或。','array':[4,1,2,1,2],'active':[0,1,2],'metrics':[['answer',7]],'diagram':'4 = 100\n1 = 001\n2 = 010\n异或 111 = 7'},
  {'title':'再读一个 1','note':'7^1=6。前缀中的两个 1 已经在异或中抵消。','array':[4,1,2,1,2],'active':[0,1,2,3],'metrics':[['answer',6]]},
  {'title':'最后一个 2 也抵消','note':'6^2=4，只剩未配对的 4。并不要求相同数字在数组中相邻。','array':[4,1,2,1,2],'active':[0,1,2,3,4],'equation':'4 ^ (1 ^ 1) ^ (2 ^ 2) = 4'}
 ]}],
 'walkthrough':['前缀累计值可以是数组里从未出现过的值，如本例中间得到 7；因此不能把每一步 answer 理解为已找出的唯一元素。','完整扫描后再利用两次出现的保证，才能得到所有其他值都恰好抵消。','数组 [0,1,1] 会返回 0，初始化为 0 并不意味着不能处理答案本身是零。'],
 'code':'''
class Solution:
    def singleNumber(self, nums: list[int]) -> int:
        # 异或的单位元是 0；相同值两次异或会抵消。
        answer = 0
        for x in nums:
            # 结合律与交换律允许重复值跨位置抵消，最后只留下出现一次的数。
            answer ^= x
        return answer
'''.strip(),
 'code_notes':['^= 是把异或结果重新赋给 answer，等价于 answer=answer^x。','0 是异或累积的正确起点；随意用其他常数初始化会让最终答案也异或上那个常数。','函数不修改 nums，也不建立集合；只有 answer 与循环变量使用额外状态。'],
 'pitfalls':['^ 不是 Python 的乘方，2^3 得到 1，而 2**3 才得到 8。','如果其他值出现三次，x^x^x=x，它们不会消去；必须使用不同的方法。','若有两个值各出现一次，最终得到的是它们的异或，不能直接拆出两者。'],
 'complexity':'在题目限定整数范围下，时间 O(n)，额外空间 O(1)。',
 'quiz':{'question':'为什么 [2,2,3,3,5] 可以不排序就得到 5？','answer':'异或的交换律和结合律允许在推导中重新分组，两个 2 与两个 3 都抵消为零。代码不必真正移动元素，只需按原顺序累计。'},
 'tests':{'method':'singleNumber','cases':[{'args':[[4,1,2,1,2]],'expected':4},{'args':[[2,2,1]],'expected':1},{'args':[[0,1,1]],'expected':0},{'args':[[-2,-2,-7]],'expected':-7},{'args':[[9]],'expected':9}]}
})

CHAPTER['problems'].append({
 'id':169,'slug':'majority-element',
 'summary':'给定非空数组，找出出现次数严格超过数组长度一半的元素。题目保证这样的多数元素存在。目标是用线性时间、常数额外空间求出它。',
 'baseline':'字典计数可以 O(n) 时间找到超过 n/2 的值，但需要 O(n) 空间；排序后取中间元素也正确，因为超过一半必然覆盖中间位置，但排序通常需要 O(n log n) 时间。进一步利用“多数对其他所有元素总和仍占优”，可以做成对抵消。',
 'insight':'每次删去两个不同值，不会把真正多数的优势完全消掉：若删去了多数元素，同时也删去一个非多数；若删去的都不是多数，多数占比反而提高。把这种抵消在线执行，只保留尚未配对的一种候选及其净票数即可。',
 'steps':['用 candidate 保存当前未抵消的值，用 votes 保存它尚未抵消的票数，初始 votes=0。','读到 x 时，若 votes==0，说明此前前缀已全部成对抵消，可把 x 设为新的 candidate。','若 x==candidate，votes 加一；否则与一个候选票配对抵消，votes 减一。','全部扫描结束后返回 candidate。依赖题目保证，多数元素必定是最终剩余候选。'],
 'invariant':'处理过的前缀可分成若干对不同值，以及 votes 个相同的 candidate。抵消对可以忽略；由于整体多数元素严格多于所有其他值，它不可能全部被不同值配对消去，因此最终非空剩余一定属于多数元素。',
 'examples':[{'label':'候选更换不影响整体多数','input':'nums=[2,2,1,1,1,2,2]','output':'2','frames':[
  {'title':'先得到两张 2 的票','note':'前两个元素都是 2，candidate=2、votes=2。票数表示未抵消数，不是最终出现次数。','array':[2,2,1,1,1,2,2],'active':[0,1],'metrics':[['candidate',2],['votes',2]]},
  {'title':'两个 1 抵消两个 2','note':'读到下标 2、3 的 1 后 votes 依次减为 1、0，前四个数可以分成两对 (2,1)。','array':[2,2,1,1,1,2,2],'active':[0,1,2,3],'metrics':[['votes',0]],'diagram':'(2,1) 抵消；(2,1) 抵消 → 没有剩余票'},
  {'title':'下一个 1 成为新候选','note':'votes=0 时读到下标 4 的 1，设 candidate=1，并把本轮这个 1 计入，votes=1。','array':[2,2,1,1,1,2,2],'active':[4],'metrics':[['candidate',1],['votes',1]]},
  {'title':'再与一个 2 抵消','note':'下标 5 的 2 与候选 1 不同，votes 回到 0。候选变量虽然暂时还是 1，但零票表示它已没有有效支持。','array':[2,2,1,1,1,2,2],'active':[4,5],'metrics':[['votes',0]]},
  {'title':'最后一个 2 留下','note':'下标 6 重新选 candidate=2、votes=1。整体有四个 2、三个 1，多数 2 无法被完全抵消。','array':[2,2,1,1,1,2,2],'active':[6],'metrics':[['最终候选',2],['未抵消票',1]]}
 ]}],
 'walkthrough':['前四个数恰好抵消为零，不表示此前出现过的 2 不可能成为最终多数，而是这段平衡前缀不影响剩余部分中的多数归属。','第五个数暂时把候选改成 1，后面又被抵消。候选可以更换多次，算法不依赖第一次猜测正确。','最终 votes=1，而多数 2 实际出现四次，说明 votes 是抵消后的差额，不是频次数组的替代查询。'],
 'code':'''
class Solution:
    def majorityElement(self, nums: list[int]) -> int:
        candidate = nums[0]
        # votes 表示当前候选在抵消过程中的净票数，不是它的总出现次数。
        votes = 0
        for x in nums:
            # 旧候选已被完全抵消，从当前数重新开始。
            if votes == 0:
                candidate = x
            # 相同值加票，不同值减票，相当于不断抵消两个不同元素。
            if x == candidate:
                votes += 1
            else:
                votes -= 1
        # 原题保证存在超过一半的众数，它不可能被其他值全部抵消。
        return candidate
'''.strip(),
 'code_notes':['两个 if 不能随意合成 if/elif：当 votes=0 选出新候选后，本轮的 x 还需要贡献一票。','votes=0 时 candidate 的旧值无关紧要，下一个元素会重新选候选。','candidate 从 nums[0] 初始化依赖非空数组；它只是初始值，真正的选择受 votes 是否为零控制。'],
 'pitfalls':['“多数元素”是严格超过一半，不是出现次数最多但可能不到一半的众数。','如果不保证多数存在，第一遍只能给候选，必须第二遍统计其次数是否 >n//2。','votes 不是 candidate 的真实总频次，不能用 votes>n//2 代替第二遍验证。'],
 'complexity':'时间 O(n)，额外空间 O(1)；若扩展题不保证多数存在，增加一次验证后仍是 O(n)。',
 'quiz':{'question':'对 [1,2,3] 运行会留下候选 3，能据此认定 3 是多数吗？','answer':'不能，这个输入没有超过一半的元素，违反原题存在性保证。通用版本需再数 3 的出现次数，发现只有一次，不大于 3//2，才返回“不存在多数”。'},
 'tests':{'method':'majorityElement','cases':[{'args':[[2,2,1,1,1,2,2]],'expected':2},{'args':[[3,2,3]],'expected':3},{'args':[[1]],'expected':1},{'args':[[0,0,1]],'expected':0},{'args':[[-1,2,-1,-1,3]],'expected':-1}]}
})

CHAPTER['problems'].append({
 'id':41,'slug':'first-missing-positive',
 'summary':'给定未排序整数数组，返回没有出现过的最小正整数。数组可能含负数、零和重复值。要求 O(n) 时间、O(1) 额外空间，允许原地修改输入。',
 'baseline':'用集合保存出现过的正数，再从 1 起检查第一个缺失值，时间 O(n)、空间 O(n)。排序也能找缺口，但时间为 O(n log n)。常数空间优化需要想办法把集合信息存回数组本身。',
 'insight':'长度为 n 的数组，答案一定在 1..n+1：如果 1..n 全出现，答案就是 n+1；否则其中必有缺失。因此只关心 1..n，把正整数 x 放到下标 x−1，数组本身就成了“这个数有没有出现”的标记表。其他数没有对应的有效位置，可暂时忽略。',
 'steps':['从下标 i=0 开始检查当前位置 x=nums[i]。','若 1≤x≤n，而且它应该去的位置 nums[x−1] 还不是 x，就交换当前位置和下标 x−1 的值。','交换后留在同一个 i 继续检查，因为换回来的新值也可能需要放到自己的位置。直到它越界、已就位或与目标位重复，才处理下一个 i。','全部位置整理后，从左向右寻找第一个 nums[i]!=i+1 的位置，返回 i+1。','如果每个位置都等于下标加一，说明 1..n 全部存在，返回 n+1。'],
 'invariant':['每次交换都会把一个在 1..n 范围内的数放到它唯一正确的位置。已经正确的位置不会被破坏：只有目标格不是这个数时才交换，而一个正确的源格也不会满足交换条件。','因此虽然有 while 嵌套，每次有效交换至少新增一个永久正确的位置，总交换次数不超过 n。整理完成后，如果 x 曾出现且在 1..n 中，它的目标位置一定会保留 x，或在检查时发现已有一个相同 x。第一个未就位的位置正是最小缺失正数。'],
 'examples':[{'label':'换回来的值也要继续处理','input':'nums=[3,4,-1,1]','output':'2；整理后的数组为 [1,-1,3,4]','frames':[
  {'title':'i=0 的 3 应去下标 2','note':'范围 1..4 内的 3 对应下标 2。交换 3 和 −1，得到 [-1,4,3,1]；3 永久就位。','array':[3,4,-1,1],'active':[0,2],'pointers':{'i':0,'目标':2}},
  {'title':'当前位置变为 −1，先跳过','note':'−1 不在 1..4 内，没有需要维护的目标位置。移动到 i=1。','array':[-1,4,3,1],'active':[2],'pointers':{'i':0}},
  {'title':'i=1 的 4 应去下标 3','note':'交换 4 和 1，得到 [-1,1,3,4]。此时不能立刻 i+=1，换回的 1 还没有就位。','array':[-1,1,3,4],'active':[3],'pointers':{'i':1}},
  {'title':'同一个 i，把 1 放回下标 0','note':'交换 1 和 −1，得到 [1,-1,3,4]。1、3、4 都已就位，−1 再次被忽略。','array':[1,-1,3,4],'active':[0,2,3],'pointers':{'i':1}},
  {'title':'扫描第一个不匹配的位置','note':'下标 0 是 1，正确；下标 1 应该放 2，却是 −1，因此最小缺失正数为 2。','array':[1,-1,3,4],'active':[1],'pointers':{'缺口':1},'equation':'第一个 nums[i]≠i+1 的 i=1 → 返回 2'}
 ]},{'label':'重复值必须停止交换','input':'nums=[1,1]','output':'2','frames':[
  {'title':'第二个 1 的目标格已经是 1','note':'i=1 时 x=1，目标 nums[0] 也等于 1。交换两个相同值毫无进展，所以直接跳过。','array':[1,1],'active':[0,1],'pointers':{'i':1,'目标':0}},
  {'title':'下标 1 没有放着 2','note':'扫描结果时发现 nums[1]=1≠2，返回 2。重复的 1 不会占据 2 的存在标记。','array':[1,1],'active':[1],'metrics':[['答案',2]]}
 ]}],
 'walkthrough':['数组不需要被完整排序，负数和超范围值最后停在哪里都无所谓。本题只要求 1..n 中存在的值能回到指定位置。','i=1 连续交换两次说明这里需要 while；只交换一次会留下仍可归位的 1，可能导致错误缺口。','每次交换带来至少一个新正确位置，总交换数受 n 限制。这比只说“指针扫描一次”更准确，因为同一 i 的确可能处理多次。'],
 'code':'''
class Solution:
    def firstMissingPositive(self, nums: list[int]) -> int:
        # 长度为 n 时，第一个缺失正数一定在 1..n+1 内。
        n = len(nums)
        for i in range(n):
            # 只把 1..n 内的数送回“值减一”的目标下标；重复值到位后必须停止。
            while 1 <= nums[i] <= n and nums[nums[i] - 1] != nums[i]:
                # 先单独保存目标位置，防止交换时读取到已经被改写的 nums[i]。
                target = nums[i] - 1
                nums[i], nums[target] = nums[target], nums[i]
        for i in range(n):
            # 重排后第一次发现位置与值不符，这个位置应有的正数就是答案。
            if nums[i] != i + 1:
                return i + 1
        return n + 1
'''.strip(),
 'code_notes':['and 短路求值：先确认 nums[i] 在 1..n 范围内，才用它减一作下标，避免负数或大数造成错误索引。','target 在交换前单独计算。不要在多目标赋值左侧重复使用会被第一次赋值改变的 nums[i] 来计算第二个目标下标。','nums[target]!=nums[i] 同时处理已经就位与重复值，保证交换真正推动进度。','函数会修改输入顺序，符合本题允许原地整理的前提；如果调用方还需要原始顺序，应在题目之外自行保存副本，并承认额外空间。'],
 'pitfalls':['漏掉重复值检查时，[1,1] 会不断交换两个相同的 1，陷入死循环。','使用 if 只换一次会漏掉换回来的新值，需要在同一 i 持续处理。','答案不是数组最大值加一；例如 [3,4,-1,1] 的答案是中间缺口 2。'],
 'complexity':'时间 O(n)，有效交换总数至多 n，再加两次线性下标遍历；额外空间 O(1)，代价是修改输入。',
 'quiz':{'question':'为什么可以忽略大于 n 的正数，比如长度为 3 的数组里的 100？','answer':'三个位置最多覆盖 1、2、3 这三个候选；若它们都在，答案为 4，否则答案更小。100 不会决定 1..4 中第一个缺口，所以无需为它分配标记位置。'},
 'tests':{'method':'firstMissingPositive','cases':[{'args':[[3,4,-1,1]],'expected':2},{'args':[[1,2,0]],'expected':3},{'args':[[7,8,9,11,12]],'expected':1},{'args':[[1,1]],'expected':2},{'args':[[2,1]],'expected':3},{'args':[[1]],'expected':2}]}
})

CHAPTER['problems'].append({
 'id':287,'slug':'find-the-duplicate-number',
 'summary':'数组长度为 n+1，每个元素的值都在 1..n，恰好有一种值出现至少两次，找出这个重复值。要求不修改数组，只使用常数额外空间。重复值可能出现超过两次。',
 'baseline':'集合能在 O(n) 时间找出重复值，但需要 O(n) 空间；排序或第 41 题的原地换位会修改输入，均不符合本题限制。单纯用总和减去 1..n 的和也不通用，因为重复值可能出现多次，同时某些值缺失。',
 'insight':['把下标当作节点，把 nums[i] 当成从节点 i 出发的下一跳。由于值在 1..n 内，下一跳总是合法下标，而且不会回到下标 0。从 0 沿下一跳不断走，在有限节点中必然进入一个环。','环入口既被尾部最后一个节点指向，又被环内前驱指向，它有两个不同来源的下一跳都等于自己。这意味着入口下标本身就是数组中出现重复的值。于是题目转为第 142 题的环入口查找。'],
 'steps':['slow、fast 都从下标 0 开始。每轮 slow=nums[slow] 走一步，fast=nums[nums[fast]] 走两步，直到移动后相遇。','第一次相遇只证明进入了同一个环，相遇位置不一定是入口，不能直接返回。','把 finder 放回下标 0，slow 保留在相遇位置。每轮两者各走一步。','当 finder==slow 时，它们位于环入口，返回 finder。这里返回的下标值正好也是重复数。'],
 'invariant':['把从 0 到入口的距离记为 μ，环长记为 λ。第一次相遇时，slow 总共走 t 步，fast 走 2t 步；两者在同一点，所以 t 是 λ 的整数倍。相遇点相对入口的环内偏移为 (t−μ) mod λ。','第二阶段，finder 从 0 再走 μ 步到入口；slow 从相遇点走 μ 步，其环内偏移变为 t mod λ=0，也到入口。finder 在这之前还在尾部，不会与环内 slow 提前相遇，因此第一次重新相遇就是入口。'],
 'examples':[{'label':'第一次相遇在 4，入口却是 2','input':'nums=[1,3,4,2,2]','output':'2','frames':[
  {'title':'先把值解释为下一跳下标','note':'节点 3 的下一跳为 2，节点 4 的下一跳也为 2，这两个不同来源说明值 2 重复出现。','array':[1,3,4,2,2],'diagram':'0 → 1 → 3 → 2 → 4 → 2 → 4 → ...\n尾部：0 → 1 → 3\n环：2 → 4 → 2；入口为 2'},
  {'title':'第一阶段：快慢指针在 4 相遇','note':'每轮先移动再比较。走四轮后 slow=fast=4，但 4 不是重复数，不能在这里结束。','array':[1,3,4,2,2],'active':[4],'table':{'headers':['轮次','slow：一步','fast：两步'],'rows':[[0,0,0],[1,1,3],[2,3,4],[3,2,4],[4,4,4]]}},
  {'title':'finder 回到 0，slow 留在 4','note':'第二阶段两者都一步一步走，不再让任何指针走两步。尾部长度 μ=3，环长 λ=2。','array':[1,3,4,2,2],'pointers':{'finder':0,'slow':4},'metrics':[['μ',3],['λ',2]]},
  {'title':'同步走三步，在入口 2 相遇','note':'finder 依次到 1、3、2，slow 依次到 2、4、2。最终相遇于 2，返回这个重复值。','array':[1,3,4,2,2],'active':[2],'table':{'headers':['第二阶段步数','finder','slow'],'rows':[[0,0,4],[1,1,2],[2,3,4],[3,2,2]]}}
 ]}],
 'walkthrough':['数组值与下标的取值范围高度重合，才允许把 nums[i] 当作下一跳。若包含负数或任意大整数，映射就不能直接成立。','下标 0 没有任何入边，因为所有值至少为 1，因此它不在环里。进入环时的入口必有尾部与环内两个不同前驱，正好对应重复值。','本例第一次相遇点为 4，真正重复值是 2，展示了为什么必须有第二阶段。','重复值出现超过两次时仍可使用这套映射；入口可以有更多入边，不影响 Floyd 沿一条确定路径查找入口。'],
 'code':'''
class Solution:
    def findDuplicate(self, nums: list[int]) -> int:
        # 把下标看成节点，nums[index] 看成 next 指针；重复值对应环入口。
        slow = fast = 0
        # 先用快慢指针找到环内相遇点，不修改数组，也不另建集合。
        while True:
            slow = nums[slow]
            fast = nums[nums[fast]]
            if slow == fast:
                break
        # 一个指针回到起点 0，另一个留在相遇点，两者每次走一步。
        finder = 0
        while finder != slow:
            finder = nums[finder]
            slow = nums[slow]
        # 再次相遇的位置是环入口，它的编号就是重复的数。
        return finder
'''.strip(),
 'code_notes':['slow、fast、finder 保存的都是节点下标，nums[slow] 才是下一跳，不是对当前值做算术加一。','第一阶段先移动再判断相等，否则两个指针初始同在 0 会被误认为已经找到了环内相遇点。','fast 的两次索引都合法，因为每个读取值仍在 1..n 内；无需真实创建链表节点。','第二阶段只重置一个指针，并让两者同速走。若仍让一个走两步，就不再满足入口距离的推导。'],
 'pitfalls':['不能直接返回第一阶段的相遇位置，它可能是环上其他节点。','不能原地排序、标负或交换，因为题目明确要求输入保持不变。','不能假设重复值恰好出现两次，例如 [3,3,3,3,3] 仍是合法输入；简单求和差会返回 5 而非 3。'],
 'complexity':'时间 O(n)，进入环、环内追赶和寻找入口都只需线性数量的跳转；额外空间 O(1)，不修改 nums。',
 'quiz':{'question':'为什么环入口下标能直接当作重复值返回？','answer':'边 i→nums[i] 的终点就是数组值。入口分别由尾部前驱和环内前驱指向，说明这两个不同下标存储了相同的入口值；题目又保证只有一种重复值，所以它就是所求。'},
 'tests':{'method':'findDuplicate','preserve_args':[0],'cases':[{'args':[[1,3,4,2,2]],'expected':2},{'args':[[3,1,3,4,2]],'expected':3},{'args':[[3,3,3,3,3]],'expected':3},{'args':[[1,1]],'expected':1},{'args':[[2,2,2,2,2]],'expected':2}]}
})

CHAPTER['problems'].append({
 'id':50,'slug':'powx-n',
 'summary':'实现实数 x 的整数次幂 xⁿ，指数 n 可以为负数或零。负指数表示倒数，题目保证零底数不会配零或负指数。需要避免逐次相乘 |n| 次。',
 'baseline':'正指数时重复相乘 n 次，负指数先转成倒数再重复，时间 O(|n|)。n 可以非常大，数十亿次循环不可接受。利用指数每次减半，可以把乘法次数降到对数量级。',
 'insight':'指数的二进制表示把乘方分解成若干个 2 的幂次。例如 13=8+4+1，所以 x¹³=x⁸·x⁴·x。不断把 base 平方，就依次得到 x、x²、x⁴、x⁸；每次读取指数最低位，若为 1 就把对应 base 乘入答案，然后右移指数。',
 'steps':['若 n<0，把底数改为 1/x，把指数改为 −n。之后只处理非负指数；n=0 时答案直接保持为 1。','初始化 answer=1、base=x、remaining=n。answer 收集已经决定要乘入的因子。','当 remaining 的最低位为 1，即 remaining&1 非零时，执行 answer*=base。最低位为 0 则跳过这个因子。','执行 base*=base，准备下一个二进制位对应的幂；再 remaining>>=1，丢掉已处理的最低位。','指数右移到 0 后，所有需要的幂都已乘入，返回 answer。'],
 'invariant':'忽略浮点舍入时，每轮开始都满足 answer×base^remaining 等于转换负指数之后的目标幂。若 remaining 为偶数，平方 base 并将指数减半保持乘积不变；若为奇数，先把一个 base 乘入 answer，再对剩余偶数部分平方和减半。结束时 remaining=0，answer 即为目标值。',
 'examples':[{'label':'2 的 13 次方：只处理四个二进制位','input':'x=2.0, n=13','output':'8192.0','frames':[
  {'title':'13 的二进制为 1101','note':'从最低位开始读取：1、0、1、1，分别对应选择 2¹、跳过 2²、选择 2⁴、选择 2⁸。','diagram':'13 = 8 + 4 + 1\n二进制： 1  1  0  1\n对应幂： x⁸ x⁴ x² x¹\n\n2¹³ = 2⁸ × 2⁴ × 2¹','metrics':[['answer',1],['base',2],['remaining',13]]},
  {'title':'最低位 1，乘入 2','note':'answer=2；base 平方为 4；remaining 从 13 右移为 6。剩余工作等价于 2×4⁶。','diagram':'answer × base^remaining\n2 × 4⁶ = 2¹³','metrics':[['answer',2],['base',4],['remaining',6]]},
  {'title':'最低位 0，跳过 4','note':'6 是偶数，这一位不贡献因子。answer 仍为 2，base 平方为 16，remaining=3。','diagram':'2 × 16³ = 2¹³','metrics':[['answer',2],['base',16],['remaining',3]]},
  {'title':'最低位 1，乘入 16','note':'answer=2×16=32，base 平方为 256，remaining=1。','diagram':'32 × 256¹ = 2¹³','metrics':[['answer',32],['base',256],['remaining',1]]},
  {'title':'最后一位 1，乘入 256','note':'answer=32×256=8192，remaining=0，循环结束。未使用的下一轮 base 不会影响答案。','diagram':'2 × 16 × 256 = 8192','metrics':[['answer',8192],['remaining',0]]}
 ]},{'label':'负指数先转为倒数','input':'x=2.0, n=-2','output':'0.25','frames':[
  {'title':'把负指数转成正指数','note':'2⁻²=(1/2)²，令 base=0.5、remaining=2。不是让结果本身取负。','diagram':'2⁻² = (0.5)²'},
  {'title':'跳过最低位，再选择平方后的因子','note':'2 的最低位为 0，先将 base 平方为 0.25；下一轮 remaining=1，乘入 answer，返回 0.25。','diagram':'二进制 10：跳过 0.5，选择 0.25','metrics':[['answer',0.25]]}
 ]}],
 'walkthrough':['普通方案要为指数 13 做多次重复乘 2，而快速幂只处理四个二进制位，每次至多两次乘法。指数增大时节省会非常明显。','answer 与 base 含义不同：answer 是已选因子的乘积，base 是当前二进制位对应的幂，不需要每一轮都把 base 乘入。','负指数转为倒数之后使用完全相同的循环。指数为零则无需进入循环，乘法单位元 1 就是正确答案。'],
 'code':'''
class Solution:
    def myPow(self, x: float, n: int) -> float:
        # 负指数先取底数倒数、指数改为正；原题保证这种情况下底数非零。
        if n < 0:
            x = 1.0 / x
            n = -n
        # answer 累计被选中的幂，初始 1 也自然处理 n=0。
        answer = 1.0
        base = x
        remaining = n
        while remaining > 0:
            # 当前二进制位为 1，就把这一位对应的 base 乘入答案。
            if remaining & 1:
                answer *= base
            # 指数右移一位时，底数平方，对应 x、x²、x⁴ 等幂。
            base *= base
            remaining >>= 1
        return answer
'''.strip(),
 'code_notes':['remaining&1 返回 0 或 1，在 if 中分别视为假或真，用来判断当前最低位是否被选中。','remaining>>=1 对非负整数相当于整除 2，循环次数等于指数的二进制位数。','n=-n 在 Python 中不会因最小固定宽度整数而溢出；迁移到固定宽度整型语言时，要在取负前扩宽类型。','实数乘法使用浮点数，会有正常舍入误差。验证非整数例子应使用合理误差比较，而不是要求十进制打印结果逐字符相同。'],
 'pitfalls':['负指数表示倒数，不是结果取负。','把 base 每轮乘原始 x，会得到 x、x²、x³，无法对应指数每次减半的过程；必须平方。','Python 中 ^ 是异或，不是幂运算。此题练习快速幂，也不应直接调用内置 pow 或 ** 跳过推导。'],
 'complexity':'时间 O(log(|n|+2))，每轮将非负指数减半；迭代版额外空间 O(1)。',
 'quiz':{'question':'为什么 answer 要从 1.0 开始，而不是从 x 开始？','answer':'1 是乘法单位元，可以表示尚未选择任何因子；它也让 n=0 时自然返回 1。若先放入 x，后续按二进制位选择会多乘一个 x，尤其零指数时直接错误。'},
 'tests':{'method':'myPow','compare':'approx','cases':[{'args':[2.0,13],'expected':8192.0},{'args':[2.0,-2],'expected':0.25},{'args':[2.1,3],'expected':9.261},{'args':[-2.0,3],'expected':-8.0},{'args':[3.0,0],'expected':1.0},{'args':[0.0,5],'expected':0.0},{'args':[-1.0,-2147483648],'expected':1.0}]}
})

CHAPTER['problems'].append({
 'id':470,'slug':'implement-rand10-using-rand7',
 'summary':'已提供 rand7()，每次均匀产生 1..7 中的一个整数。只调用这个接口，实现均匀产生 1..10 的 rand10()，不能使用语言自带的随机函数。评测中的 n 是调用次数，不是 rand10 的参数。',
 'baseline':'直接对 rand7() 取模无法覆盖 8、9、10。把两次结果相加虽然扩大了范围，但不同和的组合数不同，例如和为 2 只有 (1,1)，和为 8 却有七种组合，不能当作均匀随机数。需要先构造真正等概率的样本点。',
 'insight':['连续两次独立调用得到有序对 (a,b)，共 7×7=49 个等概率组合。通过 value=(a−1)×7+b，把它们一一编号为 1..49。','49 不是 10 的整数倍，直接取模会让部分结果更常见。只接受 1..40，拒绝 41..49 并重新抽取；40 个等概率结果可以均分给 10 个输出，每个输出恰好有 4 个来源。'],
 'steps':['循环调用两次 rand7()，将结果 a、b 映射为 value=(a−1)*7+b。','若 value>40，本轮样本位于不能均分的尾部，丢弃它并重新抽两次。','若 value≤40，把编号先减一变为 0..39，再 %10 得到 0..9，最后加一返回 1..10。','不要设置固定重试次数后随意返回某个值；那会破坏等概率。基础实现的期望调用次数有限，但最坏重试次数不受固定上界限制。'],
 'invariant':['每一轮的 49 个有序对等概率，各以 1/49 出现；接受区域内每个 value 仍等概率。输出 k 对应 k、k+10、k+20、k+30 四个编号，所以接受条件下每个输出概率为 4/40=1/10。','拒绝后重新进行独立的相同试验，不偏向任何输出。更形式化地说，最终输出某个 k 的概率为 (4/49)×[1+(9/49)+(9/49)²+…]=1/10。随机重试只是改变何时返回，不改变各结果的对称性。'],
 'examples':[{'label':'49 个等概率编号，接受其中 40 个','input':'两次调用结果 a、b 均在 1..7','output':'每个 1..10 最终概率均为 1/10','frames':[
  {'title':'两次抽签组成 7×7 网格','note':'行代表第一次结果 a，列代表第二次结果 b；每个格子显示 (a−1)×7+b。49 个格子等概率，而不是把 a+b 当成均匀数。','grid':[[r*7+c+1 for c in range(7)] for r in range(7)],'metrics':[['每格概率','1/49']]},
  {'title':'只接受高亮的 1..40','note':'前五行共有 35 格，第六行再接受前五格，合计 40。41..49 共九格拒绝后重抽。','grid':[[r*7+c+1 for c in range(7)] for r in range(7)],'active_cells':[[r,c] for r in range(7) for c in range(7) if r*7+c+1<=40],'metrics':[['接受',40],['拒绝',9],['单轮成功率','40/49']]},
  {'title':'每个输出都有四个等概率来源','note':'对接受值使用 (value−1)%10+1。每组数量一致，这才是均匀性的依据。','table':{'headers':['输出','对应的接受编号'],'rows':[[k,f'{k}、{k+10}、{k+20}、{k+30}'] for k in range(1,11)]}},
  {'title':'拒绝并不改变输出分布','note':'某一轮是否失败对之后的新抽签没有偏向。每次成功时，十组仍然各占 4/40，因此最终每个结果仍为 1/10。','diagram':'一轮抽取\n  ├─ value=1..40：均分为十组，返回\n  └─ value=41..49：丢弃本轮，重新抽取','equation':'每个结果：4/40 = 1/10'}
 ]},{'label':'一次拒绝之后返回 10','input':'模拟 rand7 依次返回 [7,7,6,5]','output':'10，调用 rand7 共 4 次','frames':[
  {'title':'第一对 (7,7) 映射为 49','note':'(7−1)×7+7=49，超出接受区域，不能直接对 49 取模返回，需丢弃整轮。','diagram':'(7,7) → 49 → 拒绝','metrics':[['已调用 rand7',2]]},
  {'title':'第二对 (6,5) 映射为 40','note':'(6−1)×7+5=40，接受；(40−1)%10+1=10。40 是合法边界，不能误写 value<40。','diagram':'(6,5) → 40 → (40−1)%10+1 → 10','metrics':[['已调用 rand7',4],['输出',10]]}
 ]}],
 'walkthrough':['49 格可以理解成两位七进制数的编号。用 (a−1)×7+b 保证不同有序对得到不同编号，因此没有像求和那样把中间值重复映射得更多。','如果直接对 1..49 取模，输出 1..9 各有五个来源，10 只有四个来源，会产生偏差。删除最后九个编号后，各组才同样有四个来源。','样例 [7,7,6,5] 只是为了演示一次具体执行路径；真正随机运行不会保证依次抽到这些数，也没有固定的返回值序列。','这份基础拒绝采样每轮调用两次 rand7。单轮成功率 40/49，平均需要 49/40 轮，平均调用次数为 2×49/40=49/20=2.45。'],
 'code':'''
# rand7() 由力扣评测环境提供，无需自己实现。
class Solution:
    def rand10(self) -> int:
        while True:
            # 两次独立均匀调用构成 7×7 个等概率结果。
            a = rand7()
            b = rand7()
            # 把二元结果一一映射成 1..49，不能直接把两个随机数相加。
            value = (a - 1) * 7 + b
            # 只接受前 40 个结果，每个 1..10 恰好对应四种情况。
            if value <= 40:
                # 41..49 会重新采样；对全部 49 个结果直接取模会产生偏差。
                return (value - 1) % 10 + 1
'''.strip(),
 'api':{'signature':'rand7() -> int','description':['这是题目提供的外部接口，不是 Python 标准库函数。每次返回 1..7 的均匀整数；解法按连续调用独立的随机抽样模型推导。','提交时直接调用 rand7，无需导入 random，也不应实现自己的 rand7。若在普通 Python 文件里演示，需要测试环境提供这个函数；本项目验证代码通过预设调用序列检查接受、拒绝与重试行为。']} ,
 'code_notes':['a、b 是两次独立抽取，不能只调用一次然后令 b=a；那样只剩七种可能的有序对。','value≤40 包含边界 40，它映射为输出 10。先减一再取模，再加一，是从 1 基编号到 1 基输出的完整转换。','while True 只在接受时 return，拒绝时自然进入下一轮，始终使用固定数量局部变量。','rand10 不接收 n。评测可能调用它很多次，每次只产生一个结果。'],
 'pitfalls':['把两次 rand7 相加不能得到均匀的 2..14。不同和的组合数量不同。','对 49 个等概率结果直接 %10 会产生分组大小不等的偏差。','设置“最多重试三次，仍失败就返回 1”会让 1 获得额外概率，破坏均匀性。','只看某一小段随机样本是否接近平均不能证明均匀，必须依据样本点映射和分组数量推导。'],
 'complexity':'期望时间 O(1)，基础方案期望调用 rand7 为 2.45 次；最坏重试次数没有固定上界。额外空间 O(1)。',
 'alternative':'进阶可以把拒绝的 41..49 看成新的 1..9 均匀样本，再配合一次 rand7 扩成 63 个样本，接受前 60 个以减少期望调用次数。要逐阶段重新证明等概率，基础版本已满足题目的核心接口要求。',
 'quiz':{'question':'为什么不能只接受 1..39？','answer':'39 不能被 10 整除，按同样的取模映射会让一些输出有四个来源、另一些只有三个。接受集合大小需要能按目标输出数量均分；接受 1..40 恰好让每组都是四个。'},
 'tests':{'method':'rand10','cases':[{'args':[],'rand7_values':[1,1],'rand7_calls':2,'expected':1},{'args':[],'rand7_values':[2,3],'rand7_calls':2,'expected':10},{'args':[],'rand7_values':[6,5],'rand7_calls':2,'expected':10},{'args':[],'rand7_values':[7,7,6,5],'rand7_calls':4,'expected':10},{'args':[],'rand7_values':[6,6,1,2],'rand7_calls':4,'expected':2},{'args':[],'rand7_values':[7,7,7,6,1,1],'rand7_calls':6,'expected':1}]}
})
