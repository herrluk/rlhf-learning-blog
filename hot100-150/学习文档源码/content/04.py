from textwrap import dedent


def balance_example(nums, label):
    """按本题实际执行顺序记录差值、字典与区间，图中标签带中文释义。"""
    first = {0: -1}
    balance = best = ones = zeros = 0
    frames = [{
        'title': '还没读元素：差值 0 记在虚拟位置 −1',
        'note': 'first 是字典；冒号左边的键 0 表示“1 的数量减 0 的数量等于 0”，右边的值 −1 表示“尚未读取任何元素”。这不是一次数组访问。',
        'array': nums, 'active': [],
        'diagram': '虚拟位置     -1  |  原数组下标从 0 开始\n已读元素     无  |  接下来读取 nums[0]\n字典 first = {0: -1}',
        'table': {'headers': ['键：累计差值 balance', '值：最早结束下标'], 'rows': [[0,-1]]},
        'metrics': [['已读 1 的数量',0],['已读 0 的数量',0],['差值 balance',0],['最长长度 best',0]],
    }]
    for i,x in enumerate(nums):
        if x==1:
            ones+=1;balance+=1
        else:
            zeros+=1;balance-=1
        if balance in first:
            previous_index=first[balance]
            current_length=i-previous_index
            best=max(best,current_length)
            active=list(range(previous_index+1,i+1))
            title=f'下标 {i} 读到 {x}：旧字典里查到了同一个差值'
            note=(f'到这里共读到 {ones} 个 1、{zeros} 个 0，差值为 {balance}。first[{balance}]={previous_index}，'
                  f'表示这个差值最早出现在结束下标 {previous_index}。中间新增的元素是下标 [{previous_index+1},{i}]，'
                  f'长度 {i}−({previous_index})={current_length}；最长长度 best={best}。保留旧记录，不把它改成 {i}。')
            metrics=[['当前下标 i',i],['当前元素 x',x],['差值 balance',balance],['旧结束下标 previous_index',previous_index],['本轮长度 current_length',current_length],['最长长度 best',best]]
        else:
            first[balance]=i;active=[]
            title=f'下标 {i} 读到 {x}：这个差值第一次出现'
            note=(f'到这里共读到 {ones} 个 1、{zeros} 个 0，差值为 {balance}。字典此前没有键 {balance}，'
                  f'因此写入 first[{balance}]={i}：键是累计差值，值是本次结束下标。当前还不能与旧前缀配对，best 仍为 {best}。')
            metrics=[['当前下标 i',i],['当前元素 x',x],['差值 balance',balance],['最长长度 best',best]]
        frames.append({'title':title,'note':note,'array':nums,'active':active,'changed':i,'pointers':{'i':i},
                       'table':{'headers':['键：累计差值 balance','值：最早结束下标'],'rows':[[k,v] for k,v in first.items()]},
                       'metrics':metrics,'diagram':'当前字典 first = '+str(first)+'\ni 标记当前下标；橙色是当前元素，绿色是本轮合法区间中的其余元素。'})
    return {'label':label,'input':'nums='+str(nums),'output':str(best),'frames':frames}

CHAPTER={
 'lead':'先从零理解前缀和：怎样构建累计表，为什么相减能得到区间和，再用它推导计数、最长区间与前后缀分解。',
 'foundation':'prefix-sum',
 'intro':[
  '这一章先不套“前缀和 + 哈希表”模板。你只需要先会读取数组下标、做加减法、从左到右循环；前缀和本身、空前缀与区间边界都会从具体例子开始解释。',
  '先完成下面的基础导读和两组互动演示，再进入三道题：560 统计区间数量，525 求最长等量区间，238 将累计思想扩展到自身左右两侧的乘积。'
 ],
 'sections':[
  {'title':'7. 从“查询一个区间”走到第 560 题：寻找需要的旧前缀','body':['基础查询已经告诉你左右端点，而 560 不知道左端点，还要统计所有可能的区间。固定当前右端点 i 后，区间和等于 k，等价于 prefix[i+1]−prefix[left]=k，移项就是 prefix[left]=prefix[i+1]−k。','例如 nums=[2,-1,3,4]，处理到下标 2 时，当前前缀是 4。若 k=2，需要此前前缀 4−2=2；它在 prefix[1] 出现，因此找到下标 [1,2] 的 [-1,3]。','如果仍然从头查所有旧前缀，总时间还是 O(n²)。用字典统计每种旧前缀的次数，查找 prefix−k 只需平均 O(1)，同一个旧值出现几次就贡献几个不同起点。','题目只依赖当前累计值与过去的计数，不再需要保存整张 prefix 数组。所以代码里的 prefix 是一个滚动更新的整数，相当于基础表中当前那一项 prefix[i+1]；count 保存的才是已经出现过的前缀信息。'], 'diagram':'prefix[i+1] - prefix[left] = k\n                         ↓ 移项\nprefix[left] = prefix[i+1] - k\n\n固定右端点 i，只查询符合条件的旧前缀有几个。'},
  {'title':'8. 第 525 题：为什么保存最早位置，又为什么出现 −1','body':['先把 0 当作 −1、1 当作 +1。一个区间两种数量相等，等价于这段转换后的和为零，也就是两端前缀平衡值相等。','代码把累计的“1 的数量减去 0 的数量”命名为 balance，把“每种差值最早出现在什么结束下标”的记录表命名为 first。这两个英文名都是我们给变量起的名字，不是 Python 自带功能。first 是字典，每条记录按“差值:最早结束下标”保存。','first={0:-1} 表示创建含一条记录的字典：左边的键 0 是差值，右边的值 −1 是还没读任何元素的虚拟结束位置。不是出现了 −1 次，也不是读取数组最后一项。字典写法与读取过程会在第 525 题用 [0,1] 从头演示。','统计区间个数时保存频次；求最长时保存同值前缀的最早位置。最早位置离当前右端点最远，所以后续不能覆盖。','基础表用 t 表示已取元素个数；这里用 j 表示旧前缀最后一个元素的下标，用 i 表示当前读取的下标。j=t−1：空前缀 t=0，对应 j=−1。','以 i 结尾，与旧前缀结束位置 j 配对的区间是 [j+1,i]，长度为 i−(j+1)+1=i−j。若改存前缀长度 t，则同一长度应算 (i+1)−t；两种约定都正确，但初始化和公式必须配套。'], 'diagram':'字典 first 的记录格式：{差值: 最早结束下标}\n初始化 first = {0: -1}\n               ↑   ↑\n              差值  虚拟结束位置\n\n存前缀长度 t： 空前缀位置 0   长度 = (i+1) - t\n存结束下标 j： 空前缀位置 -1  长度 = i - j'},
  {'title':'9. 第 238 题：从前缀和推广到严格左右两侧的乘积','body':['“累计一段信息”不局限于求和。238 要排除自身，把答案拆为严格左侧的乘积 × 严格右侧的乘积。这是前后缀分解，不是直接把前缀和减法换成除法。','加法空前缀为 0，因为 x+0=x；乘法空区间为 1，因为 x×1=x。位于最左或最右的元素，一侧没有元素，也能用统一公式处理。','数组可能含 0，而题目禁止除法，所以不能用两个前缀积相除来恢复区间。应从左、从右各扫描一次，并在乘入当前元素之前计算它的贡献，保证结果不包含自身。'], 'diagram':'nums = [1, 2, 3, 4]，求下标 2 的答案\n严格左侧：[1,2] → 乘积 2\n严格右侧：[4]   → 乘积 4\nanswer[2] = 2 × 4 = 8（没有乘入 nums[2]=3）'}
 ],
 'apis':[{'signature':'dict.get(key, default=None) -> value','description':['get 返回键对应值；缺失时返回 default，不会把默认值写入字典。第 560 题查询 get(prefix-k,0) 表示没有出现过这样的起点就新增 0 个答案。前缀值可能是负数，字典键不受数组下标范围限制。']}],
 'problems':[]
}

CHAPTER['problems'].append({
 'id':560,'slug':'subarray-sum-equals-k',
 'summary':'给定整数数组 nums 和整数 k，统计元素和恰好为 k 的非空连续子数组个数。数组可以含负数和零，不是只找一个区间，也不是求最长长度。',
 'baseline':'枚举左端点，再从该位置向右累计总和，可以用 O(n²) 时间统计所有区间。直接套“和过大就收缩”的滑动窗口并不正确，因为移出一个负数会让和变大，加入负数又可能让和变小。',
 'insight':'以当前下标结尾的区间和等于 k，要求此前某个前缀和恰好等于 prefix-k。此前这种前缀值出现几次，就有几个不同的左端点。因此用字典保存旧前缀的频次，而不是只保存是否出现过。',
 'steps':['初始化 prefix=0、answer=0、count={0:1}。这一次 0 代表数组开始之前的空前缀。','读入 x，先更新当前累计和 prefix += x。','查询 count.get(prefix-k,0)，把它加入 answer。这里查的是本轮之前的所有前缀。','再把当前 prefix 的出现次数加一，让它成为后续右端点可使用的旧前缀。','对所有右端点做相同处理，最后返回 answer。每个非空子数组会按唯一右端点被统计一次。'],
 'invariant':'每轮查询之前，count 中只包含严格早于当前完整前缀的那些前缀，次数也精确。由 prefix_current−prefix_old=k 可一一对应到以当前右端点结尾的合法非空区间，因此新增数量既不重也不漏。先查后存避免把当前前缀自己当作旧前缀，尤其在 k=0 时不会多计空区间。',
 'examples':[{'label':'负数与重复前缀','input':'nums=[1,-1,0], k=0','output':'3：区间 [0,1]、[0,2]、[2,2]','frames':[
  {'title':'空前缀先出现一次','note':'尚未读入元素时前缀和为 0。保留这一次出现，后面才能识别从数组开头出发的合法区间。','array':[1,-1,0],'active':[],'table':{'headers':['前缀值','此前次数'],'rows':[[0,1]]},'metrics':[['answer',0]]},
  {'title':'下标 0：prefix=1','note':'需要旧前缀 1−0=1，目前不存在，新增 0。随后记录前缀 1 出现一次。','array':[1,-1,0],'active':[0],'pointers':{'right':0},'table':{'headers':['前缀值','次数'],'rows':[[0,1],[1,1]]},'metrics':[['新增',0],['累计',0]]},
  {'title':'下标 1：prefix 回到 0','note':'旧前缀 0 有一次，对应数组之前的位置，得到 [1,-1]。计数后将前缀 0 的次数增加为 2。','array':[1,-1,0],'active':[0,1],'pointers':{'right':1},'table':{'headers':['前缀值','次数'],'rows':[[0,2],[1,1]]},'metrics':[['新增',1],['累计',1]]},
  {'title':'下标 2：再次得到 prefix=0','note':'此前 0 有两次：空前缀与下标 1 之后的前缀。分别产生 [1,-1,0] 和 [0]，新增 2，累计 3。','array':[1,-1,0],'active':[0,1,2],'pointers':{'right':2},'table':{'headers':['此前前缀位置','本轮产生的子数组'],'rows':[['数组之前','[1,-1,0]'],['下标 1 之后','[0]']]},'metrics':[['新增',2],['累计',3]]}
 ]}],
 'walkthrough':['下标 1 处回到前缀 0，说明这一段元素把前缀从 0 带回了 0，区间和为 0。','下标 2 加入的值是零，当前前缀没有变化，但产生了新的右端点。此前相同前缀的两个位置各贡献一个区间，不能只把它们当成一个“存在”标志。','最终三个区间按下标不同区分，即使其中数值看起来有重复也分别计数。'],
 'code':'''
class Solution:
    def subarraySum(self, nums: list[int], k: int) -> int:
        # 空前缀和为 0，出现过一次，才能统计从下标 0 开始的子数组。
        count: dict[int, int] = {0: 1}
        # prefix 是从数组开头到当前位置的累计和。
        prefix = 0
        answer = 0
        for x in nums:
            prefix += x
            # 若当前前缀为 prefix，需要此前的前缀为 prefix-k；次数就是新增答案数。
            answer += count.get(prefix - k, 0)
            # 先查后存，避免 k=0 时把当前前缀与自己配对成空区间。
            count[prefix] = count.get(prefix, 0) + 1
        return answer
'''.strip(),
 'code_notes':['{0:1} 不是随意的初始化常数，而是在统一公式中加入空前缀。缺少它就会漏掉从下标 0 开始、和为 k 的子数组。','answer 保存子数组数量，count 保存前缀出现次数，两者都是计数但含义不同。','当前前缀必须在查询之后加入 count。交换这两行，在 k=0 时每一轮都会把一个长度为 0 的区间错误加入答案。'],
 'pitfalls':['用 set 只能知道是否出现，无法区分多个合法起点。','只保存某种前缀第一次出现的位置适合求最长，不适合统计全部区间个数。','不要在 sum>k 时直接 break：后面出现负数仍可能把总和拉回 k。'],
 'complexity':'平均时间 O(n)，每个元素进行常数次哈希操作；额外空间 O(n)，最多保存 n+1 个不同前缀值。',
 'quiz':{'question':'nums=[0,0]、k=0 时为什么答案是 3，而不是 2？','answer':'三个非空区间分别是第一个零、第二个零、两个零一起。两轮查询旧前缀 0 的次数分别为 1 和 2，因此总数为 1+2=3。'},
 'tests':{'method':'subarraySum','cases':[{'args':[[1,-1,0],0],'expected':3},{'args':[[0,0],0],'expected':3},{'args':[[1,1,1],2],'expected':2},{'args':[[1,2,3],3],'expected':2},{'args':[[-1,-1,1],-1],'expected':3}]}
})

CHAPTER['problems'].append({
 'id':525,'slug':'contiguous-array',
 'summary':'输入列表 nums 只含数字 0 和 1。寻找其中 0 的个数与 1 的个数相同的最长连续一段，返回这一段的元素个数。例如 nums=[0,1]，整个数组中两种数各有一个，答案是 2。',
 'notation':{
  'intro':['本题维护三类信息：当前累计差值 balance、每种差值的最早结束位置 first、已找到的最长长度 best。下面先约定变量的算法含义，再推导它们如何配合。'],
  'headers':['符号或变量','在算法中的含义','本题中的具体例子'],
  'rows':[
   ['nums','输入的整数列表，只含 0 和 1。','nums=[0,1]。'],
   ['n','正文中表示列表长度；只用来说明复杂度。','nums=[0,1] 时 n=2。'],
   ['i','本轮读到的原数组下标，下标从 0 开始。','读第二个元素时 i=1。'],
   ['x','本轮读到的元素值，就是 nums[i]。','nums=[0,1]、i=1 时 x=1。'],
   ['balance','一个整数：从开头到本次已读位置，1 的数量减去 0 的数量；下文简称“差值”。','读完 [0,1]：1−1=0；只读 [0]：0−1=−1。'],
   ['first','记录“累计差值 → 最早结束下标”。同一个差值只保留最早位置，以便形成最长区间。','first={0:-1}：差值 0 最早记在虚拟结束位置 −1。'],
   ['first[balance]','与当前前缀差值相同的旧前缀，其最早结束下标；两者之间就是合法区间。','若 balance=0 且 first={0:-1}，旧前缀结束位置为 −1。'],
   ['previous_index','从 first[balance] 查出的旧结束下标。','读完 [0,1] 时查到 previous_index=−1。'],
   ['current_length','本轮两个同差值前缀之间的区间长度：i−previous_index。','当前 i=1，旧位置 −1，长度 1−(−1)=2。'],
   ['best','目前找到的最长合法区间长度；没有找到时为 0。','发现 [0,1] 后 best 从 0 更新为 2。']
  ]},
 'baseline':'从每个起点向右扩张，分别数 0 和 1，数量相等时更新最长长度。这需要 O(n²) 时间，其中 n 是数组长度。想减少重复计数，就需要保存已经读过的那一段的统计信息。',
 'insight':'只保存“两种数量之差”就够了：读到 1，差值加一；读到 0，差值减一。如果更早与更晚两个位置的累计差值相同，中间新加入的 1 和 0 必然一样多。下面先说明差值和记录表，再推导长度公式。',
 'insight_sections':[
  {'title':'① balance：用累计数量差判断两个前缀之间是否等量',
   'body':['balance 表示从数组开头到当前下标为止，“1 的数量减去 0 的数量”。如果前后两个位置的累计差值相等，中间新增的 1 和 0 必然一样多；因此，只需保存这个差值，就能判断一段区间是否合法。','对 [0,1]：没读任何元素时，两种数都是 0 个，差值为 0；读第一个 0 后，差值为 0−1=−1；再读到 1 后，差值为 1−1=0。原来的 nums 不需要改写成 −1 和 +1。'],
   'table':{'headers':['处理到哪里','已读 1 的数量','已读 0 的数量','balance'], 'rows':[['还没读元素',0,0,0],['下标 0，读到 0',0,1,-1],['下标 1，读到 1',1,1,0]]}},
  {'title':'② first 保存什么：累计差值 → 最早结束下标',
   'body':['知道差值重复出现，只能判断存在合法区间；要计算区间长度，还必须知道这个差值上次出现在哪里。因此 first 的键是累计差值，值是这个差值最早出现时的结束下标。','本题求最长长度，同一个差值要保留离当前位置最远的旧位置，也就是最早位置。记录次数适合统计区间个数，无法直接算出本题的最长长度。','first={0:-1} 是一条初始状态记录：累计差值为 0 的前缀，最早结束位置是 −1。这里的 0 描述数量差，−1 描述位置；下面解释为什么空前缀的位置要这样约定。'],
   'diagram':'first 记录：累计差值 → 最早结束下标\n\nfirst = {0: -1}\n         ↓   ↓\n       差值  这个差值最早出现的位置\n\n算法含义：还没读元素时，差值已经是 0；\n          把这个空前缀的结束位置记为 -1。'},
  {'title':'③ 为什么用 −1：给“一个元素都没读”安排一个位置',
   'body':['原数组第一个元素的下标是 0。在它前面，一个元素都没读的状态没有真实下标；为了让长度公式统一，我们把这个状态的“结束下标”约定为 −1。','这时 1 和 0 都没出现过，累计差值是 0。因此初始记录为 first={0:-1}。以后遇到累计差值为 0 的位置，就能与这个空前缀配对，识别从数组开头出发的合法区间。'],
   'diagram':'                         原数组 nums=[0,1]\n结束位置      -1    |       0       1\n本次读到的值  无    |       0       1\n累计差值       0    |      -1       0\n               ↑                     ↑\n         没读元素时的差值       读完两个元素的差值\n\n两处差值都是 0，中间新增的 [0,1] 中两种数一样多。'},
  {'title':'④ 查到旧位置以后，配对的是哪两个前缀',
   'body':['沿着 [0,1] 看记录如何变化：读完第一个 0，balance=−1，这是差值 −1 首次出现，记下 first[-1]=0；表中此时有两条记录，如下表。','继续读到下标 1 的元素 1，balance 回到 0。旧记录 first[0]=−1，说明同样差值的前缀最早结束在 −1。于是当前前缀 [0,1] 与旧的空前缀配对，两者之间新增的就是整个 [0,1]。','代码用 previous_index 保存查到的旧结束位置。只有差值已经出现过，才有旧前缀可配对并计算长度；差值首次出现时，只登记位置，留给后续元素使用。'],
   'table':{'headers':['字典中的记录','键：累计差值','值：最早结束下标','整句话的意思'], 'rows':[['0: -1',0,-1,'差值 0 在还没读元素时就出现了。'],['-1: 0',-1,0,'差值 −1 最早在读完下标 0 时出现。']]}},
  {'title':'⑤ 从 [0,1] 推导长度，不直接丢一个公式',
   'body':['读到 [0,1] 的第二个元素时，当前下标 i=1，差值 balance=0。从 first[0] 查到旧结束下标 −1。旧前缀没有任何元素，所以中间新加入的就是下标 0 到 1，一共两个元素。','长度的通用算法是“右端点−左端点+1”。旧前缀已经到 previous_index 为止，因此新段从 previous_index+1 开始，右端点为 i。代入后就是 i−(previous_index+1)+1=i−previous_index。','本例为 1−(−1)=2。previous_index 是区间开始之前的位置，不是区间左端点，所以最终表达式 i−previous_index 后面不再加 1。','如果把初始记录误写为 first={0:0}，同样的代码只会得到 1−0=1，少算原数组的第一个元素。如果完全不记空前缀，则这次查不到键 0，会漏掉整个 [0,1]。'],
   'diagram':'旧前缀结束位置 previous_index   | 新区间开始 ... 新区间结束\n              -1               |      0              1\n\n左端点 = previous_index + 1 = 0\n右端点 = i = 1\n长度   = 1 - 0 + 1 = 2\n       = i - previous_index = 1 - (-1) = 2'},
  {'title':'⑥ 为什么 first 只记最早位置，而不是每次更新',
   'body':['对 [0,1,0,1]，差值 0 出现在虚拟下标 −1、真实下标 1、真实下标 3。读到下标 1 时已经得到长度 2，但 first[0] 仍保留 −1。','等读到下标 3，使用旧位置 −1 得到长度 3−(−1)=4；如果之前把 first[0] 覆盖成 1，这次只剩 3−1=2，最长答案就丢了。','我们要的是最长长度，因此同一个差值只留最早结束位置。第 560 题要的是区间个数，字典的值才表示次数。这两道题虽然都用字典，值的含义并不相同。','差值本身不必等于 0。例如 [0,0,1] 在下标 0 和 2 的累计差值都为 −1，两个前缀之间新增的 [0,1] 仍然两种数一样多，长度为 2−0=2。']}
 ],
 'steps':['初始化 first={0:-1}，登记差值为 0 的空前缀及其结束位置。balance=0 表示还没有读入元素，best=0 表示尚未找到合法区间。','从左到右遍历，i 是当前下标，x 是当前元素。读到 1 就让 balance 加 1，读到 0 就减 1。','查找当前差值是否有旧记录，判断是否存在可配对的旧前缀。','如果出现过，读取 previous_index=first[balance]，计算 current_length=i−previous_index，再更新最长长度 best。这个分支保留 first 中原有的最早位置。','如果没出现过，记录 first[balance]=i，留给后续右端点配对使用。','处理完所有元素，返回 best，即所有合法区间中的最大长度。'],
 'invariant':['每轮读取元素并更新 balance 后，它精确等于从数组开头到当前下标 i 的“1 的数量−0 的数量”；first 在检查时只包含更早的前缀，每个键对应最早的结束下标。','更早与当前的累计差值相同，说明中间新增的 1 的数量与新增的 0 的数量之差为 0，所以中间是一段合法连续区间。对固定当前下标 i，旧结束位置越早，中间区间越长；保留最早位置就不会漏掉最长候选。','best 记录所有已经找到的合法区间中的最大长度。扫描全部右端点后，每种可能的结束位置都检查过，因此返回的是全局最长长度。'],
 'examples':[balance_example([0,1],'从零看懂 {0:-1} 与长度 2'),balance_example([0,1,0,1],'保留最早位置，才能得到长度 4'),balance_example([0,0,1],'差值为 −1，也能找到等量区间')],
 'walkthrough':['先看 [0,1]：第一轮写入 first[-1]=0；第二轮查到 first[0]=−1。前者的 −1 是键，表示差值；后者的 −1 是读出的值，表示旧结束位置。','再看 [0,1,0,1]：差值 0 重复出现时，表中的 0:−1 始终保持不变，最长长度从 2 增长为 4。图里的字典表每步都区分键和值。','最后看 [0,0,1]：整个前缀的差值不是 0，但之前与现在的差值都为 −1，两者相减还是 0，所以中间新增的一段仍合法。'] ,
 'code':'''
class Solution:
    def findMaxLength(self, nums: list[int]) -> int:
        # first 是字典：键 = 累计差值，值 = 该差值最早出现时的结束下标。
        # 0: -1 表示差值 0 在“还没读任何元素”的虚拟位置 -1 就出现过。
        # 这里存入整数 -1，没有读取 nums[-1]，也不表示出现次数。
        first: dict[int, int] = {0: -1}

        # balance = 已读到的 1 的数量 - 已读到的 0 的数量。
        balance = 0
        # best 保存已经找到的最长合法区间长度。
        best = 0

        # i 是当前元素下标，x 是当前元素值（也就是 nums[i]）。
        for i, x in enumerate(nums):
            if x == 1:
                balance += 1  # 多读到一个 1，数量差加一。
            else:
                balance -= 1  # 题目只含 0/1；此处多读到一个 0。

            # in 检查字典里是否已有“当前差值”这个键。
            if balance in first:
                # 按差值查出旧前缀的最早结束下标，不是查出现次数。
                previous_index = first[balance]
                # 新区间从 previous_index+1 到 i，长度化简为 i-previous_index。
                current_length = i - previous_index
                best = max(best, current_length)
                # 已存在的键不覆盖，最早位置才能产生最长区间。
            else:
                # 这个差值第一次出现：登记“差值 -> 当前结束下标”。
                first[balance] = i

        return best
'''.strip(),
 'code_notes':['按变量表对照：输入是 nums，每轮的位置和值是 i、x，已读数量差是 balance，旧位置登记表是 first，查出的旧位置是 previous_index，本轮长度是 current_length，最终答案是 best。','两个 if 做不同工作：第一个区分当前元素是 1 还是 0，用来更新差值；第二个区分这个差值过去有没有出现，用来决定计算长度还是登记位置。','代码把“查旧位置、算长度、更新最大值”分成三行，顺序与 [0,1] 的推演一致。这样可以逐行检查 −1 从哪里读出来，以及为什么得到长度 2。','只有第一次遇到某个差值时才写入 first。重复出现时保留最早位置；原数组 nums 没有被修改。'],
 'pitfalls':['不要把 first 字典的值理解成次数。本题 first[0]=−1 是旧结束下标；第 560 题 count[0]=1 才表示出现次数。','不要把字典值 −1 与 nums[-1] 混为一谈。本代码用 −1 标记空前缀位置，没有通过它读取输入数组。','不要用 first={0:0} 配合 i−previous_index：对 [0,1] 会得到 1，而不是 2。','不要把同一个差值的最早位置覆盖成最新位置；[0,1,0,1] 会因此漏掉长度 4。','不要只在 balance==0 时更新答案；[0,0,1] 中相同的非零差值也能形成合法区间。','不能只比较整个数组里两种数量，再忽略位置排列；题目要求选出连续的一段。'],
 'complexity':'n 表示 nums 的元素个数。平均时间 O(n)：每个元素更新一次差值，并做常数次字典操作；额外空间 O(n)：最多保存 n+1 个前缀记录，重复差值只保留一个最早位置。',
 'quiz':{'question':'如果题目改为统计等量 0 和 1 的子数组个数，first 还够用吗？','answer':'不够。仍然把 0 视为 −1，但要像第 560 题一样保存每个平衡值的出现次数，初始化 {0:1}，每轮先累加次数再记录当前平衡值。'},
 'tests':{'method':'findMaxLength','preserve_args':[0],'cases':[{'args':[[0,1,0,1]],'expected':4},{'args':[[0,1,0]],'expected':2},{'args':[[1,1,1]],'expected':0},{'args':[[0,0,1,0,0,0,1,1]],'expected':6},{'args':[[0,1]],'expected':2},{'args':[[0,0,1]],'expected':2},{'args':[[0]],'expected':0},{'args':[[1,0]],'expected':2}]}
})

CHAPTER['problems'].append({
 'id':238,'slug':'product-of-array-except-self',
 'summary':'返回数组 answer，其中 answer[i] 等于 nums 中除了 nums[i] 以外所有元素的乘积。要求 O(n) 时间，不能用除法；进阶要求不计输出数组时使用 O(1) 额外空间。',
 'baseline':'对每个下标 i 再扫描全部其他元素，时间为 O(n²)。先算总乘积再除以 nums[i] 看似更快，但题目禁止除法，而且数组出现零时会遇到除零与信息丢失问题。',
 'insight':'除了自身之外的元素可以分成两段：左侧所有元素和右侧所有元素。answer[i] = 左侧乘积 × 右侧乘积。先让输出数组保存每个位置的左侧乘积，再从右往左用一个变量累计右侧乘积，就无需两个额外数组。',
 'steps':['创建长度为 n 的 answer，全部初始化为 1。乘法的空区间贡献是 1，不是 0。','从左到右扫描，用 left_product 保存严格位于当前位置左侧的乘积。先写 answer[i]=left_product，再乘入 nums[i]。','此时 answer[i] 已经保存 nums[0] 到 nums[i-1] 的乘积，尚未包含右侧部分。','从右到左扫描，用 right_product 保存严格位于当前位置右侧的乘积。先让 answer[i] 乘上 right_product，再将 nums[i] 乘入这个变量。','两个方向都完成后，每个位置正好使用左侧与右侧全部元素，并且从未乘入自身。'],
 'invariant':'左向扫描处理 i 时，left_product 只包含 i 之前的元素；右向扫描处理 i 时，right_product 只包含 i 之后的元素。两段不相交，合起来覆盖除 i 外的所有位置。先写答案后更新累计变量，保证当前位置不会混入自己的结果。',
 'examples':[{'label':'两次扫描拼出答案','input':'nums=[1,2,3,4]','output':'[24,12,8,6]','frames':[
  {'title':'输出数组先作为左侧乘积表','note':'从左到右依次写入空乘积 1、1、1×2、1×2×3。注意每项都不包含当前位置本身。','array':[1,2,3,4],'active':[],'table':{'headers':['i','严格左侧的元素','写入 answer[i]'],'rows':[[0,'空',1],[1,'[1]',1],[2,'[1,2]',2],[3,'[1,2,3]',6]]}},
  {'title':'从最右侧开始，右侧为空','note':'i=3 时 right_product=1，所以 answer[3]=6×1=6。随后把 nums[3]=4 累计进右侧变量。','array':[1,1,2,6],'active':[3],'pointers':{'i':3},'array_label':'当前输出数组 [1,1,2,6]','metrics':[['用于计算的右积',1],['更新后的右积',4]]},
  {'title':'i=2，右侧乘积为 4','note':'answer[2] 原来存左侧乘积 2，与右侧乘积 4 相乘得到 8。随后右积更新为 3×4=12。','array':[1,1,8,6],'active':[2],'pointers':{'i':2},'metrics':[['计算','2×4=8'],['新右积',12]]},
  {'title':'i=1，右侧乘积为 12','note':'左侧乘积是 1，所以 answer[1]=12。随后右积更新为 2×3×4=24。','array':[1,12,8,6],'active':[1],'pointers':{'i':1},'metrics':[['计算','1×12=12'],['新右积',24]]},
  {'title':'i=0，完成两侧组合','note':'左侧为空，贡献为 1；右积为 24，得到 answer[0]=24。全部结果为 [24,12,8,6]。','array':[24,12,8,6],'active':[0,1,2,3],'metrics':[['结果','[24,12,8,6]']]}
 ]}],
 'walkthrough':['第一遍后的 [1,1,2,6] 不是最终答案，而是每个位置严格左侧的乘积。这解释了为什么第二遍要乘，不是覆盖。','第二遍用一个变量就能代表全部严格右侧元素，因为扫描方向已经从右向左。','若 nums=[1,2,0,4]，零所在位置可以得到左积 2 × 右积 4 = 8；其余位置的某一侧一定含零，因此自然得到 [0,0,8,0]，无需零值特判。'],
 'code':'''
class Solution:
    def productExceptSelf(self, nums: list[int]) -> list[int]:
        n = len(nums)
        # 输出数组先存当前位置左边所有数的乘积。
        answer = [1] * n
        left_product = 1
        for i in range(n):
            # 先写再乘当前值，保证不把 nums[i] 算入自己的答案。
            answer[i] = left_product
            left_product *= nums[i]

        # 反向扫描时只需一个变量保存右侧乘积。
        right_product = 1
        for i in range(n - 1, -1, -1):
            # 左乘积乘右乘积，正好排除当前位置；没有除法，零也能正确处理。
            answer[i] *= right_product
            right_product *= nums[i]
        return answer
'''.strip(),
 'api':{'signature':'range(start: int, stop: int, step: int = 1) -> range','description':['range 不包含 stop。range(n-1,-1,-1) 从 n−1 逐次减一到 0；如果 stop 写成 0，就会漏掉下标 0。range 对象按需表示整数序列，不先分配长度为 n 的数字列表。']},
 'code_notes':['answer=[1]*n 中的元素是不可变整数，用重复初始化没有共享可变子对象的问题。若写二维列表，则不能照搬同样方式创建共享行。','左侧累计必须在写入 answer[i] 之后乘入 nums[i]，右侧也是同样顺序；这样才表示“严格左侧/右侧”。','输入 nums 不被修改。answer 同时承担中间表与最终输出，因而额外变量数固定。'],
 'pitfalls':['累计乘积若初始化为 0，所有结果都会错误地变为 0。','如果先乘入自身再写答案，会把需要排除的 nums[i] 也计入。','逆序循环边界写错容易漏掉下标 0；测试两元素数组和含零数组最容易发现。'],
 'complexity':'在题目整数范围的常规运算模型下，时间 O(n)，两次线性扫描；不计输出数组时额外空间 O(1)，包含输出时总辅助存储 O(n)。',
 'quiz':{'question':'如果数组中有两个零，这个算法需要额外分支吗？','answer':'不需要。无论排除哪个位置，剩余元素中至少仍有一个零，所以每个输出自然为零。前后缀相乘已经覆盖这种情况。'},
 'tests':{'method':'productExceptSelf','cases':[{'args':[[1,2,3,4]],'expected':[24,12,8,6]},{'args':[[1,2,0,4]],'expected':[0,0,8,0]},{'args':[[0,0]],'expected':[0,0]},{'args':[[-1,1,0,-3,3]],'expected':[0,0,9,0,0]},{'args':[[2,3]],'expected':[3,2]}]}
})
