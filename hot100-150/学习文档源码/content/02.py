from textwrap import dedent

CHAPTER={
 'lead':'数组和字符串只是输入形式。本章从读写位置、相向排除、原地重排到逐字符解析，训练如何为指针和中间状态赋予明确含义。',
 'intro':['先区分题目的输出要求：283、88、75、189、31 要修改原数组；26 要返回有效前缀的长度；11、125 只返回一个判断或最优值；字符串题通常构造新字符串。输出要求决定能否覆盖输入、哪些尾部内容需要保留。','学习路径分成四段：283、26、88 理解读写与覆盖安全；125、11、15 理解相向指针为什么可以移动；75、189、151、31 研究分区和反转；14、165、415、8、43、468 逐步提高字符串边界与解析能力。','不把所有题都称为双指针模板。比如字符串相乘的核心是十进制位权与进位，IP 校验的核心是逐条满足格式规则。指针只是实现工具，真正的解法来自每题要维护的性质。'],
 'sections':[
  {'title':'读写指针：只覆盖已经读过的区域','body':['read 指向下一个待检查元素，write 指向下一个有效输出位置。处理原地过滤时，通常能保持 write≤read，因此写前缀不会覆盖尚未检查的后缀。','如果目的数组的空位在末尾，正向合并可能盖住还没读的旧值。第 88 题反向填入当前最大值，把写入位置保持在未读区间之外。'],'diagram':'原地保留： [ 已确定的有效前缀 | 已读但无须保留 | 尚未读取 ]\n                            write             read\n\n逆向合并： [ 尚未读完的旧元素 | 可写空间 ... 最大值后缀 ]\n                          i           write'},
  {'title':'相向指针：移动必须有排除依据','body':['回文比较时，两端有效字符相同，就把问题缩小为内部子串；不同则已经确定失败。','盛水容器移动较短边，因为保留较短边而缩短宽度不可能得到更大面积；三数之和固定一个数后，根据剩余两数和与目标的大小排除一侧。它们都用左右指针，但正确性证明不同。']},
  {'title':'Python 的原地修改与局部变量重新绑定','body':['nums[i]=x、nums.reverse() 会修改列表对象，调用者能看到变化；nums=另一个列表只改变函数内部变量指向，不能完成原地题的接口要求。','Python str 不可变。字符串题即使从右向左扫描，也通常要分配输出字符串或片段列表。需要分别说明输出空间与额外中间存储，不能因为只有两个下标就宣称全程 O(1) 空间。']}
 ],
 'apis':[{'signature':'enumerate(iterable, start: int = 0) -> enumerate\nlist.append(item) -> None\nlist.reverse() -> None','description':['enumerate 逐个返回下标与元素，可用于同时描述读取位置和值。append 把元素加到列表末尾，reverse 原地反转列表；两种列表方法都返回 None，不能把返回值重新赋给原列表变量。','Python 切片 a[left:right] 不包含 right，通常会创建新列表或字符串。想保持常数额外空间的原地数组变换，应使用元素交换而不是复制整个切片。']}],
 'problems':[]
}

CHAPTER['problems'].append({
 'id':283,'slug':'move-zeroes',
 'summary':'把整数数组中的所有零移动到末尾，同时保持非零元素原来的相对顺序。需要直接修改输入列表，不返回一个替代的新列表。',
 'baseline':'新建列表收集所有非零元素，再补上相同数量的零，思路直接但额外空间 O(n)。也可以遇到零就删除再末尾追加，但列表中部删除会移动后续元素，反复进行可能达到 O(n²)。',
 'insight':'最终数组的前半部分就是原输入中非零元素依次出现的顺序。用 write 指向下一个非零数应写的位置，read 从左到右扫描；write 不会超过 read，所以可以安全地把非零数覆盖到已经读过的区域。剩余后缀统一填零。',
 'steps':['初始化 write=0，表示目前还没有保留任何非零元素。','read 从 0 到 n−1 扫描。读到零就跳过；读到非零数就写入 nums[write]，随后 write+=1。','扫描结束时，nums[:write] 已经是按原顺序保留的全部非零元素。','把下标 write 到 n−1 的位置全部写成 0。函数原地完成工作，按接口返回 None。'],
 'invariant':'读取下标 read 之前，nums[0:write] 恰好保存已处理前缀中的所有非零数，顺序与原输入一致，且 write≤read。写入只能发生在当前或更早的位置，不会污染尚未读取的后缀。最后的零填充只覆盖已不需要的部分。',
 'examples':[{'label':'前缀保留完成后再填零','input':'nums=[0,1,0,3,12]','output':'原数组变为 [1,3,12,0,0]','frames':[
  {'title':'读到第一个零，写指针不动','note':'read=0 的值为 0，不保留；write 仍为 0，等待第一个非零值。','array':[0,1,0,3,12],'pointers':{'read':0,'write':0}},
  {'title':'把 1 写入下标 0','note':'读取下标 1 的 1，写到 nums[0]，write=1。数组里暂时出现两个 1 没关系，只有前缀 [0,write) 已确定。','array':[1,1,0,3,12],'active':[0],'pointers':{'read':1,'write':1}},
  {'title':'跳过零后，把 3 紧接着写入','note':'下标 2 的零不写；下标 3 的 3 写到 nums[1]，write=2。非零数 1、3 的相对顺序保留。','array':[1,3,0,3,12],'active':[0,1],'pointers':{'read':3,'write':2}},
  {'title':'最后的 12 写入下标 2','note':'全部扫描完成，write=3。前缀 [1,3,12] 已正确，末尾残留的 3、12 只是尚未清理的旧值。','array':[1,3,12,3,12],'active':[0,1,2],'pointers':{'read':4,'write':3}},
  {'title':'有效前缀之后统一补零','note':'把下标 3、4 设为 0，得到要求的原地结果。','array':[1,3,12,0,0],'active':[3,4],'metrics':[['非零数量',3],['零数量',2]]}
 ]}],
 'walkthrough':['write 统计保留了多少非零数，read 统计检查到哪个原位置。遇到零时，两者之间的差距变大。','先构造有效前缀，允许数组中间暂时有重复残留值，这比要求每一步都满足最终形状更容易维护。','后缀填零必须等原数组读取结束后进行，否则可能把后面尚未读取的非零数提前清掉。'],
 'code':'''
class Solution:
    def moveZeroes(self, nums: list[int]) -> None:
        # write 是下一个非零元素应写入的位置。
        write = 0
        # read 检查原数据；write 不超过 read，因此不会覆盖未读元素。
        for read in range(len(nums)):
            if nums[read] != 0:
                # 按遇到的顺序写回非零数，保持它们的相对次序。
                nums[write] = nums[read]
                write += 1
        # 非零部分已确定，剩余位置统一补零；直接修改传入列表。
        for i in range(write, len(nums)):
            nums[i] = 0
'''.strip(),
 'code_notes':['nums[write]=nums[read] 修改实际传入的列表，不是创建新列表后重新绑定局部变量。','write≤read 保证写操作不会破坏未来读操作；当两者相等时只是给当前位置赋相同的值。','第二个 range 从 write 开始，正好覆盖有效前缀以外的所有位置。全零输入 write=0；没有零时 write=n，第二个循环为空。'],
 'pitfalls':['题目要求稳定保留非零数，不能用排序或与末尾任意交换的方式随意改变顺序。','遍历列表时 remove(0) 会改变后续下标，容易漏读，而且中部删除成本不是 O(1)。','只返回新列表但不修改 nums，不满足本题接口。'],
 'complexity':'时间 O(n)，一次读取与至多一次后缀填充；额外空间 O(1)。',
 'alternative':'也可在遇到非零时交换 nums[write] 与 nums[read]，让零同时被移向后方。其正确性同样依赖 [write,read) 中是待移走的零，且 write 只在保留非零元素时增加。',
 'quiz':{'question':'第一次写完后数组是 [1,1,0,3,12]，为何不是错误的重复元素？','answer':'此时只有 nums[:write] 即 [1] 是有效输出前缀。原下标 1 的旧值已经读取过，之后会被新值覆盖或在末尾清零，不应把中间过程的整个数组当作最终答案。'},
 'tests':{'method':'moveZeroes','cases':[{'args':[[0,1,0,3,12]],'mutated_arg':0,'expected':[1,3,12,0,0]},{'args':[[0,0,0]],'mutated_arg':0,'expected':[0,0,0]},{'args':[[1,2,3]],'mutated_arg':0,'expected':[1,2,3]},{'args':[[0]],'mutated_arg':0,'expected':[0]},{'args':[[-1,0,-2,0]],'mutated_arg':0,'expected':[-1,-2,0,0]}]}
})

CHAPTER['problems'].append({
 'id':26,'slug':'remove-duplicates-from-sorted-array',
 'summary':'给定非递减整数数组，原地删除重复项，让每个不同值只保留一次。返回不同值数量 k，评测只检查前 k 个位置；后面的旧内容可以忽略，不要求真正缩短列表。',
 'baseline':'可以把值放到集合再排序，但需要额外空间，也没有利用输入已经有序的事实。逐项删除重复元素又会移动后缀，最坏 O(n²)。有序数组中，同一个值必然连续出现，因此只需比较当前值与已经保留的最后一个值。',
 'insight':'沿用读写指针：nums[:write] 是已经去重的有效前缀。读到的新值如果不同于 nums[write−1]，就是一个新种类，应追加到前缀；相同则跳过。这里“相邻”的对象是最后保留值，不是某个已经被覆盖的任意位置。',
 'steps':['若数组为空，返回 0。否则第一个元素一定保留，初始化 write=1。','read 从 1 开始扫描。若 nums[read]==nums[write−1]，说明仍处在同一组重复值中，跳过。','若不相同，就写 nums[write]=nums[read]，随后 write+=1。','扫描结束返回 write，它既是不同值数量，也是有效前缀长度。无需清空后缀。'],
 'invariant':'处理完 read 之前的元素后，nums[:write] 恰好是这一前缀去重后的有序结果，write≤read。输入有序保证重复值只会与最后保留值相同；新值不同则必为更大的新种类。向前覆盖不会影响还没读取的后缀。',
 'examples':[{'label':'只检查有效前缀','input':'nums=[0,0,1,1,1,2,2,3]','output':'k=4；有效前缀 [0,1,2,3]','frames':[
  {'title':'第一个 0 先保留','note':'write=1，前缀 [0] 有效。第二个 0 与最后保留值相同，跳过。','array':[0,0,1,1,1,2,2,3],'active':[0],'pointers':{'write':1,'read':1}},
  {'title':'第一次读到 1，写入下一格','note':'nums[2]=1 不同于最后保留的 0，写到下标 1，write=2；后面两个 1 都跳过。','array':[0,1,1,1,1,2,2,3],'active':[0,1],'pointers':{'write':2,'read':2}},
  {'title':'第一次读到 2，继续扩展有效前缀','note':'下标 5 的 2 写到下标 2，write=3。下标 6 的 2 与保留尾值相同，不再保留。','array':[0,1,2,1,1,2,2,3],'active':[0,1,2],'pointers':{'write':3,'read':5}},
  {'title':'读到 3 后返回长度 4','note':'把 3 写到下标 3，得到有效前缀 [0,1,2,3]。后面的 [1,2,2,3] 不属于结果范围，不需要删除。','array':[0,1,2,3,1,2,2,3],'active':[0,1,2,3],'metrics':[['返回 k',4],['只检查','nums[0:4]']]}
 ]}],
 'walkthrough':['读到新种类时 write 才前进，因此 write 就是目前保留的不同元素个数，不需要另外维护一个 count。','本题与移动零共享“先构造有效前缀”的思想，但不要求对尾部填特定值，因为接口明确只检查前 k 项。','有序性非常关键；如果输入 [1,2,1] 未排序，最后那个 1 不同于当前保留尾值 2，会被误认为新种类。'],
 'code':'''
class Solution:
    def removeDuplicates(self, nums: list[int]) -> int:
        # 空数组没有有效元素，避免后面读取第一个元素。
        if not nums:
            return 0
        # 首个元素一定保留；nums[:write] 是去重后的有效前缀。
        write = 1
        for read in range(1, len(nums)):
            # 输入有序，同值连续；只需与最后保留的值比较。
            if nums[read] != nums[write - 1]:
                nums[write] = nums[read]
                write += 1
        # 题目只读前 write 项，后面残留的数据不必删除。
        return write
'''.strip(),
 'code_notes':['write 从 1 开始，因为非空数组第一个元素无需与任何前驱比较，直接保留。','nums[write−1] 是当前有效前缀的最后一个值，正是判断当前重复组的参照。','返回的是长度，不是 nums[:write] 这个切片，也不是最后一个下标 write−1。切片只适合在本地查看结果。'],
 'pitfalls':['题目不要求列表物理长度变短，不必用 del、pop 去清理后缀。','如果把返回值写成 write−1，会少算最后保留的一个元素。','忘记输入有序的条件，会把这个算法错误用到任意顺序的全局去重。'],
 'complexity':'时间 O(n)，额外空间 O(1)，保留顺序与原有序顺序一致。',
 'quiz':{'question':'若 nums=[1,1,1]，最终 write 是多少，后两格是否必须改成零？','answer':'write=1，前一格是 [1]。后两格无需修改，评测不会把它们当作结果。把“有效前缀长度”与“列表总长度”分开理解即可。'},
 'tests':{'method':'removeDuplicates','cases':[{'args':[[0,0,1,1,1,2,2,3]],'prefix':True,'expected':[0,1,2,3]},{'args':[[1,1,1]],'prefix':True,'expected':[1]},{'args':[[1,2,3]],'prefix':True,'expected':[1,2,3]},{'args':[[-2,-2,0,1,1]],'prefix':True,'expected':[-2,0,1]},{'args':[[1]],'prefix':True,'expected':[1]}]}
})

CHAPTER['problems'].append({
 'id':88,'slug':'merge-sorted-array',
 'summary':'nums1 的前 m 个元素与 nums2 的前 n 个元素分别有序，nums1 总长度为 m+n，末尾 n 个位置是可覆盖空间。把两组有效数据合并进 nums1，原地得到一个非递减数组。',
 'baseline':'新建列表做普通双路归并，再复制到 nums1，时间 O(m+n)、额外空间 O(m+n)。若直接从 nums1 头部写结果，较小的 nums2 元素可能覆盖还没读取的 nums1 数据，导致信息丢失。',
 'insight':'空位集中在 nums1 尾部，所以反过来每次写当前最大的剩余值。nums1 的有效末尾 i=m−1、nums2 的末尾 j=n−1，当前最大值必在两个末尾之一。write 从 m+n−1 向左移动，可安全利用尾部空间。',
 'steps':['初始化 i=m−1、j=n−1、write=m+n−1，三者都从各自有效区间的末尾开始。','只要 nums2 还没用完，就比较两个有效末尾。若 i≥0 且 nums1[i]>nums2[j]，把 nums1[i] 写到 write，i 减一。','否则把 nums2[j] 写到 write，j 减一。每轮写完都让 write 减一。','nums2 用完时即可停止；nums1 剩余前缀本来就在正确位置，不必再次搬运。若 nums1 先用完，则把 nums2 剩余元素继续填入。'],
 'invariant':'write 之后的后缀已经按顺序放好所有已选出的最大元素；尚未处理的 nums1 前缀为 [0,i]，nums2 前缀为 [0,j]。当 j≥0 时 write=i+j+1>i，因此写入不会覆盖 nums1 未读元素。每次取两个有序前缀末尾的较大者，就正确确定了当前最后一格。',
 'examples':[{'label':'空位必须按 m 识别，而非按数值识别','input':'nums1=[1,2,3,0,0,0], m=3, nums2=[2,5,6], n=3','output':'nums1=[1,2,2,3,5,6]','frames':[
  {'title':'有效区与预留区分开','note':'nums1 只有前三项是输入数据，后三项的零是占位。本题按 m 定义有效长度，真正的数据也可能等于零。','panels':[{'heading':'nums1','array':[1,2,3,0,0,0],'active':[0,1,2],'pointers':{'i':2,'write':5}},{'heading':'nums2','array':[2,5,6],'active':[0,1,2],'pointers':{'j':2}}]},
  {'title':'依次把最大值 6、5 放到末尾','note':'6、5 都大于 nums1 的有效末尾 3，先把它们写到下标 5、4。nums1 的未读前三项完全没有被覆盖。','panels':[{'heading':'nums1','array':[1,2,3,0,5,6],'active':[4,5],'pointers':{'i':2,'write':3}},{'heading':'nums2','array':[2,5,6],'active':[0],'pointers':{'j':0}}]},
  {'title':'3 大于剩余的 2，先搬 3','note':'把 nums1[2]=3 写到下标 3，i=1、write=2。','array':[1,2,3,3,5,6],'active':[3,4,5],'pointers':{'i':1,'write':2}},
  {'title':'两个 2 相等，取 nums2 的 2','note':'把 nums2 的最后一个 2 写到下标 2，j=-1。nums1 的 [1,2] 原本就在正确位置，停止即可。','array':[1,2,2,3,5,6],'active':[0,1,2,3,4,5],'metrics':[['结果','[1,2,2,3,5,6]']]}
 ]}],
 'walkthrough':['与正向归并不同，当前写入的是整个结果的最大剩余值，因此方向从后往前。比较逻辑与填入顺序必须配套。','当 nums2 最后一个值写完，nums1 剩下的元素都比已填后缀小，且仍在自己的前缀位置，不需要额外 while 处理。','若 m=0，i=-1，分支会始终从 nums2 取值，把全部数据填入 nums1，不会读取负下标。'],
 'code':'''
class Solution:
    def merge(self, nums1: list[int], m: int, nums2: list[int], n: int) -> None:
        # i、j 指向两个数组尚未合并部分的末尾。
        i, j = m - 1, n - 1
        # 从最大值所在的最后一格向前填，避免覆盖 nums1 的未读数据。
        write = m + n - 1
        # 只要 nums2 还有值就继续；nums1 剩余值本来就在正确位置。
        while j >= 0:
            # nums1 用完时 i=-1，必须先判断下标，再读取元素。
            if i >= 0 and nums1[i] > nums2[j]:
                nums1[write] = nums1[i]
                i -= 1
            else:
                nums1[write] = nums2[j]
                j -= 1
            # 每轮确定一个最大剩余值，下一轮填它前面的格子。
            write -= 1
'''.strip(),
 'code_notes':['i≥0 必须写在 and 的前面，确保 nums1 已无有效数据时不会用 Python 的负下标读取末尾旧值。','相等时从任意一组取一个值都能得到正确的数值排序；这份代码先取 nums2 的值。','nums2 只读，nums1 原地写入。不能把 nums1 局部绑定成 sorted(...) 后就认为调用者的数组已更新。'],
 'pitfalls':['预留空间由 m 指定，不能通过查找零来区分有效数据与空位，零也可能是有效元素。','正向写入可能覆盖 nums1 尚未读取的元素；反向写入的安全性来自 write>i。','循环只需在 j≥0 时继续；若写成两组都未耗尽才循环，还需另外处理 nums2 剩余数据，不能漏掉。'],
 'complexity':'时间 O(m+n)，每轮消耗一个输入元素；额外空间 O(1)。',
 'quiz':{'question':'为什么 nums2 用完即可结束，而 nums1 用完还不能结束？','answer':'nums1 的未用前缀本来就在输出数组前面，无需搬运；nums2 的未用前缀还在另一个数组里，必须继续复制进 nums1。因此循环条件围绕 j 是否还有数据。'},
 'tests':{'method':'merge','preserve_args':[2],'cases':[{'args':[[1,2,3,0,0,0],3,[2,5,6],3],'mutated_arg':0,'expected':[1,2,2,3,5,6]},{'args':[[0],0,[1],1],'mutated_arg':0,'expected':[1]},{'args':[[1],1,[],0],'mutated_arg':0,'expected':[1]},{'args':[[-2,0,0,0],2,[-1,0],2],'mutated_arg':0,'expected':[-2,-1,0,0]},{'args':[[2,0],1,[1],1],'mutated_arg':0,'expected':[1,2]}]}
})

CHAPTER['problems'].append({
 'id':125,'slug':'valid-palindrome',
 'summary':'忽略非字母数字字符，并忽略英文字母大小写，判断字符串是否正读倒读相同。题目输入为可打印 ASCII 字符，数字也参与比较；过滤后为空视为回文。',
 'baseline':'先过滤字母数字并转成小写，得到新字符串，再与其反转结果比较，时间 O(n)、额外空间 O(n)。如果只需要判断，可以让两个指针直接跳过无关字符，不必分配完整过滤副本。',
 'insight':'一个串是回文，当且仅当两端有效字符相同，并且去掉这对字符后的内部部分也是回文。左指针向右找有效字符，右指针向左找有效字符，逐对比较即可。无关字符只影响指针位置，不应参与相等判断。',
 'steps':['初始化 left=0、right=len(s)−1。','只要 left<right，先从左边跳过非字母数字字符，再从右边跳过非字母数字字符，两次跳过都要检查指针尚未交叉。','将当前两个字符分别转为小写后比较，不相等立即返回 False。','相等就 left+=1、right-=1，继续处理内部。相遇或交叉后所有成对字符都已匹配，返回 True。'],
 'invariant':'指针外侧的所有有效字符已经成对匹配，尚未比较的有效字符只存在于 [left,right] 内。跳过无关字符不会改变题目定义的规范化内容；匹配两端后，问题严格缩小到内部子串。任意一次不匹配足以确定整个串不是回文。',
 'examples':[{'label':'同时跳过标点并忽略大小写','input':'s="A,b!a"','output':'True','frames':[
  {'title':'先比较 A 与 a','note':'两端都是有效字符，转成小写后都是 a，匹配。随后 left=1、right=3。','array':['A',',','b','!','a'],'active':[0,4],'pointers':{'left':0,'right':4}},
  {'title':'逗号与感叹号不参与比较','note':'左边跳过逗号到下标 2，右边跳过感叹号也到下标 2。规范化后只剩中心 b 未被成对比较。','array':['A',',','b','!','a'],'active':[2],'pointers':{'left':2,'right':2}},
  {'title':'中心字符无需寻找另一个搭档','note':'奇数长度回文允许一个中心字符。外侧 a 与 a 已匹配，因此返回 True。','diagram':'规范化内容：a b a\n            ↑   ↑  两端相同\n              b    中心保留','metrics':[['结果','True']]}
 ]},{'label':'数字同样是有效字符','input':'s="0P"','output':'False','frames':[
  {'title':'0 不应被跳过','note':'0 是数字，P 是字母，均属于有效字符。不能只用 isalpha 判断。','array':['0','P'],'active':[0,1],'pointers':{'left':0,'right':1}},
  {'title':'规范化后 0 与 p 不等','note':'第一次有效字符比较就失败，返回 False。','diagram':'0 != p → False'}
 ]}],
 'walkthrough':['A,b!a 不需要真的创建 aba。指针跳过两个标点就能直接比较相同的有效字符序列。','两侧跳过循环都用 left<right，避免全是标点时越过彼此继续索引。若停在同一个标点，比较它与自身也不会误判，随后退出。','题目允许过滤后为空，例如一个空格。外层循环不执行或只执行跳过过程，最终正确返回 True。'],
 'code':'''
class Solution:
    def isPalindrome(self, s: str) -> bool:
        # 两端同时向中间检查，不创建过滤后的字符串副本。
        left, right = 0, len(s) - 1
        while left < right:
            # 跳过不参与比较的符号；边界判断防止全是符号时越界。
            while left < right and not s[left].isalnum():
                left += 1
            while left < right and not s[right].isalnum():
                right -= 1
            # 统一大小写后比较，两端不相同就不可能是回文。
            if s[left].lower() != s[right].lower():
                return False
            # 这一对已匹配，继续检查内部的一对。
            left += 1
            right -= 1
        return True
'''.strip(),
 'api':{'signature':'str.isalnum() -> bool\nstr.lower() -> str','description':['isalnum 判断字符串是否非空且所有字符都是字母或数字；本题每次在单个字符上调用，用来决定是否跳过。它不等于只检查字母的 isalpha。','lower 返回转成小写后的新字符串，不修改原串。本题只含可打印 ASCII，每次处理单个字符，成本可视为常数；若推广到 Unicode，需要按新题意重新明确字符分类与大小写折叠规则。']},
 'code_notes':['两段内层 while 只移动各自的指针，每个字符至多被跳过一次，总工作量仍为 O(n)。','大小写转换只针对当前待比较的两个字符，没有创建长度为 n 的小写副本。','left、right 相遇时可以停止，因为中心单个字符天然与自身相同。'],
 'pitfalls':['数字也参与回文，使用 isalpha 会误判 0P。','直接比较原字符串两端会把大小写和标点当成差异，不符合题目定义。','过滤后为空不是错误输入，而是应返回 True 的回文情况。'],
 'complexity':'在 ASCII 输入限制下，时间 O(n)，额外空间 O(1)。',
 'quiz':{'question':'为什么 "race a car" 不是回文？','answer':'忽略空格后是 raceacar，两端逐步比较到 e 与 a 时不同。不能因为它含有很多对称字符就忽略中间这次不匹配。'},
 'tests':{'method':'isPalindrome','cases':[{'args':['A,b!a'],'expected':True},{'args':['0P'],'expected':False},{'args':['A man, a plan, a canal: Panama'],'expected':True},{'args':['race a car'],'expected':False},{'args':[' '],'expected':True},{'args':['.,'],'expected':True}]}
})

CHAPTER['problems'].append({
 'id':11,'slug':'container-with-most-water',
 'summary':'数组下标表示竖线的横坐标，值表示高度。选择两条线与横轴组成容器，面积为两线距离乘较短线的高度，求最大面积。不能倾斜容器，也不能改变线的位置。',
 'baseline':'枚举所有左右端点对，逐个计算 min(height[left],height[right])×(right−left)，时间 O(n²)。若从最宽的两端开始，就能根据较短边证明一些候选不可能更好，减少枚举。',
 'insight':'容器由短边决定水位。假设左边不高于右边，当前以左边为界已经获得它能在剩余区间里使用的最大宽度；若只把右边向左移，宽度变小，水位又不可能高于这条左短边，面积不会更大。因此可以永久舍弃左边，尝试更高的边。右边较短时同理。',
 'steps':['left 指向最左线，right 指向最右线，best=0。','计算当前面积并更新 best。宽度使用 right−left，高度使用两端较小值。','若左边更短，left+=1；否则 right-=1。两边相等时舍弃任意一边都安全，本解法移动右边。','当两指针相遇，已无两条不同的线可选，返回 best。'],
 'invariant':'每次移动前已记录当前面积。移动较短边时，所有固定这条短边且另一边在当前区间内部的容器，宽度更小而水位不会更高，都不可能改善 best。被舍弃端点相关的未知候选由此全部可以安全排除，剩余可能改善答案的端点对仍在新区间中。',
 'examples':[{'label':'最宽不一定装水最多','input':'height=[1,8,6,2,5,4,8,3,7]','output':'49','frames':[
  {'title':'最宽容器被高度 1 限制','note':'left=0、right=8，宽度 8、水位 1，面积 8。固定左侧高度 1 再缩短宽度不会更好，移走左边。','array':[1,8,6,2,5,4,8,3,7],'active':[0,8],'pointers':{'left':0,'right':8},'equation':'min(1,7) × (8−0) = 8'},
  {'title':'宽度变小，高度大幅增加','note':'left=1、right=8，高度由 8 与 7 的较小值 7 决定，面积 7×7=49。右边较短，接下来 right=7。','array':[1,8,6,2,5,4,8,3,7],'active':[1,8],'pointers':{'left':1,'right':8},'metrics':[['水位',7],['宽度',7],['best',49]]},
  {'title':'高度 3 的右边不能改进','note':'left=1、right=7，面积 3×6=18。右边仍较短，继续左移，best 保持 49。','array':[1,8,6,2,5,4,8,3,7],'active':[1,7],'pointers':{'left':1,'right':7},'metrics':[['面积',18],['best',49]]},
  {'title':'两个高度 8，面积却只有 40','note':'宽度只有 5，面积 8×5=40。两端同高，可以舍弃任意一边；代码将 right 从 6 减到 5。','array':[1,8,6,2,5,4,8,3,7],'active':[1,6],'pointers':{'left':1,'right':6},'metrics':[['面积',40],['best',49]]},
  {'title':'后续容器均未超过 49','note':'左端仍为 1，右端依次到 5、4、3、2，面积为 16、15、4、6，最终两指针相遇，返回历史最大值。','table':{'headers':['left','right','短边高度','面积'],'rows':[[1,5,4,16],[1,4,5,15],[1,3,2,4],[1,2,6,6]]},'equation':'最大面积 = 49'}
 ]}],
 'walkthrough':['第一步牺牲了一点宽度，却让短边从 1 增加到 7，面积从 8 增加到 49，说明只追求最宽不够。','两个最高的 8 相距只有 5，面积 40，说明只选择最高两条线也不够。','算法并不保证每次移动都让面积增加，而是保证被舍弃的候选不可能超过已记录结果。这是贪心式排除的真正依据。'],
 'code':'''
class Solution:
    def maxArea(self, height: list[int]) -> int:
        # 先选最宽的区间，再逐步缩小候选范围。
        left, right = 0, len(height) - 1
        best = 0
        while left < right:
            # 水高受短板限制，面积等于短板高度乘两板间距。
            area = min(height[left], height[right]) * (right - left)
            best = max(best, area)
            # 保留短板只会让宽度变小、面积不增，因此移动短板寻找更高的边。
            if height[left] < height[right]:
                left += 1
            else:
                right -= 1
        return best
'''.strip(),
 'code_notes':['宽度是横坐标之差 right−left，不是包含多少个数组元素，所以不加 1。','先记录面积再移动端点，确保舍弃该端点之前已考虑它与最远剩余端点的组合。','不排序 height，因为下标本身就是线的位置，排序会改变问题。'],
 'pitfalls':['移动较高边不能保证获得更高水位，固定的短边仍会限制面积，同时宽度缩短。','选择最高的两条线不一定最优，距离也是面积的一部分。','本题的水量是两条边形成的矩形面积，不是第 42 题逐列计算凹槽里的雨水。'],
 'complexity':'时间 O(n)，每轮一个指针向内部移动；额外空间 O(1)。',
 'quiz':{'question':'两边同高时，为什么可以只移动一边而不同时移动两边？','answer':'固定任意一条等高边，向内缩短另一边都无法得到更大面积，因此舍弃任意一边已经安全。每轮只移动一边便于统一实现，不需要把所有相同高度的情况单独枚举。'},
 'tests':{'method':'maxArea','cases':[{'args':[[1,8,6,2,5,4,8,3,7]],'expected':49},{'args':[[1,1]],'expected':1},{'args':[[0,0,0]],'expected':0},{'args':[[4,3,2,1,4]],'expected':16},{'args':[[1,2,4,3]],'expected':4}]}
})

CHAPTER['problems'].append({
 'id':15,'slug':'3sum',
 'summary':'找出数组中所有和为 0 的三元组，三个元素来自不同下标。答案按数值去重，同一组三个值即使由不同下标组成也只输出一次。',
 'baseline':'枚举三个下标需要 O(n³)，输出还要去重。固定第一个值后，剩下是两数求和问题；如果数组有序，左右两端的和能决定移动方向，将每个固定值的搜索降为 O(n)。',
 'insight':'先排序，固定 nums[i]，在右侧用 left=i+1、right=n−1 寻找和为 −nums[i] 的一对。总和太小就增加 left，总和太大就减小 right。重复值在排序后相邻，跳过相同的固定值，并在命中后跳过两端重复值，就能从生成过程避免重复答案。',
 'steps':['对 nums 排序。i 从 0 到 n−3 枚举第一个元素；若它与前一个固定值相同则跳过。','若 nums[i]>0，后面都不小于它，三数不可能和为 0，提前结束。','初始化 left=i+1、right=n−1，计算三数总和。太小则 left+=1，太大则 right-=1。','总和为 0 时记录三个数值，然后 left+=1、right-=1。','命中后，在两指针未交叉时分别跳过与刚使用值相同的元素，继续寻找当前固定值的其他组合。'],
 'invariant':'固定 i 后，当总和过小时，保持 left 而减小 right 只会让和更小，所以可以舍弃当前 left；总和过大时对称地舍弃 right。命中后，这对值已经输出，再与相同端点值组合只会生成同一数值三元组，因此去重不会丢失新答案。固定值也只处理一次，每组排序后的三元值恰好输出一次。',
 'examples':[{'label':'两个不同答案，重复的 -1 只固定一次','input':'nums=[-1,0,1,2,-1,-4]','output':'[[-1,-1,2],[-1,0,1]]','frames':[
  {'title':'排序后再分配三个角色','note':'得到 [-4,-1,-1,0,1,2]。固定 -4 时，即使取后面最大的 1、2，总和仍小于 0，所以这一轮不会找到答案。','array':[-4,-1,-1,0,1,2],'active':[0],'pointers':{'i':0},'metrics':[['所需两数和',4]]},
  {'title':'固定第一个 -1，找到 [-1,-1,2]','note':'i=1、left=2、right=5，三数和为 0。记录数值后，两端各前进一步。','array':[-4,-1,-1,0,1,2],'active':[1,2,5],'pointers':{'i':1,'left':2,'right':5},'equation':'−1 + (−1) + 2 = 0'},
  {'title':'同一个固定值，再找到 [-1,0,1]','note':'left=3、right=4，三数和再次为 0。这是不同的数值组合，应保留。','array':[-4,-1,-1,0,1,2],'active':[1,3,4],'pointers':{'i':1,'left':3,'right':4},'equation':'−1 + 0 + 1 = 0'},
  {'title':'第二个 -1 作为固定值时跳过','note':'i=2 与 i=1 的值相同，若再搜索，会重复生成已经找到的 [-1,0,1]。直接跳过。','array':[-4,-1,-1,0,1,2],'active':[2],'pointers':{'i':2},'metrics':[['动作','跳过相同固定值']]},
  {'title':'固定 0 无新答案，结束','note':'剩余的 1、2 之和大于 0，右指针移动后与左指针相遇。最终保留两个三元组。','table':{'headers':['答案编号','三个值'],'rows':[[1,'[-1,-1,2]'],[2,'[-1,0,1]']]}}
 ]},{'label':'多个相同下标组合，只输出一个数值组合','input':'nums=[0,0,0,0]','output':'[[0,0,0]]','frames':[
  {'title':'第一次命中三个零','note':'固定下标 0，取下标 1、3，得到 [0,0,0]。三个下标互不相同。','array':[0,0,0,0],'active':[0,1,3],'pointers':{'i':0,'left':1,'right':3}},
  {'title':'后续相同固定值直接跳过','note':'虽然还可以选择其他三个下标，但题目按数值三元组去重，最终只输出一次 [0,0,0]。','diagram':'不同下标组合 ≠ 不同数值答案\n只保留 [[0,0,0]]'}
 ]}],
 'walkthrough':['排序既提供两数和的单调性，也让重复值相邻，是这两个关键步骤共同的基础。','固定第一个 -1 时，第二个 -1 仍可以被 left 选中。去重并不是把输入里相同值全部删除，否则会丢掉 [-1,-1,2]。','跳过固定值重复与命中后跳过两端重复解决不同层次的重复，缺少其中一层都可能输出重复三元组。'],
 'code':'''
class Solution:
    def threeSum(self, nums: list[int]) -> list[list[int]]:
        # 排序后才能根据和的大小移动双指针，并跳过重复组合。
        nums.sort()
        answer = []
        n = len(nums)
        for i in range(n - 2):
            # 相同的第一个数只处理一次，避免输出重复三元组。
            if i > 0 and nums[i] == nums[i - 1]:
                continue
            # 最小的候选数已为正，后面的三个数之和不可能为零。
            if nums[i] > 0:
                break
            # 固定 nums[i]，在它右边找两数之和等于 -nums[i]。
            left, right = i + 1, n - 1
            while left < right:
                total = nums[i] + nums[left] + nums[right]
                # 和偏小就增大左端；和偏大就减小右端。
                if total < 0:
                    left += 1
                elif total > 0:
                    right -= 1
                else:
                    # 保存找到的三个值，然后两端都移动。
                    answer.append([nums[i], nums[left], nums[right]])
                    left += 1
                    right -= 1
                    # 跳过与刚才相同的左右端值，避免同一组答案被重复加入。
                    while left < right and nums[left] == nums[left - 1]:
                        left += 1
                    while left < right and nums[right] == nums[right + 1]:
                        right -= 1
        return answer
'''.strip(),
 'api':{'signature':'list.sort(key=None, reverse: bool = False) -> None','description':['sort 默认按升序原地排序列表，返回 None。这里没有指定 key，直接按整数大小比较；不能写 nums=nums.sort()。','原地排序指结果写回同一个列表，并不承诺排序内部只用 O(1) 临时存储。Python 列表排序可能使用线性额外空间，本题复杂度需要把它算进去。']},
 'code_notes':['left 从 i+1 开始，且 left<right，天然保证三个下标互不相同。','固定值去重比较 nums[i−1]，表示同一值此前已经作为第一个数处理过；不是禁止其他位置再使用相同数值。','命中后先各移动一步，再与刚使用的 nums[left−1]、nums[right+1] 比较，连续跳过同值。','代码直接对输入排序；若需要保留原顺序，可先 nums=sorted(nums)，但应说明会创建一个副本。'],
 'pitfalls':['先把 nums 转成集合会丢失重复次数，[-1,-1,2] 或 [0,0,0] 可能被错误消除。','只用 set 去重输出却保留三重枚举，并没有解决主要的 O(n³) 时间问题。','只要排序后固定值大于零才可提前结束，等于零时仍可能有 [0,0,0]。'],
 'complexity':'时间 O(n²)，包含 O(n log n) 排序与每个固定值的一次线性指针扫描。双指针部分除输出外 O(1)；考虑 Python 排序临时空间，整体额外空间按 O(n) 计，输出另计。',
 'quiz':{'question':'为什么可以跳过第二个 -1 作为固定值，却不能提前把两个 -1 合成一个？','answer':'第一个 -1 作为固定值时，可以与第二个 -1 共同组成 [-1,-1,2]，需要保留两个下标。跳过的是同一个固定值的重复搜索，不是删除输入中组成答案所需的重复元素。'},
 'tests':{'method':'threeSum','compare':'groups','cases':[{'args':[[-1,0,1,2,-1,-4]],'expected':[[-1,-1,2],[-1,0,1]]},{'args':[[0,0,0,0]],'expected':[[0,0,0]]},{'args':[[0,1,1]],'expected':[]},{'args':[[-2,0,0,2,2]],'expected':[[-2,0,2]]},{'args':[[-4,-2,-2,0,2,2,4]],'expected':[[-4,0,4],[-4,2,2],[-2,-2,4],[-2,0,2]]}]}
})

CHAPTER['problems'].append({
 'id':75,'slug':'sort-colors',
 'summary':'数组只含 0、1、2，分别代表三种颜色。原地把它们排成所有 0 在前、1 居中、2 在后，不调用库排序；本节实现一次扫描的常数空间方案。',
 'baseline':'先数三种颜色的数量，再按数量覆盖数组，可以 O(n) 时间、O(1) 空间完成，是正确基线。若希望只扫描一遍，可以直接维护已经分类的两端，把未分类元素逐步交换到对应区域。',
 'insight':'使用三个指针把数组分成四段：[0,left) 都是 0，[left,i) 都是 1，[i,right] 尚未分类，(right,n) 都是 2。每轮只处理 nums[i]，按颜色把它放到左端、留在中间或交换到右端。',
 'steps':['初始化 left=0、i=0、right=n−1，整个数组都是未知区。','遇到 0，与 nums[left] 交换，left 与 i 都加一：0 区扩张，交换回来的是已知的 1，或当前位置本身。','遇到 1，直接 i+=1，1 区扩张。','遇到 2，与 nums[right] 交换，right 减一，但 i 不动，因为右侧换回的值尚未分类。','当 i>right，未知区为空，三种颜色按顺序排列完成。'],
 'invariant':'四段区间含义在每次操作后保持成立。左交换把 0 放入零区，并把已知的一移动到扫描位置；右交换把 2 放入二区，换回的未知值仍留在未知区等待下一轮。每轮至少让未知区减少一格，因此总工作量线性。',
 'examples':[{'label':'换回未知元素后不能立刻前进','input':'nums=[2,0,2,1,1,0]','output':'[0,0,1,1,2,2]','frames':[
  {'title':'四段中只有未知区非空','note':'left=i=0、right=5。当前 nums[i]=2，要把它放到右侧。','array':[2,0,2,1,1,0],'active':[0,1,2,3,4,5],'pointers':{'left':0,'i':0,'right':5}},
  {'title':'2 与末尾 0 交换，i 保持 0','note':'得到 [0,0,2,1,1,2]，right=4。换回的 0 尚未处理，不能跳到下标 1。','array':[0,0,2,1,1,2],'active':[5],'pointers':{'left':0,'i':0,'right':4}},
  {'title':'两个 0 依次并入左区','note':'先处理下标 0，再处理下标 1；两次都是原位交换，left=i=2，前两格已确认为 0。','array':[0,0,2,1,1,2],'active':[0,1,5],'pointers':{'left':2,'i':2,'right':4}},
  {'title':'再把下标 2 的 2 送到右区','note':'与 right=4 的 1 交换，得到 [0,0,1,1,2,2]，right=3，i 仍是 2。','array':[0,0,1,1,2,2],'active':[0,1,4,5],'pointers':{'left':2,'i':2,'right':3}},
  {'title':'两个 1 只需推进 i','note':'i 从 2 走到 4，超过 right=3，未知区为空。零区 [0,2)、一区 [2,4)、二区 [4,6) 完整覆盖数组。','array':[0,0,1,1,2,2],'active':[0,1,2,3,4,5],'diagram':'0 区：[0,2)    1 区：[2,4)    2 区：[4,6)\n未知区：[4,3] 为空'}
 ]}],
 'walkthrough':['关键不是三个指针的数量，而是四段的含义。每次移动都必须让这些区间继续成立。','遇到 2 时换回的 0 来自未知区域，如果立刻 i+=1，就会把这个 0 错误留在中间。','遇到 0 时左侧交换位置已经属于 1 区；若 left=i，则是自交换。因此两种交换后能否前进 i 的答案不同。'],
 'code':'''
class Solution:
    def sortColors(self, nums: list[int]) -> None:
        # [0,left) 全是 0，[left,i) 全是 1，[i,right] 还未检查。
        left = i = 0
        right = len(nums) - 1
        while i <= right:
            # 把 0 送往左边；换回来的值已检查过，因此 i 可以前进。
            if nums[i] == 0:
                nums[left], nums[i] = nums[i], nums[left]
                left += 1
                i += 1
            # 1 已处在中间区域，只需扩大已检查范围。
            elif nums[i] == 1:
                i += 1
            else:
                # 把 2 送往右边；换回来的值未知，这轮不能增加 i。
                nums[i], nums[right] = nums[right], nums[i]
                right -= 1
'''.strip(),
 'code_notes':['Python 多目标赋值先取得右侧旧值，再写入左侧位置，适合安全交换两个列表元素。','while 使用 i≤right，因为未知区是闭区间；只剩一个未知格也必须处理。','else 表示值为 2，依赖题目只允许三种值，不是通用整数排序算法。','所有分支都会缩小未知区：0、1 让 i 增大，2 让 right 减小，所以即使 i 暂时不动也不会无限循环。'],
 'pitfalls':['与右端交换后同时 i+=1，会跳过刚换回来的未知颜色。','将循环条件写成 i<right 会漏掉未知区最后一个元素。','只说“一次遍历”不能替代状态说明，必须解释左交换与右交换的不同处理。'],
 'complexity':'时间 O(n)，未知区每轮至少减少一格；额外空间 O(1)，原地修改。',
 'quiz':{'question':'为什么与 left 交换后可以推进 i，而与 right 交换后不行？','answer':'left 所在位置属于已知的 1 区，或与 i 相同，换回值的分类已经明确；right 所在位置仍属于未知区，换回任何 0、1、2 都可能，必须重新检查。'},
 'tests':{'method':'sortColors','cases':[{'args':[[2,0,2,1,1,0]],'mutated_arg':0,'expected':[0,0,1,1,2,2]},{'args':[[2,0,1]],'mutated_arg':0,'expected':[0,1,2]},{'args':[[0]],'mutated_arg':0,'expected':[0]},{'args':[[2,2,2]],'mutated_arg':0,'expected':[2,2,2]},{'args':[[1,0]],'mutated_arg':0,'expected':[0,1]}]}
})

CHAPTER['problems'].append({
 'id':189,'slug':'rotate-array',
 'summary':'将非空数组向右轮转 k 步，k 为非负整数。越过末尾的元素回到开头，要求原地修改；本节使用常数额外空间。',
 'baseline':'每次拿出末尾元素插到开头，重复 k 次，会反复移动其他元素，时间 O(nk)。切片拼接 nums[-k:]+nums[:-k] 能在线性时间构造正确结果，但会额外分配数组。要原地处理，可以把整体位置变化分解为三次反转。',
 'insight':'先让 k%=n，轮转一整圈不改变数组。把数组切成 A=前 n−k 项与 B=后 k 项，目标是 B+A。整体反转得到 reverse(B)+reverse(A)，再分别反转这两个块内部，就恢复为 B+A。',
 'steps':['将 k 对数组长度取模；若 k=0，数组已处于目标位置，直接结束。','定义闭区间反转函数 reverse(left,right)，用相向交换直到两端相遇。','反转整个数组 [0,n−1]，让 B 块来到前面、A 块来到后面，但两块内部方向均颠倒。','反转前 k 项 [0,k−1]，恢复 B 内部顺序。','反转后 n−k 项 [k,n−1]，恢复 A 内部顺序，完成右轮转。'],
 'invariant':'每次子区间反转都只改变该块内部顺序，不影响块外元素。恒等式 reverse(A+B)=reverse(B)+reverse(A) 说明第一步交换了两块的整体位置；随后的两次反转各自抵消块内颠倒，因此结果恰好为 B+A。',
 'examples':[{'label':'三次反转实现右移 3 步','input':'nums=[1,2,3,4,5,6,7], k=3','output':'[5,6,7,1,2,3,4]','frames':[
  {'title':'目标是后面三项移到前面','note':'A=[1,2,3,4]，B=[5,6,7]，希望得到 B+A。块内顺序应保持不变。','array':[1,2,3,4,5,6,7],'active':[4,5,6],'diagram':'A=[1,2,3,4] | B=[5,6,7]'},
  {'title':'先反转整个数组','note':'得到 [7,6,5,4,3,2,1]，两块已交换位置，但各自顺序颠倒。','array':[7,6,5,4,3,2,1],'active':[0,1,2,3,4,5,6],'diagram':'reverse(B) | reverse(A)'},
  {'title':'恢复前 k 项的内部顺序','note':'反转下标 0..2，前块 [7,6,5] 变为 [5,6,7]。','array':[5,6,7,4,3,2,1],'active':[0,1,2]},
  {'title':'恢复后 n−k 项的内部顺序','note':'反转下标 3..6，[4,3,2,1] 变为 [1,2,3,4]，完成目标 B+A。','array':[5,6,7,1,2,3,4],'active':[3,4,5,6],'equation':'右轮转 3 步 → [5,6,7,1,2,3,4]'}
 ]}],
 'walkthrough':['右轮转 3 步不是把每个元素移动三次，而是把最后三个元素作为一个块搬到前面。','整体反转先解决块的位置问题，两次局部反转再恢复块内顺序。这种先换块、再恢复内部的想法也可用于字符串单词重排。','k=10、n=7 时与右轮转 3 步相同，取模同时简化边界与避免无效工作。'],
 'code':'''
class Solution:
    def rotate(self, nums: list[int], k: int) -> None:
        n = len(nums)
        # 移动 n 次等于没动；原题数组非空，可先取余。
        k %= n
        if k == 0:
            return
        # 原地反转闭区间 [left,right]，只使用两个下标。
        def reverse(left: int, right: int) -> None:
            while left < right:
                nums[left], nums[right] = nums[right], nums[left]
                left += 1
                right -= 1
        # 先整体反转，把原末尾 k 个元素移到前面，但两段内部也被反转了。
        reverse(0, n - 1)
        # 分别反转前后两段，恢复各段内部的相对顺序。
        reverse(0, k - 1)
        reverse(k, n - 1)
'''.strip(),
 'code_notes':['局部 reverse 函数捕获外层 nums，直接修改同一个列表，返回 None。','局部范围是闭区间，前 k 项的最后下标为 k−1；后半块从 k 开始，两块不重叠也不漏格。','没有使用切片复制，也没有为每个元素保存原下标，额外状态只有几个整数。'],
 'pitfalls':['忘记 k%=n，k 大于长度时容易访问越界，或错误切分块。','右轮转取最后 k 项到前面，方向不能与左轮转混淆。','nums=nums[-k:]+nums[:-k] 只是局部重新绑定；即使用 nums[:]=... 修改原列表，也会分配线性临时空间。'],
 'complexity':'时间 O(n)，三次反转的总交换次数仍是线性；额外空间 O(1)。',
 'quiz':{'question':'n=1 或 k 是 n 的倍数时为什么直接返回？','answer':'取模后 k=0，表示完整轮转若干圈后每个元素回到原位置。直接结束既正确，也避免对空块做不必要的边界处理。'},
 'tests':{'method':'rotate','cases':[{'args':[[1,2,3,4,5,6,7],3],'mutated_arg':0,'expected':[5,6,7,1,2,3,4]},{'args':[[-1,-100,3,99],2],'mutated_arg':0,'expected':[3,99,-1,-100]},{'args':[[1,2,3],4],'mutated_arg':0,'expected':[3,1,2]},{'args':[[1],100],'mutated_arg':0,'expected':[1]},{'args':[[1,2,3],0],'mutated_arg':0,'expected':[1,2,3]}]}
})

CHAPTER['problems'].append({
 'id':151,'slug':'reverse-words-in-a-string',
 'summary':'反转字符串中单词的顺序，单词内部字符顺序保持不变。输入可能有首尾空格或连续多个空格，输出必须用恰好一个空格连接单词，且没有首尾多余空格。',
 'baseline':'Python 中先 split() 分词，再反转单词列表并 join，是简洁正确的 O(n) 解法。为了看清如何确定单词边界，本节给出从右向左扫描的实现；它直接按最终顺序收集单词，不需要再反转列表。',
 'insight':'输出的第一个单词就是输入中最后一个单词。先跳过末尾空格，再向左找到这个单词的起点，把完整片段加入列表；重复操作就按逆序得到了所有单词。最后统一用一个空格连接，空格格式不必在扫描中逐个搬运。',
 'steps':['用 i 指向最后一个字符，words 为空列表。','向左跳过空格；若已经越过开头，扫描结束。否则当前 i 是一个单词的最后下标 end。','继续向左移动 i，直到遇到空格或越过开头。单词范围便是 s[i+1:end+1]。','把这个完整单词追加到 words，继续从当前空格位置寻找下一个单词。','返回 " ".join(words)，由连接器负责在相邻单词之间插入一个空格。'],
 'invariant':'每轮开始时，i 右侧的所有完整单词都已按从右到左的顺序收集，i 左侧及自身尚未处理。跳过空格只丢弃分隔信息，不丢字符；扫描一个非空片段会收集一个且仅一个单词，且切片保留其原内部顺序。最终列表就是目标单词顺序。',
 'examples':[{'label':'首尾空格与三个连续空格','input':'s="  hello   world  "','output':'"world hello"','frames':[
  {'title':'跳过末尾空格','note':'字符串长度 17，i 从 16 跳过下标 16、15 的空格，来到 world 的最后一个字符下标 14。','diagram':'下标 0..1：两个空格\n下标 2..6：hello\n下标 7..9：三个空格\n下标 10..14：world\n下标 15..16：两个空格','metrics':[['end',14]]},
  {'title':'从右往左定位 world 的起点','note':'继续向左扫描到下标 9 的空格，单词范围为 [10,15)，切片得到 world 并追加。','diagram':'s[10:15] = "world"\nwords = ["world"]','metrics':[['i',9]]},
  {'title':'跳过中间空格，收集 hello','note':'跳过下标 9、8、7 后 end=6，再向左扫描到下标 1 的空格，s[2:7] 得到 hello。','diagram':'s[2:7] = "hello"\nwords = ["world", "hello"]'},
  {'title':'连接器统一处理输出空格','note':'首部空格跳完后 i<0。用一个空格连接两个单词，得到 world hello，既没有首尾空格，也没有连续空格。','array':['world','hello'],'equation':'" ".join(["world","hello"]) = "world hello"'}
 ]}],
 'walkthrough':['逆序的是单词块，不是字符。若直接反转整个字符串，会得到 dlrow、olleh 等颠倒的单词。','向左定位边界，但切片仍按从小下标到大下标读取，因此每个单词内部顺序保持正常。','把单词收集到列表后一次 join，避免在循环中反复把新单词拼到长字符串前面所产生的重复复制。'],
 'code':'''
class Solution:
    def reverseWords(self, s: str) -> str:
        # 按从右到左的次序收集单词，单词内部仍保持正向。
        words = []
        i = len(s) - 1
        while i >= 0:
            # 跳过单词间或末尾的多余空格。
            while i >= 0 and s[i] == ' ':
                i -= 1
            if i < 0:
                break
            # 记住当前单词的右端，再向左寻找它的开始位置。
            end = i
            while i >= 0 and s[i] != ' ':
                i -= 1
            # 切片左闭右开：i 已停在空格或 -1，故单词从 i+1 开始。
            words.append(s[i + 1:end + 1])
        # 用一个空格连接单词，自动消除首尾和重复空格。
        return ' '.join(words)
'''.strip(),
 'api':{'signature':'str.split(sep=None, maxsplit: int = -1) -> list[str]\nstr.join(iterable_of_strings) -> str','description':['不传 sep 时，split 按连续空白分词，忽略首尾空白并合并连续分隔；split(" ") 明确以单个空格分割，会在连续空格处产生空字符串，行为不同。题目仅用普通空格分隔，默认 split 可直接得到所需单词。','join 使用调用它的字符串作为连接符，在相邻片段之间插入连接符并返回新字符串。" ".join(words) 不会自动在首尾添加空格。本节手动扫描主要用于解释边界，简洁版本可写 words=s.split(); words.reverse(); return " ".join(words)。']},
 'code_notes':['end 是单词最后一个字符下标，所以切片终点要用 end+1；i 停在空格或 −1，因此起点为 i+1。','每个内层 while 都先检查 i≥0，避免 Python 负下标在越过开头后又去读取末尾。','words 保存切出的单词字符串，最后 join 还会创建输出；因此这份 Python 实现使用线性存储，不能称为常数空间原地反转。'],
 'pitfalls':['直接 s[::-1] 反转的是所有字符，不满足单词内部顺序保持不变的要求。','使用 split(" ") 后不清理空字符串，会保留不需要的额外空格。','Python str 不可变。若转为字符列表再做整体与逐词反转，创建字符列表本身仍需 O(n) 空间；原地进阶要求适用于可变字符串表示。'],
 'complexity':'时间 O(n)，每个字符扫描常数次，所有切片总长度与最后连接长度均为 O(n)；空间 O(n)，保存单词片段与输出。',
 'quiz':{'question':'扫描方向从右往左，为什么单词 world 没有变成 dlrow？','answer':'从右往左只是用于寻找单词的起止下标，真正提取时使用 s[start:end] 的正向切片。逆序的是提取各个单词的先后顺序，单词内部字符依然正序。'},
 'tests':{'method':'reverseWords','cases':[{'args':['  hello   world  '],'expected':'world hello'},{'args':['the sky is blue'],'expected':'blue is sky the'},{'args':['a good   example'],'expected':'example good a'},{'args':['word'],'expected':'word'},{'args':['  a  '],'expected':'a'}]}
})

CHAPTER['problems'].append({
 'id':31,'slug':'next-permutation',
 'summary':'将数组原地变为使用相同元素的、字典序紧接在当前排列之后的排列。如果已经是最大排列，则改为最小排列。可以有重复值，要求常数额外空间。',
 'baseline':['先弄清“下一排列”的含义：把使用相同元素的不同排列按字典序排好，取当前排列后面紧挨着的那一个。字典序从左向右逐个比较元素，第一个不同的位置就决定大小。例如 [1,4,2,3,5] 大于 [1,3,5,4,2]，因为第二个数 4 大于 3；之后的 2 比 5 小，也不会改变这个比较结果。','最直接的办法是生成所有排列、排序，再找到当前排列的下一项。但 n 个互不相同的元素有 n! 种排列，枚举成本增长得太快。我们需要直接构造“大于当前排列的所有排列中，最小的那个”。'],
 'insight':'下面始终使用 nums=[1,3,5,4,2]。先不记公式，而是试着保留左边的数，只重排右边；只有右边已经无法变大时，才把改动范围向左扩大。数组开头的一段叫前缀，末尾的一段叫后缀。',
 'insight_sections':[
  {'title':'1. 先只动最右边：还能排出更大的结果吗？','body':['只看最后一个数 [2]，只有一种摆法。再看最后两个数 [4,2]，把它们交换成 [2,4] 只会变小，因此保留前面的 [1,3,5] 时，已经没有更大的排列。','再向左多看一位，末尾三个数是 [5,4,2]。要让这三个数排得最大，第一位应该放最大的 5，第二位再放剩下最大的 4，最后放 2。当前恰好就是这个顺序，所以固定 [1,3] 后，末尾无论怎样重排，都不可能让整个数组变大。','于是必须动到第二个数 3。它右边有 4 和 5，可以把它换大；第一位的 1 就暂时不用动。保留第一位 1 得到的更大排列，会小于那些把第一位直接换成 2、3、4 或 5 的排列，因为字典序在第一位就已经分出了大小。'],'diagram':'原数组： [1, 3, 5, 4, 2]\n\n保留 [1, 3, 5, 4] | 只动 [2]       → 只有一种摆法\n保留 [1, 3, 5]    | 只动 [4, 2]    → 已经最大\n保留 [1, 3]       | 只动 [5, 4, 2] → 仍然已经最大\n保留 [1]          | 改动 [3, 5, 4, 2] → 3 可以换大'},
  {'title':'2. 必须增大 3：为什么选 4，而不是 5？','body':['现在第一位仍然是 1，我们要让第二位比 3 大。右边可用的数是 5、4、2：2 不够大，4 和 5 都可以。但 [1,4,…] 一定排在 [1,5,…] 前面，无论后面怎样摆。因此应该选 4；选 5 会跳过所有以 [1,4] 开头的排列。','怎样找到 4？右侧 [5,4,2] 从左向右不会变大，这叫“非递增”，也允许相等值。从右往左看就相当于从小到大挑：先看到 2，不大于 3；接着看到 4，已经大于 3，立即停止。它就是能让 3 变大的最小可用值。','现在再对应代码中的下标：从右往左检查相邻元素，4≥2、5≥4 都要继续，遇到 3<5 时停下，得到 i=1。这就是“找到最靠右的 nums[i]<nums[i+1]”。nums[i] 是这次要增大的数，通常叫枢轴；它右边的整段已经非递增。'],'diagram':'待增大的数：3       右侧可用：[5, 4, 2]\n\n选 2：变小，不符合要求\n选 4：[1, 4, …]  ← 选这个\n选 5：[1, 5, …]  ← 跳过了更接近的结果\n\n从右向左挑选：2（跳过）→ 4（找到，停止）'},
  {'title':'3. 交换完还不够：后面的数怎样摆才紧挨着原排列？','body':['交换 3 和 4，得到 [1,4,5,3,2]。第二位已经从 3 变成 4，所以整个数组肯定比原来大。但它还不是答案：保持前面 [1,4] 不变时，末尾 [5,3,2] 还能排得更小。','剩下的数是 5、3、2。要使整个结果尽量小，就先放最小的 2，再放 3，最后放 5，得到 [1,4,2,3,5]。这解释了为什么交换后还要把后缀变成升序；后面的数可以变小，因为第二位的 4 已经保证了整体比原排列大。'],'diagram':'原排列：     [1, 3, 5, 4, 2]\n                 ↓ 3 换成 4\n只交换：     [1, 4 | 5, 3, 2]  ← 已经更大，还能更接近\n后缀排最小： [1, 4 | 2, 3, 5]  ← 下一排列\n\n[1, 3, 5, 4, 2] < [1, 4, 2, 3, 5] < [1, 4, 5, 3, 2]'},
  {'title':'4. 要把后缀排成升序，为什么反转就够了？','body':['例子中，交换前的后缀是 [5,4,2]，把 4 换成原来的 3 后，变为 [5,3,2]，仍然从大到小排列。因此只需反转，就会得到从小到大的 [2,3,5]。关键是：为什么换完以后仍然有序？','把原来的枢轴记为 x，选中的替代值记为 y，这里 x=3、y=4。由于后缀原本非递增，y 左边的数都不小于 y，因此也大于 x；由于 y 是从右往左找到的第一个大于 x 的数，它右边的数都已经检查过，都不大于 x。','所以把 y 换成 x 后，左边仍然不小于 x，右边仍然不大于 x，其他位置的顺序也没有变。整个后缀仍然非递增，反转就能使它升序。这里选择“从右往左遇到的第一个严格更大值”，正是反转成立的保证。'],'diagram':'例子：后缀 [5, 4, 2] → 用 3 替换 4 → [5, 3, 2]\n                  5 > 3 ≥ 2，顺序没有被破坏\n\n一般情况（y > x）：\n交换前：[左边都 ≥ y | y | 右边都 ≤ x]\n交换后：[左边都 > x | x | 右边都 ≤ x]\n\n后缀仍然非递增 → 反转 → 升序，即最小的排列'},
  {'title':'5. 如果从右查到左，都找不到能增大的位置呢？','body':['例如 [3,2,1]：整个数组都从大到小排好了。即使允许重排所有元素，也不可能得到更大的排列。题目规定这时回到最小排列，所以直接反转整个数组，得到 [1,2,3]。','有重复值时逻辑一样。比如 [5,5,2] 也是这些元素的最大排列，所以查找时要跳过相等的一对：寻找 i 用 nums[i]≥nums[i+1] 继续向左，寻找替代值时也必须严格大于 nums[i]。交换相等值不能让排列变大。'],'diagram':'已经最大：[3, 2, 1] → 整体反转 → [1, 2, 3]\n全部相等：[2, 2, 2] → 整体反转 → [2, 2, 2]'}
 ],
 'steps':['找要增大的位置 i：从 i=n−2 开始，只要 nums[i]≥nums[i+1] 就向左走。这些位置属于已经排到最大的后缀。本例依次越过 4≥2、5≥4，在 3<5 处停下，i=1。','如果 i≥0，找替代位置 j：从数组末尾向左走，跳过所有不大于 nums[i] 的数。本例跳过 2，选中 4，j=3。交换 nums[i] 与 nums[j]，得到 [1,4,5,3,2]。','反转 i 右边的后缀：left=i+1、right=n−1，从两端向中间交换。本例把 [5,3,2] 变成 [2,3,5]，最终为 [1,4,2,3,5]。','如果第一步使 i 变成 −1，说明已经是最大排列，跳过寻找 j 和交换。第三步的 left=i+1=0 会自然地反转整个数组，不需要另一套反转逻辑。'],
 'invariant':['为什么一定比原排列大：i 左边的元素全部保留，位置 i 换成严格更大的值，字典序在这里就决定了大小；随后怎样重排后缀都不会改变这个结论。','为什么没有跳过更接近的结果：保留 nums[i] 及它左边所有元素时，右侧已经是最大排列，无法得到更大结果；改变更早的位置又会使结果更大。因此应在 i 处改变，再选最小可用的更大值，并让剩余后缀取最小顺序。这三次选择逐层排除了更大的候选。'],
 'examples':[{'label':'只让靠右的一个位置尽量小地变大','input':'nums=[1,3,5,4,2]','output':'[1,4,2,3,5]','frames':[
  {'title':'末尾已经排到最大，必须动到 3','note':'从右比较：4≥2，继续；5≥4，继续；3<5，停下。固定 [1,3] 后，[5,4,2] 无法排得更大，因此把 i 停在 3 所在的下标 1。','array':[1,3,5,4,2],'active':[2,3,4],'pointers':{'i':1},'diagram':'保留 [1] | 要增大的数 3 | 已经最大的后缀 [5,4,2]'},
  {'title':'3 换成 4，比换成 5 更接近原排列','note':'从右挑选：2 不大于 3，跳过；4 大于 3，停止。[1,4,…] 小于所有 [1,5,…]，所以选 4。i=1、j=3 是下标，待交换的值分别为 3 和 4。','array':[1,3,5,4,2],'active':[1,3],'pointers':{'i':1,'j':3}},
  {'title':'交换后已经更大，但还不是紧挨着的结果','note':'得到 [1,4,5,3,2]。第二位的 4 已保证整体大于原排列，所以现在可以放心把后面排得尽量小。后缀是 [5,3,2]，仍然满足 5≥3≥2。','array':[1,4,5,3,2],'active':[0,1],'diagram':'已确定的前缀 [1,4] | 仍然非递增的后缀 [5,3,2]'},
  {'title':'反转后缀，让这个更大的结果尽量小','note':'只反转 i 右边的 [5,3,2]，变成 [2,3,5]。结果 [1,4,2,3,5] 是所有以 [1,4] 开头的排列中最小的，也就是原排列的下一项。','array':[1,4,2,3,5],'active':[2,3,4],'equation':'下一排列 = [1,4,2,3,5]'}
 ]},{'label':'当前已经是最大排列','input':'nums=[3,2,1]','output':'[1,2,3]','frames':[
  {'title':'找不到任何上升对','note':'从右到左始终满足前一个值不小于后一个值，最终 i=−1。不存在更大的排列。','array':[3,2,1],'active':[0,1,2]},
  {'title':'整个数组反转为最小排列','note':'后缀起点 i+1=0，因此统一的反转步骤恰好反转全数组。','array':[1,2,3],'active':[0,1,2],'metrics':[['结果','最小字典序']]}
 ]}],
 'walkthrough':['i=1 的含义是：只重排它右侧的 [5,4,2] 已经无法变大，所以必须改动 nums[1]=3。i 是下标，3 才是待增大的值。','j=3 的含义是：nums[3]=4 是后缀里严格大于 3 的最小值。交换后第二位从 3 变成 4，已经保证了整个排列变大。','反转区间从 i+1=2 开始，只包括 [5,3,2]。位置 i 上刚选好的 4 必须保留，不能一起反转。后缀越小，最终结果就越接近原排列。'],
 'code':'''
class Solution:
    def nextPermutation(self, nums: list[int]) -> None:
        # 原地求下一排列；下面始终以 [1,3,5,4,2] 为例。
        n = len(nums)
        # 第一步：从右找必须增大的位置。最后一个数没有右邻居，从倒数第二位开始。
        i = n - 2
        # 跳过非递增后缀：4≥2、5≥4 都继续，直到 3<5，得到 i=1。
        while i >= 0 and nums[i] >= nums[i + 1]:
            i -= 1
        # i=-1 表示整个数组已经最大，不必交换，后面直接整体反转。
        if i >= 0:
            # 第二步：从右挑严格大于 nums[i] 的最小数。
            j = n - 1
            # 本例先跳过 2，再找到 4。必须跳过相等值，否则排列不会变大。
            while nums[j] <= nums[i]:
                j -= 1
            # 交换 3 与 4，得到 [1,4,5,3,2]，此时整体已经变大。
            nums[i], nums[j] = nums[j], nums[i]
        # 第三步：只反转 i 右边的后缀，不动刚选好的 4。
        left, right = i + 1, n - 1
        # 后缀仍非递增；反转 [5,3,2] 得 [2,3,5]，才是最小的更大排列。
        while left < right:
            # 从两端向中间交换，最终原数组变为 [1,4,2,3,5]。
            nums[left], nums[right] = nums[right], nums[left]
            left += 1
            right -= 1
'''.strip(),
 'code_notes':['寻找上升对时用 ≥ 跳过相等值，确保后缀非递增；寻找替换者时用 ≤ 跳过不够大的值，确保枢轴严格变大。','i=−1 时后缀从 0 开始，统一的反转代码自然处理“已经最大”的情况。','找到 i 后必然有 nums[i+1]>nums[i]，所以从右向左寻找 j 一定能找到合法替代，不需要把 j 扫到数组外。'],
 'pitfalls':['寻找 i 的扫描方向是从右往左，但比较的仍是相邻元素 nums[i] 和它右边的 nums[i+1]。停止条件是左边的数更小，不是把比较号反过来。','替代值必须严格更大；交换相等值不会让排列增加。若最小替代值出现多次，应选最右边那个，才能保证交换后后缀仍非递增。','只交换不反转后缀会得到更大的排列，但通常不是紧接着的那个。反转从 i+1 开始，不能包含位置 i，也不能在找到了 i 时把整个数组排序。'],
 'complexity':'时间 O(n)，寻找枢轴、寻找替代值和后缀反转均为线性；额外空间 O(1)。',
 'quiz':{'question':'为什么 [1,5,1] 的下一排列是 [5,1,1]，重复的 1 不会破坏推导？','answer':'右侧 [5,1] 非递增，枢轴为第一个 1。唯一严格更大的替代值是 5，交换后后缀 [1,1] 已最小。相等值通过 ≥、≤ 条件正确处理，算法针对的是多重集合排列。'},
 'tests':{'method':'nextPermutation','cases':[{'args':[[1,3,5,4,2]],'mutated_arg':0,'expected':[1,4,2,3,5]},{'args':[[1,2,3]],'mutated_arg':0,'expected':[1,3,2]},{'args':[[3,2,1]],'mutated_arg':0,'expected':[1,2,3]},{'args':[[1,1,5]],'mutated_arg':0,'expected':[1,5,1]},{'args':[[1,5,1]],'mutated_arg':0,'expected':[5,1,1]},{'args':[[1]],'mutated_arg':0,'expected':[1]}]}
})

CHAPTER['problems'].append({
 'id':14,'slug':'longest-common-prefix',
 'summary':'给定一个非空字符串数组，找出所有字符串共同拥有的最长开头片段。若连第一个字符都不相同，或某个字符串为空，结果为空字符串。',
 'baseline':'先把第一个字符串作为候选前缀，遇到不能匹配的其他字符串就不断缩短它，这可以正确求解，但反复切片和重新比较较难看清工作量。按字符位置从左到右纵向检查，可以每一列只验证一次。',
 'insight':'公共前缀必须从下标 0 连续开始。第一个字符串的每个字符都是候选；只有所有其他字符串在相同位置也有同样字符，这个位置才可以延伸进答案。一旦某个位置不一致或有字符串已结束，公共前缀就恰好停在这里，后面是否再次相同没有意义。',
 'steps':['取第一个字符串 first 作为逐字符参照，不复制它。','按下标 i 和字符 ch 遍历 first。对每个字符串 word 检查 i 是否还在其长度内。','若某个 word 已经结束，或 word[i]!=ch，立即返回 first[:i]。下标 i 是第一个不能进入公共前缀的位置。','如果 first 的全部字符都通过，first 本身就是最长公共前缀，返回它。'],
 'invariant':'开始检查第 i 列前，所有字符串的前 i 个字符完全相同。若第 i 列失败，任何更长的共同前缀都必须包含这一列，因此不可能存在；若第 i 列全部通过，不变量自然扩展到 i+1。',
 'examples':[{'label':'第三列失败，前缀就是 fl','input':'strs=["flower","flow","flight"]','output':'"fl"','frames':[
  {'title':'第一列全部是 f','note':'先比较所有字符串下标 0 的字符，全部相同，公共前缀至少为 f。','table':{'headers':['字符串','下标 0','下标 1','下标 2'],'rows':[['flower','f','l','o'],['flow','f','l','o'],['flight','f','l','i']]},'metrics':[['已确认前缀','f']]},
  {'title':'第二列全部是 l','note':'所有下标 1 的字符也是 l，已确认前缀扩展为 fl。','array':['f','l','o','w','e','r'],'active':[0,1],'pointers':{'i':1},'metrics':[['已确认前缀','fl']]},
  {'title':'第三列中 o 与 i 不同','note':'first[2]=o，而 flight[2]=i。不论后面还出现什么字符，公共前缀都不能跨过这里，返回 first[:2]。','diagram':'flower\nflow\nflight\n  ↑ 第 2 列出现差异','equation':'返回 first[:2] = "fl"'}
 ]},{'label':'短字符串提前结束','input':'strs=["abc","ab"]','output':'"ab"','frames':[
  {'title':'前两列都相同','note':'已经确认 ab。准备检查 i=2 时，第二个字符串的长度正好为 2。','array':['a','b','c'],'active':[0,1],'pointers':{'i':2}},
  {'title':'字符串结束也是停止边界','note':'不能读取 word[2]，先发现 i≥len(word)，直接返回 first[:2]。','diagram':'abc\nab|  ← 第二个字符串已结束','metrics':[['答案','ab']]}
 ]}],
 'walkthrough':['纵向比较并不要求先找到最短字符串，长度检查会在必要时自动停止。','当第一个字符串恰好是最短且被所有其他字符串包含为前缀，外层循环完整结束，直接返回 first。','如果 first 为空，循环不会执行，返回空字符串；如果后面的某个字符串为空，第一列检查长度时就返回空。'],
 'code':'''
class Solution:
    def longestCommonPrefix(self, strs: list[str]) -> str:
        # 用第一个单词逐列比较，答案一定是它的某个前缀。
        first = strs[0]
        for i, ch in enumerate(first):
            for word in strs:
                # 任一单词已结束，或当前字符不同，共同前缀就在这一列之前结束。
                if i >= len(word) or word[i] != ch:
                    # 切片不含下标 i，因为这一列已经不相同。
                    return first[:i]
        return first
'''.strip(),
 'code_notes':['or 从左向右短路：先检查长度，越界时不会再读取 word[i]。','first[:i] 不包含失败的第 i 个字符，恰好保留之前已经确认的部分。','内层直接遍历 strs，避免每一列都用 strs[1:] 创建一份新列表；把 first 自己再比较一次只增加常数比例的工作。'],
 'pitfalls':['公共前缀必须从开头连续，不能把中间相同片段当作答案。','不能只比较第一个和最后一个输入字符串，原输入未排序时，中间字符串可能有更短的共同前缀。','长度检查必须在字符索引之前，否则遇到短字符串会抛出 IndexError。'],
 'complexity':'设字符串数为 n、最短长度为 L，字符比较至多 O(n(L+1))，也可按实际检查的字符总量计；除返回切片外额外空间 O(1)，输出最多 O(L)。',
 'quiz':{'question':'["abx","aby","ac"] 为什么不能返回 "ab"？','answer':'第三个字符串的下标 1 是 c，与前两个的 b 不同，因此第 1 列失败，只能保留更早的共同前缀 a。公共条件必须对所有字符串成立。'},
 'tests':{'method':'longestCommonPrefix','cases':[{'args':[['flower','flow','flight']],'expected':'fl'},{'args':[['dog','racecar','car']],'expected':''},{'args':[['abc','ab']],'expected':'ab'},{'args':[['','abc']],'expected':''},{'args':[['single']],'expected':'single'},{'args':[['abx','aby','ac']],'expected':'a'}]}
})

CHAPTER['problems'].append({
 'id':165,'slug':'compare-version-numbers',
 'summary':'版本号由点分隔的非负整数段组成，从左到右逐段按数值比较，忽略段内前导零。缺失的后续段视为零。version1 更小返回 −1，更大返回 1，完全等价返回 0。输入保证格式合法，每段数值在 32 位整数范围内。',
 'baseline':'用 split(".") 分出各段、转为整数再逐段比较，思路清楚但需要保存分割结果。不能直接比较整串字典序，也不能把它当浮点数，因为每段的位数和段数都不固定。逐字符扫描可以避免分配所有段。',
 'insight':'一段遇到点或字符串末尾才结束。在这之前用 value=value×10+当前数字累积段值，前导零自然没有影响。两边每次各读一段再比较；某边已经结束，就让它本轮段值保持为零，继续比较另一边剩余段。',
 'steps':['初始化两个扫描下标 i=0、j=0，只要任一版本号还有内容就继续。','把 x、y 重新置零。分别从当前下标开始读到点或末尾，每个数字都乘十累加进对应段值。','若 x 与 y 不同，立即返回大小关系；更晚的段不能改变第一个不同段决定的顺序。','若相同，分别跳过尚存在的点分隔符，继续读下一段。字符串已结束的一侧不再移动，下一轮段值仍为零。','两边都结束且没有数值差异，返回 0。'],
 'invariant':'每轮开始前，已读完的对应段全部相等，两个下标位于下一段开头或各自末尾。每次累积精确得到当前段的整数值；缺失段为零统一了不同段数的情况。第一个不等段按题目比较规则即可决定最终结果。',
 'examples':[{'label':'前导零与缺失零段同时出现','input':'version1="1.01.0", version2="1.001"','output':'0','frames':[
  {'title':'第一段都为 1','note':'两边扫描到第一个点，得到 x=y=1，前缀版本段相等，继续向后。','table':{'headers':['段号','version1','version2','数值比较'],'rows':[[0,'1','1','1 = 1']]}},
  {'title':'01 与 001 都累积为 1','note':'前导零不改变整数段值。不能因字符长度不同就判定版本不同。','diagram':'"01"：0 → 0×10+1 = 1\n"001"：0 → 0 → 0×10+1 = 1','metrics':[['当前段关系','相等']]},
  {'title':'最后的 0 对应一个缺失段','note':'version2 已结束，本轮 y 保持 0；version1 的最后一段也是 0。两边所有段都相等，返回 0。','table':{'headers':['段号','version1 数值','version2 数值'],'rows':[[0,1,1],[1,1,1],[2,0,'缺失 → 0']]}}
 ]},{'label':'版本段不能按字符串字典序比较','input':'version1="1.2", version2="1.10"','output':'-1','frames':[
  {'title':'主版本段相等','note':'第一段都是 1，比较第二段。','diagram':'1 | 2\n1 | 10'},
  {'title':'比较整数 2 与 10','note':'2<10，所以返回 −1。字符串比较会因首字符 2>1 得出相反结论，说明必须按段数值处理。','equation':'第二段：2 < 10 → 返回 −1','diagram':'"2" 的数值是 2\n"10" 的数值是 10'}
 ]}],
 'walkthrough':['1.01.0 与 1.001 的字符序列不同，但规范化后的段数值分别为 [1,1,0] 与 [1,1]，补零后完全一致。','某边读完并不意味着它更小；若另一边剩余全是零，两个版本仍相等。','每段值比较后才能进入下一段，后面的巨大段号也不能推翻更前面的大小关系。'],
 'code':'''
class Solution:
    def compareVersion(self, version1: str, version2: str) -> int:
        # 两个下标分别扫描两串版本号，不额外创建分段数组。
        i = j = 0
        m, n = len(version1), len(version2)
        # 一边提前结束时，缺失的后续段按 0 比较。
        while i < m or j < n:
            # 每轮重新累计一个版本段；逐位读数会自然忽略前导零。
            x = y = 0
            while i < m and version1[i] != '.':
                x = x * 10 + ord(version1[i]) - ord('0')
                i += 1
            while j < n and version2[j] != '.':
                y = y * 10 + ord(version2[j]) - ord('0')
                j += 1
            # 版本号按第一个不同的整数段决定大小。
            if x != y:
                return 1 if x > y else -1
            # 跳过当前小数点，让下一轮从下一段开始。
            if i < m:
                i += 1
            if j < n:
                j += 1
        return 0
'''.strip(),
 'api':{'signature':'ord(c: str) -> int','description':['ord 接收一个字符，返回其 Unicode 码点。ASCII 数字 0..9 连续排列，因此 ord(ch)-ord("0") 可以把一个数字字符转换为数值 0..9。','本题保证段内都是数字，所以可以直接转换；对任意外部字符串做解析时，需要像后面的 atoi 与 IP 校验一样先检查字符范围。']},
 'code_notes':['外层使用 or，任一边还有段就要继续；用 and 会忽略较长版本号的剩余段。','x、y 每轮重置为 0，使已结束的一侧自然贡献缺失零段。','两次 i/j 增加分两种用途：内部循环消费数字，内部结束后的一次增加跳过点。不要漏掉分隔符，否则下一轮会停在同一个点。','原题每段可放入 32 位整数，逐位累积的数值范围受控；本解法只保存当前两段，不保存全部分割片段。'],
 'pitfalls':['版本号不是小数，例如 1.10 表示第二段为 10，不等于十进制小数 1.1。','字符串长度或字典序不能代表版本大小，前导零与多位段都会导致误判。','一边结束就立刻判它更小会把 1.0 与 1.0.0 错误区分。'],
 'complexity':'时间 O(m+n)，每个字符至多扫描一次；在题目段值范围内，额外空间 O(1)。',
 'quiz':{'question':'"2.0" 和 "1.999999" 哪个更大，为什么不必比较最后一段？','answer':'第一段已经有 2>1，版本顺序由第一个不同段决定，因此 2.0 更大。后面段号再大也不能弥补主版本段的差异。'},
 'tests':{'method':'compareVersion','cases':[{'args':['1.01.0','1.001'],'expected':0},{'args':['1.2','1.10'],'expected':-1},{'args':['1.0','1.0.0.0'],'expected':0},{'args':['0.1','1.1'],'expected':-1},{'args':['1.0.1','1'],'expected':1},{'args':['2.0','1.999999'],'expected':1}]}
})

CHAPTER['problems'].append({
 'id':415,'slug':'add-strings',
 'summary':'两个非负整数以十进制字符串形式给出，返回它们相加后的字符串。输入除单个 "0" 外没有前导零，不能把整个字符串直接转换成整数或调用大整数加法来代替逐位实现。',
 'baseline':'直接 int(num1)+int(num2) 会绕过题目要考查的逐位处理，也违反转换限制。纸上的竖式加法提供了线性方案：从个位开始，把当前两位和上一位进位相加，写下个位，把十位传给下一轮。',
 'insight':'两个字符串末尾都是个位，因此从右向左对齐读取。某一边已经没有数字时，视作当前位为零；即使两边都结束，若 carry 仍为 1，还要继续处理一次。每位最大 9+9+1=19，所以进位始终只需一个小整数。',
 'steps':['初始化 i、j 为两串末尾下标，carry=0，digits=[]，用于按从低位到高位收集结果。','只要任一字符串还有字符，或 carry 非零，就继续。缺失的位当作 0，已有字符用 ord(ch)-ord("0") 转成单个数字。','计算 total=x+y+carry，用 divmod(total,10) 同时取得新进位和本位数字。','把本位数字转为单个字符串追加到 digits，i、j 各减一。','最后反向读取 digits 并连接，恢复从最高位到最低位的通常表示。'],
 'invariant':'处理完低 k 位后，digits 已保存这 k 位正确结果的逆序，carry 是尚未写出的向更高一位进位。逐位计算 x+y+carry 的余数决定当前位，商决定新进位，正好对应十进制位权分解。最终 carry 也被写出后，所有位都正确。',
 'examples':[{'label':'最后的进位也需要一轮','input':'num1="99", num2="1"','output':'"100"','frames':[
  {'title':'个位：9+1=10','note':'个位写 0，进位为 1。两个字符串按末尾对齐，较短字符串的十位视作零。','diagram':'  9 9\n+   1\n-----\n    0    carry=1','metrics':[['逆序结果','["0"]']]},
  {'title':'十位：9+0+1=10','note':'再写一个 0，进位仍为 1。此时两边的原始数字都已经读完。','diagram':'  9 9\n+ 0 1\n-----\n  0 0    carry=1','metrics':[['逆序结果','["0","0"]']]},
  {'title':'额外一轮写出最终进位','note':'x=y=0，carry=1，本轮写 1，新的 carry=0，得到逆序字符列表 ["0","0","1"]。','array':['0','0','1'],'metrics':[['读取方向','低位 → 高位']]},
  {'title':'反向连接成正常十进制表示','note':'反向读取列表得到 1、0、0，连接后返回 "100"。','diagram':'["0","0","1"] → reversed → ["1","0","0"]\n连接 → "100"'}
 ]}],
 'walkthrough':['两边长度不同时，不必先补齐字符串，只在下标越界的一侧使用数字零即可。','如果循环条件只检查 i、j，会在十位处理完后立刻退出，漏掉 carry=1，得到错误的 "00"。','从末尾读自然先得到低位，用列表 append 后整体反向连接，比每轮把一个字符插到长字符串前面更高效。'],
 'code':'''
class Solution:
    def addStrings(self, num1: str, num2: str) -> str:
        # 从个位向高位相加，两串长度可以不同。
        i, j = len(num1) - 1, len(num2) - 1
        # carry 是上一位向当前位的进位，digits 暂按低位到高位保存。
        carry = 0
        digits = []
        # 所有数字读完后，最后一位进位仍要输出。
        while i >= 0 or j >= 0 or carry:
            # 某串已读完时用 0 补齐；逐字符转数值，不把整串转成大整数。
            x = ord(num1[i]) - ord('0') if i >= 0 else 0
            y = ord(num2[j]) - ord('0') if j >= 0 else 0
            # divmod 同时给出进位（商）与本位数字（余数）。
            carry, digit = divmod(x + y + carry, 10)
            digits.append(str(digit))
            i -= 1
            j -= 1
        # 收集顺序是反的，输出前恢复高位在前。
        return ''.join(reversed(digits))
'''.strip(),
 'api':{'signature':'divmod(a: int, b: int) -> tuple[int, int]\nreversed(sequence) -> iterator','description':['divmod 返回 (a//b,a%b)。对本题非负的逐位总和，除以 10 的商是进位，余数是当前要写的数字，顺序不能颠倒。','reversed 返回逆向迭代器，不原地改变 digits，也不必创建一份反向列表。join 消费迭代器中的字符并创建最终字符串。str(digit) 只把 0..9 的单个小整数转成字符，不涉及把完整输入转成大整数。']},
 'code_notes':['三个 or 条件覆盖长短不等与最终进位的情况，任何一个仍有未处理信息都要继续。','i、j 可以降到负数，条件表达式会先检查下标再读取，避免把负下标当作有效数字。','carry 最大为 1，total 最大为 19，代码中的整数运算始终是逐位的小范围运算。'],
 'pitfalls':['从左向右直接逐位加会受到右侧未知进位影响，普通竖式应从低位开始。','漏掉最后的 carry 会在 99+1、999+1 等例子上失败。','在循环中反复 answer=新字符+answer 会重复复制越来越长的字符串，不能据此直接声称线性时间。'],
 'complexity':'时间 O(max(m,n))，结果至多多一位；空间 O(max(m,n))，保存结果字符与返回字符串。',
 'quiz':{'question':'两个输入都为 "0" 时，为什么不会返回空字符串？','answer':'初始 i=j=0，循环至少处理一轮，x+y+carry=0，明确向 digits 追加 "0"。随后条件全为假，连接结果正好是 "0"。'},
 'tests':{'method':'addStrings','cases':[{'args':['99','1'],'expected':'100'},{'args':['11','123'],'expected':'134'},{'args':['456','77'],'expected':'533'},{'args':['0','0'],'expected':'0'},{'args':['1','999'],'expected':'1000'},{'args':['12345','0'],'expected':'12345'}]}
})

CHAPTER['problems'].append({
 'id':8,'slug':'string-to-integer-atoi',
 'summary':'按给定规则读取字符串开头的整数：先跳过前导空格，再读取可选的一个正负号，随后连续读十进制数字，遇到第一个非数字即停止。未读到数字返回 0，超出 32 位有符号整数范围则截到对应上下界。',
 'baseline':'直接 int(s) 不能处理 "42abc" 这类需要只读数字前缀的输入，也没有自动完成题目指定的截断规则。先抽出数字片段再转换虽然可写，但会隐藏边界处理过程。按阶段扫描并逐位累积能把每条规则对应到明确状态。',
 'insight':'解析分为只能向前推进的三个阶段：前导空格、可选符号、数字前缀。进入数字阶段后遇到任何非数字都结束，不能跳过它继续寻找后面的数字。累积 value×10+digit 前先判断是否超过对应符号允许的绝对值上界，避免依赖大整数或已经溢出的中间值。',
 'steps':['用 i 从 0 开始，只跳过开头的普通空格。','若当前位置是 + 或 −，记录 sign 并只消费一个符号；默认 sign=1。','根据 sign 选择绝对值 limit：正数为 2³¹−1，负数为 2³¹。初始化 value=0。','连续读取字符 0..9。若 value>limit//10，或 value==limit//10 且本位 digit>limit%10，立即返回对应符号的截断边界。','否则 value=value×10+digit 并前进。遇到非数字或末尾后返回 sign×value。'],
 'invariant':'进入数字循环前，i 位于可能的首个数字，sign 已确定。每轮开始时 value 精确表示已读数字前缀的非负数值且不超过 limit。预先比较商和余数，等价于检查再追加一个十进制位是否越界；未越界就安全扩展前缀，越界后继续追加数字也无法把绝对值变小，直接截断正确。',
 'examples':[{'label':'只解析合法的数字前缀','input':'s="   -0042xyz"','output':'-42','frames':[
  {'title':'跳过三个前导空格','note':'i 从 0 到 3，停在负号。只有这一阶段会跳过空格，后面数字中的空格是停止标记。','diagram':'[空格 空格 空格] [-] [0 0 4 2] [x y z]\n                    ↑ 接下来读取符号','metrics':[['i',3]]},
  {'title':'读取一个负号，选择负数绝对值上界','note':'sign=−1、limit=2147483648，i=4。负数下界的绝对值比正数上界多一。','diagram':'sign = −1\n允许的 value 最大值 = 2³¹','metrics':[['value',0]]},
  {'title':'两个前导零自然被累积公式忽略','note':'读到 0、0 后 value 仍为 0；再读 4、2，value 依次为 4、42。','table':{'headers':['读到字符','累积结果'],'rows':[['0',0],['0',0],['4',4],['2',42]]}},
  {'title':'遇到 x，数字阶段结束','note':'i=8 的 x 不在 0..9 内，立即停止。后面的 y、z 不再参与解析，返回 −1×42。','diagram':'数字前缀 "0042" → value=42\n符号 −1 → 返回 −42'}
 ]},{'label':'正负边界最后一位不同','input':'"2147483648" 与 "-2147483648"','output':'分别为 2147483647 与 -2147483648','frames':[
  {'title':'前九位都是 214748364','note':'两种符号都允许目前的前缀。最后准备追加 digit=8，value 恰好等于 limit//10。','diagram':'value = 214748364\n下一位 digit = 8'},
  {'title':'正数只允许最后一位至多 7','note':'正数 limit%10=7，8 超过边界，返回 2147483647；负数 limit%10=8，追加 8 仍合法，返回 −2147483648。','table':{'headers':['符号','limit','最后允许位','结果'],'rows':[['正',2147483647,7,2147483647],['负',2147483648,8,-2147483648]]}}
 ]}],
 'walkthrough':['前导零不需要额外删除，0×10+0 仍为 0，逐位累积会自然消去它们的数值影响。','符号只允许出现在跳过前导空格之后的一处。因此 "+-12" 读完 + 后立即遇到非数字 −，结果为 0，不能把多个符号合并。','"words 987" 开头不是数字或允许的符号，数字循环一次也不执行，返回 0；不能继续扫描到后面的 987。','正数最大值是 2147483647，负数最小值是 −2147483648，绝对值边界不同，所以先按符号选 limit。'],
 'code':'''
class Solution:
    def myAtoi(self, s: str) -> int:
        i, n = 0, len(s)
        # 只跳过开头空格，数字中间出现空格会结束解析。
        while i < n and s[i] == ' ':
            i += 1
        # 可选符号只读一次，缺省为正数。
        sign = 1
        if i < n and s[i] in '+-':
            if s[i] == '-':
                sign = -1
            i += 1
        # 负数允许到 -2^31，绝对值上界比正数多 1。
        limit = 2**31 - 1 if sign == 1 else 2**31
        value = 0
        # 只接受 ASCII 数字，遇到其他字符立即停止。
        while i < n and '0' <= s[i] <= '9':
            digit = ord(s[i]) - ord('0')
            # 在乘 10 加新数字之前检查溢出，超界直接截到题目要求的端点。
            if value > limit // 10 or (value == limit // 10 and digit > limit % 10):
                return sign * limit
            value = value * 10 + digit
            i += 1
        # 返回已经成功读到的部分；没有数字时 value 仍为 0。
        return sign * value
'''.strip(),
 'code_notes':['每次读取 s[i] 前都有 i<n 检查，空串、全空格、只有符号都能安全返回 0。','数字范围显式写为 "0"≤s[i]≤"9"，与题目允许的十进制字符一致。','value 保存绝对值，最后再乘 sign，避免在每一步同时处理正负累积公式。','先判断边界再乘十，讲清了可迁移到固定宽度整型的防溢出思路。Python 自身支持大整数，也仍需按题意限制最终结果到 32 位范围。'],
 'pitfalls':['不是删除所有空格，只允许忽略前导空格，"12 34" 应返回 12。','小数点会结束数字前缀，"3.14" 返回 3；这不是浮点解析。','负数下界可以取到 −2147483648，不能错误限制成 −2147483647。','读到非数字立即结束，不能继续搜集后面的数字或符号。'],
 'complexity':'时间 O(n)，至多扫描字符串一次；额外空间 O(1)，数值始终限制在 32 位绝对值边界内。',
 'quiz':{'question':'"0-1"、"  + 42" 分别返回什么？','answer':'都返回 0。前者数字阶段读到 0 后遇到 − 就停止；后者读取 + 之后遇到空格，数字阶段不能再次跳过空格，因此没有读到数字。'},
 'tests':{'method':'myAtoi','cases':[{'args':['   -0042xyz'],'expected':-42},{'args':['42'],'expected':42},{'args':['words and 987'],'expected':0},{'args':['2147483648'],'expected':2147483647},{'args':['-2147483648'],'expected':-2147483648},{'args':['-2147483649'],'expected':-2147483648},{'args':['+-12'],'expected':0},{'args':[''],'expected':0},{'args':['   '],'expected':0},{'args':['0-1'],'expected':0},{'args':['12 34'],'expected':12},{'args':['3.14'],'expected':3}]}
})

CHAPTER['problems'].append({
 'id':43,'slug':'multiply-strings',
 'summary':'两个非负整数以十进制字符串给出，返回它们乘积的字符串。输入没有多余前导零，每个长度为 1..200。不能直接把完整输入转换成整数，也不能调用大整数乘法库。',
 'baseline':'可以把乘法理解成重复加法，但第二个数很大时，加法次数与数值成正比，远超输入长度。竖式乘法按每一对数字贡献相应数位，运算量只与两个字符串的长度乘积有关。',
 'insight':['乘积最多有 m+n 位，先准备这么多位置。num1[i] 的权重是 10^(m−1−i)，num2[j] 的权重是 10^(n−1−j)，相乘后权重为 10^(m+n−2−i−j)。在长度 m+n 的结果数组中，这个权重恰好对应下标 i+j+1。','先把所有小乘积累加到对应位置，暂时允许一个位置超过 9；等贡献全部累积完成，再从右向左统一处理进位。这样把“贡献放在哪里”和“如何写成十进制”拆成两个清楚的阶段。'],
 'steps':['任一输入为 "0"，直接返回 "0"；否则分配 m+n 个零。','枚举两边的数字下标 i、j，计算单个数字 a、b，把 a×b 加到 digits[i+j+1]。','从最后一个位置走到下标 1：把该位置除以 10 的商加到左邻位置，自己只保留余数。','去掉结果数组开头的零，把剩余的单个数字转成字符并连接。'],
 'invariant':['累积阶段，数组表示的加权总和始终等于已经处理过的数字对的乘积贡献；所有数字对完成后，这个加权总和就是原乘积。某个格子大于 9 只表示尚未标准化，不代表乘积算错。','进位阶段处理位置 k 时，比 k 更靠右的位置已规范成 0..9；将 digits[k] 的十位转移到 k−1 不改变加权总和。因为从右向左处理，左侧之后还会接收并继续传递进位。两数都小于相应的 10 次幂，所以总乘积小于 10^(m+n)，最终最高位也不会超过 9。'],
 'examples':[{'label':'先累计贡献，再统一进位','input':'num1="12", num2="34"','output':'"408"','frames':[
  {'title':'准备四位，确定每个小乘积的位置','note':'m=n=2。个位乘个位进入下标 3；十位乘个位、个位乘十位都进入下标 2；十位乘十位进入下标 1。','table':{'headers':['数字对','贡献','结果下标'],'rows':[['1×3',3,1],['1×4',4,2],['2×3',6,2],['2×4',8,3]]}},
  {'title':'所有贡献累计为 [0,3,10,8]','note':'中间的 10 表示十位上有十份，尚未进位。按权重解释仍然是 0×1000+3×100+10×10+8=408。','array':[0,3,10,8],'active':[2],'array_label':'尚未标准化的数位'},
  {'title':'从右向左，十位向百位进一','note':'个位 8 不进位；十位 10 除以 10 得商 1、余数 0，所以百位从 3 变成 4，十位写 0。','array':[0,4,0,8],'active':[1,2],'changed':2},
  {'title':'最高位为零，跳过后连接','note':'百位 4 无需继续进位，最终有效数字为 4、0、8。这里只去掉前导零，结果中间的 0 必须保留。','diagram':'[0, 4, 0, 8]\n    └──────→ "408"'}
 ]},{'label':'进位可以连续传递','input':'num1="99", num2="99"','output':'"9801"','frames':[
  {'title':'两个交叉项都进入十位','note':'原始累积为 [0,81,162,81]。不能把每个位置直接转成字符串拼接。','array':[0,81,162,81]},
  {'title':'先规范个位，再规范十位','note':'个位 81 留 1、向左进 8，使十位变为 170；十位留 0、向左进 17，使百位变为 98。','table':{'headers':['刚处理的位置','数组'],'rows':[['个位','[0,81,170,1]'],['十位','[0,98,0,1]']]}},
  {'title':'百位继续向千位进九','note':'98 留 8、向左进 9，得到 [9,8,0,1]。这次四位都需要保留。','array':[9,8,0,1],'equation':'99 × 99 = 9801'}
 ]}],
 'walkthrough':['12×34 可以展开成 10×30+10×4+2×30+2×4。两个交叉项都属于十位，所以它们在同一位置相加，而不是各占一个新的格子。','i+j+1 是从两边位权相乘推出来的。若背成 i+j，所有贡献会向左错一位；若为每对数字单独追加结果，则完全丢失了位权。','规范化必须从右向左，因为右侧进位会改变左侧。如果先规范左边，它后来收到新的进位后还可能再次超过 9。','两个输入都非零时乘积一定非零，因此跳过前导零后至少剩一位；代码仍保留最后一个位置，使边界行为直观。'],
 'code':'''
class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        # 零乘任何数为零，也避免输出多余前导零。
        if num1 == '0' or num2 == '0':
            return '0'
        m, n = len(num1), len(num2)
        # m 位与 n 位相乘，结果最多 m+n 位。
        digits = [0] * (m + n)
        for i in range(m):
            a = ord(num1[i]) - ord('0')
            for j in range(n):
                b = ord(num2[j]) - ord('0')
                # 第 i、j 位的乘积累加到对应位，暂时允许该格超过 9。
                digits[i + j + 1] += a * b
        # 从低位向高位统一进位，保证较高位接收完全部贡献再处理。
        for k in range(m + n - 1, 0, -1):
            digits[k - 1] += digits[k] // 10
            digits[k] %= 10
        start = 0
        # 跳过前导零，但至少保留一位数字。
        while start < len(digits) - 1 and digits[start] == 0:
            start += 1
        return ''.join(str(digits[k]) for k in range(start, len(digits)))
'''.strip(),
 'code_notes':['ord 只提取单个数字的值，没有转换完整输入。每次小乘法的两个因数均在 0..9。','digits 的格子在第一阶段是位权系数，在第二阶段结束后才保证都是十进制数字。给中间状态起清楚的名字，有助于解释为什么临时出现 162 是正确的。','进位循环止于下标 1，访问 k−1 始终有效；最高位的合法性由乘积位数上界保证。','最后用生成器逐位产生字符并由 join 连接，避免循环中反复拼接整个结果。'],
 'pitfalls':['不能用 str(int(num1)*int(num2)) 规避题目的核心限制。','m 位乘 n 位可能产生 m+n 位，例如 99×99=9801，结果数组不能只分配 max(m,n) 位。','不能把所有零都删除；100×10 的结果是 1000，只有最左侧多余零应被去掉。','先取余再算进位会丢失商，必须先保存或转移原值的进位部分。'],
 'complexity':'时间 O(mn+m+n)，通常写为 O(mn)；额外空间 O(m+n)，用于数位数组和结果。这里按题目至多 200 位的范围分析小整数运算成本。',
 'quiz':{'question':'为什么最左边的 digits[0] 在累积阶段一直为零？它有什么作用？','answer':'i、j 最小为 0，所以最小写入下标为 i+j+1=1。下标 0 专门承接最高位置的进位，例如 99×99 最后把百位 98 的商 9 写到千位。'},
 'tests':{'method':'multiply','cases':[{'args':['12','34'],'expected':'408'},{'args':['99','99'],'expected':'9801'},{'args':['123','456'],'expected':'56088'},{'args':['0','123'],'expected':'0'},{'args':['999','0'],'expected':'0'},{'args':['1','9999'],'expected':'9999'},{'args':['100','10'],'expected':'1000'}]}
})

CHAPTER['problems'].append({
 'id':468,'slug':'validate-ip-address',
 'summary':'按本题规定的完整格式判断输入属于 IPv4、IPv6 还是 Neither。IPv4 必须有四段十进制数，每段 0..255 且无多余前导零；IPv6 必须有八段，每段为 1..4 位十六进制字符，可以有前导零。',
 'baseline':'直接调用 int 或网络地址解析器不能替代题目的格式规则：整数转换可能接受前导零，而通用 IPv6 解析器还可能接受本题没有纳入的压缩形式。先验证结构，再逐段验证字符和范围，更容易逐条对应题意。',
 'insight':['格式校验可以分成两层。第一层检查分隔符与段数；第二层检查每段的长度、字符集合以及额外约束。一条规则不满足就立即返回 Neither，只有全部通过才返回地址类别。','本题只接受八段齐全的 IPv6 写法，因此 :: 压缩形式在这道题中应返回 Neither。这是题目规定的范围，不是在否定真实网络协议中的 IPv6 压缩表示。'],
 'steps':['如果包含点号，按点号切分，要求恰好四段。','逐段检查长度为 1..3、字符全为 ASCII 数字；长度大于 1 时不能以 0 开头；最后再把这一小段转成整数检查是否不超过 255。','否则若包含冒号，按冒号切分，要求恰好八段，每段长度为 1..4，且每个字符属于 0..9、a..f、A..F。','没有对应分隔符，或者任何规则失败，返回 Neither；所有段通过则返回 IPv4 或 IPv6。'],
 'invariant':'逐段循环开始时，已经检查的段都满足相应类别的全部局部规则。段数在循环前已确认，循环结束便同时满足全局结构和所有局部约束。显式分隔符切分会保留空段，因此连续分隔符、开头分隔符、结尾分隔符都不会被悄悄忽略。',
 'examples':[{'label':'IPv4 的零值与前导零不同','input':'"172.16.0.1" 与 "172.16.00.1"','output':'分别为 IPv4、Neither','frames':[
  {'title':'先检查恰好四段','note':'两个字符串都能切成四段。段数正确只是第一层，不能立刻返回 IPv4。','table':{'headers':['输入','切分结果'],'rows':[['172.16.0.1','172 | 16 | 0 | 1'],['172.16.00.1','172 | 16 | 00 | 1']]}},
  {'title':'合法的单个零通过所有规则','note':'"0" 长度为 1，是数字，数值不超过 255。前导零规则只禁止长度超过 1 时以零开头。','array':['172','16','0','1'],'active':[2],'metrics':[['结果','IPv4']]},
  {'title':'"00" 不能用数值相同来豁免','note':'虽然 int("00") 也是 0，但原始格式包含多余前导零。必须在数值比较之外保留字符层面的校验。','array':['172','16','00','1'],'active':[2],'metrics':[['结果','Neither']]}
 ]},{'label':'IPv6 的八段与字符规则','input':'"2001:db8:0:0:0:0:0:1"；"2001:db8::1"','output':'分别为 IPv6、Neither','frames':[
  {'title':'完整八段逐个验证','note':'db8 包含合法的十六进制字母，单个 0 也是合法段。每段都在 1..4 个字符范围内，八段全部通过。','table':{'headers':['段号','内容','检查'],'rows':[[1,'2001','4 位合法'],[2,'db8','3 位合法'],[3,'0','合法'],[4,'0','合法'],[5,'0','合法'],[6,'0','合法'],[7,'0','合法'],[8,'1','合法']]}},
  {'title':'连续冒号会留下空段','note':'显式 split(":") 得到四个元素，其中含空字符串。本题要求八个非空段，因此压缩写法在本题中不通过。','diagram':'"2001:db8::1".split(":")\n→ ["2001", "db8", "", "1"]\n段数不为 8 → Neither'},
  {'title':'段数正确仍可能含非法字母','note':'若把最后一段改成 1g，长度虽然合格，但 g 不属于十六进制字符，仍需返回 Neither。','diagram':'允许：0123456789abcdefABCDEF\n"1g"：字符 g 不在允许集合中'}
 ]}],
 'walkthrough':['检查 IPv4 时先验证长度，再访问 part[0]，避免空段触发下标错误。随后检查字符，再调用 int，避免让无效字符触发转换异常。','IPv4 中 256 是字符合法但数值越界；01 是数值合法但格式不合法。把两条规则分开，才能覆盖这两类错误。','IPv6 每段最多四位且字符合法时，其数值自然不超过 FFFF，因此不必再做整数转换。','混用点与冒号的输入会进入点号分支，但相应段包含冒号，无法通过纯数字检查。本题不接受 IPv4 嵌入式 IPv6 这样的额外形式。'],
 'code':'''
class Solution:
    def validIPAddress(self, queryIP: str) -> str:
        if '.' in queryIP:
            # IPv4 必须恰好四段，空段、过长段都非法。
            parts = queryIP.split('.')
            if len(parts) != 4:
                return 'Neither'
            for part in parts:
                if not 1 <= len(part) <= 3:
                    return 'Neither'
                # 只接受 ASCII 十进制数字，不能靠 isdigit 接受其他数字字符。
                if any(not '0' <= ch <= '9' for ch in part):
                    return 'Neither'
                # 除单独的 0 外，不允许前导零。
                if len(part) > 1 and part[0] == '0':
                    return 'Neither'
                # 字符和长度已验证，再检查 IPv4 每段的数值范围。
                if int(part) > 255:
                    return 'Neither'
            return 'IPv4'
        if ':' in queryIP:
            # 本题 IPv6 要求完整八段，不接受 :: 压缩写法。
            parts = queryIP.split(':')
            if len(parts) != 8:
                return 'Neither'
            hex_digits = '0123456789abcdefABCDEF'
            for part in parts:
                # 每段一到四个十六进制字符，大小写均可。
                if not 1 <= len(part) <= 4:
                    return 'Neither'
                if any(ch not in hex_digits for ch in part):
                    return 'Neither'
            return 'IPv6'
        return 'Neither'
'''.strip(),
 'api':{'signature':'str.split(sep: str | None = None, maxsplit: int = -1) -> list[str]\nany(iterable) -> bool','description':['split 显式传入 "." 或 ":" 时，按这个分隔符切分；默认 maxsplit=−1 表示不限制切分次数。连续分隔符以及两端分隔符会产生空字符串，不能先过滤空段。省略 sep 时的空白分词规则不同。','any 对传入的迭代对象逐项取真值，只要遇到一项为真就返回 True；全为假或为空则返回 False。这里传入生成器，表示“是否存在非法字符”。空段单独由长度检查拦截，不依赖 any 来发现。']},
 'code_notes':['所有提前返回都代表一条具体规则失败；最后的类别返回只在全部段通过之后执行。','IPv4 的 int(part) 只转换已验证的至多三位数字，用于范围检查，既安全又不遮掩格式规则。','ASCII 数字范围与题目允许的字符吻合，不用宽泛的 Unicode 数字分类扩展问题范围。','hex_digits 仅有 22 个字符，成员检查成本为常数；改成集合也可以，但不是复杂度优化的关键。'],
 'pitfalls':['不能用 split 后过滤空字符串，否则连续分隔符可能被当成合法输入。','不能统一禁止前导零：IPv4 禁止多余前导零，IPv6 允许。','IPv6 字母只到 f，不能用 isalnum 接受任意英文字母。','题目规定八段完整形式，直接使用支持 :: 压缩的通用库可能接受不符合本题范围的输入。'],
 'complexity':'设字符串长度为 n，扫描与切分时间 O(n)，切分结果占 O(n) 额外空间。每段长度上限和字符集合大小都是常数。',
 'quiz':{'question':'"1.1.1.1." 为什么不合法？如果先删除末尾分隔符会发生什么？','answer':'显式切分得到五段，最后一段为空，不满足四段规则。删除末尾点号会擅自修复输入，使无效地址变成有效地址；校验题应判断原始输入，而不是替用户修正格式。'},
 'tests':{'method':'validIPAddress','cases':[{'args':['172.16.0.1'],'expected':'IPv4'},{'args':['172.16.00.1'],'expected':'Neither'},{'args':['0.0.0.0'],'expected':'IPv4'},{'args':['255.255.255.255'],'expected':'IPv4'},{'args':['256.0.0.1'],'expected':'Neither'},{'args':['1.1.1.1.'],'expected':'Neither'},{'args':['1..1.1'],'expected':'Neither'},{'args':['2001:db8:0:0:0:0:0:1'],'expected':'IPv6'},{'args':['2001:0db8:85a3:0000:0000:8A2E:0370:7334'],'expected':'IPv6'},{'args':['2001:db8::1'],'expected':'Neither'},{'args':['1:2:3:4:5:6:7:1g'],'expected':'Neither'},{'args':['12345:2:3:4:5:6:7:8'],'expected':'Neither'},{'args':['1.2.3:4'],'expected':'Neither'},{'args':[''],'expected':'Neither'}]}
})
